schema_version = 1
project_id = "bolt-v2"
target_namespace = "bolt-v2"

[commands]
test = ["cargo", "nextest", "run"]
clippy = ["cargo", "clippy"]

[commands.build]
argv = ["cargo", "zigbuild", "--release"]
target_dir_mode = "env"
artifact_layout = "cargo"
