schema_version = 1
project_id = "bolt-v2"
target_namespace = "bolt-v2"

[commands]

[commands.test]
recipe = "test"

[commands.clippy]
recipe = "clippy"

[commands.build]
recipe = "build"
artifact_layout = "cargo"
profile = "release"
target = "aarch64-unknown-linux-gnu"
