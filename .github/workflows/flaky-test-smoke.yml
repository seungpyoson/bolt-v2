name: Flaky Test Smoke

on:
  schedule:
    - cron: '0 */12 * * 1-5'  # Every 12 hours, Monday-Friday

permissions:
  contents: read

env:
  JUST_VERSION: "1.49.0"
  CARGO_TERM_COLOR: always
  RUST_VERIFICATION_ROOT_BASE: ${{ github.workspace }}/.rust-verification
  # Scheduled smoke runs read the full-CI warm compile cache through the
  # PR-read-only role. They never write main's sccache prefix.
  SCCACHE_BUCKET: ${{ vars.CI_SCCACHE_BUCKET }}
  SCCACHE_REGION: ${{ vars.CI_SCCACHE_REGION }}
  SCCACHE_S3_KEY_PREFIX: ${{ vars.CI_SCCACHE_S3_KEY_PREFIX }}
  SCCACHE_S3_SERVER_SIDE_ENCRYPTION: "true"
  SCCACHE_IGNORE_SERVER_IO_ERROR: "1"

jobs:
  flaky-smoke-rust-root:
    name: Root Rust Tests - Flaky Smoke (Run ${{ matrix.run_number }})
    runs-on: ${{ vars.CI_RUNNER_MANAGED_HEAVY }}
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        run_number: [1]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup environment
        id: setup
        uses: ./.github/actions/setup-environment
        with:
          just-version: ${{ env.JUST_VERSION }}
          include-nextest-version: "true"
          include-managed-target-dir: "true"
          install-rust-linker: "true"
          build-jobs-key: flaky_test_smoke.flaky-smoke-rust-root

      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          cache-on-failure: true
          cache-bin: false
          cache-targets: false
          shared-key: cargo-registry-git-v1
          save-if: false

      - name: Restore test target cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ steps.setup.outputs.managed_target_dir }}
          key: managed-target-v1-${{ runner.os }}-${{ runner.arch }}-test-archive-test-${{ hashFiles('Cargo.lock', 'Cargo.toml', 'rust-toolchain.toml', '.cargo/config.toml', 'ci/rust-verification.toml', 'scripts/rust_verification.py', 'scripts/command_understanding.py', 'justfile', '.github/workflows/ci.yml', '.github/actions/setup-environment/action.yml', '.no-mistakes.yaml', '.config/nextest.toml', 'build.rs', 'gated_source_roots.manifest', 'src/**', 'tests/**') }}
          restore-keys: |
            managed-target-v1-${{ runner.os }}-${{ runner.arch }}-test-archive-test-

      - name: Install cargo-nextest
        uses: taiki-e/install-action@e49978b799e49ff429d162b7a30601a569ab6538 # v2.81.1
        with:
          tool: cargo-nextest@${{ steps.setup.outputs.nextest_version }}
          fallback: none

      - name: Resolve sccache eligibility
        id: sccache-eligible
        continue-on-error: true
        env:
          PR_READONLY_ROLE_ARN: ${{ vars.AWS_CI_CACHE_PR_READONLY_ROLE_ARN }}
          BUCKET: ${{ vars.CI_SCCACHE_BUCKET }}
          REGION: ${{ vars.CI_SCCACHE_REGION }}
          PREFIX: ${{ vars.CI_SCCACHE_S3_KEY_PREFIX }}
        run: |
          vars_present=true
          [[ -n "$PR_READONLY_ROLE_ARN" && -n "$BUCKET" && -n "$REGION" && -n "$PREFIX" ]] || vars_present=false
          location_matches=true
          [[ "$BUCKET" == "bolt-v2-ci-cache-675819144420-us-east-2" && "$REGION" == "us-east-2" && "$PREFIX" == "sccache/bolt-v2/arm64/root-nextest/" ]] || location_matches=false
          if [[ "$vars_present" == "true" && "$location_matches" == "true" ]]; then
            echo "eligible=true" >> "$GITHUB_OUTPUT"
          else
            echo "eligible=false" >> "$GITHUB_OUTPUT"
          fi
          echo "role_arn=$PR_READONLY_ROLE_ARN" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only vars_present=$vars_present location_matches=$location_matches"

      - name: Configure AWS credentials for sccache
        id: sccache-aws
        if: steps.sccache-eligible.outputs.eligible == 'true'
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6.2.0
        with:
          role-to-assume: ${{ steps.sccache-eligible.outputs.role_arn }}
          aws-region: ${{ vars.CI_SCCACHE_REGION }}

      - name: Install sccache
        id: sccache-install
        if: steps.sccache-eligible.outputs.eligible == 'true' && steps.sccache-aws.outcome == 'success'
        continue-on-error: true
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10
        with:
          version: "v0.10.0"

      - name: Resolve sccache enablement
        id: sccache
        continue-on-error: true
        run: |
          enabled=false
          if [[ "${{ steps.sccache-eligible.outputs.eligible }}" == "true" \
                && "${{ steps.sccache-aws.outcome }}" == "success" \
                && "${{ steps.sccache-install.outcome }}" == "success" \
                && -n "${SCCACHE_PATH:-}" ]]; then
            if "$SCCACHE_PATH" --start-server; then
              "$SCCACHE_PATH" --zero-stats || true
              enabled=true
            else
              echo "sccache server failed to start; building without cache"
            fi
          fi
          echo "enabled=$enabled" >> "$GITHUB_OUTPUT"
          echo "sccache enabled=$enabled"

      - name: Summarize sccache state
        if: always()
        shell: bash
        env:
          SCCACHE_ELIGIBLE: ${{ steps.sccache-eligible.outputs.eligible || 'false' }}
          SCCACHE_ENABLED: ${{ steps.sccache.outputs.enabled || 'false' }}
          SCCACHE_AWS_OUTCOME: ${{ steps.sccache-aws.outcome || 'skipped' }}
          SCCACHE_INSTALL_OUTCOME: ${{ steps.sccache-install.outcome || 'skipped' }}
        run: |
          echo "sccache read-only cache: eligible=${SCCACHE_ELIGIBLE} enabled=${SCCACHE_ENABLED} aws=${SCCACHE_AWS_OUTCOME} install=${SCCACHE_INSTALL_OUTCOME}" >> "$GITHUB_STEP_SUMMARY"
          if [[ "$SCCACHE_ENABLED" != "true" ]]; then
            echo "::notice::sccache read-only cache disabled; compiling without remote cache"
          fi

      - name: Configure nextest JUnit output
        run: |
          cp .config/nextest.toml "$RUNNER_TEMP/nextest-junit.toml"
          printf '%s\n' \
            '' \
            '[profile.default.junit]' \
            'path = "junit-unit-${{ matrix.run_number }}.xml"' \
            'store-success-output = false' \
            'store-failure-output = true' \
            >> "$RUNNER_TEMP/nextest-junit.toml"

      - name: Run tests
        env:
          BOLT_RUST_VERIFICATION_SCCACHE: ${{ steps.sccache.outputs.enabled == 'true' && '1' || '0' }}
        run: |
          log="$RUNNER_TEMP/flaky-smoke-root-${{ matrix.run_number }}.log"
          rc=0
          set +e
          bash .github/scripts/sccache-fail-open.sh "$log" just test --config-file "$RUNNER_TEMP/nextest-junit.toml" --no-fail-fast
          rc=$?
          set -e
          printf 'MERGIFY_TEST_EXIT_CODE=%s\n' "$rc" >> "$GITHUB_ENV"

      - name: Print sccache stats
        if: always() && steps.sccache.outputs.enabled == 'true'
        run: '"$SCCACHE_PATH" --show-stats || true'

      - name: Stage JUnit report
        run: cp "target/nextest/default/junit-unit-${{ matrix.run_number }}.xml" "junit-unit-${{ matrix.run_number }}.xml"

      - name: Upload test results to Mergify
        if: success() || failure()
        uses: mergifyio/gha-mergify-ci@d01f69e6275942be9a9066fd22cda1c49b0c85e3 # v14
        env:
          MERGIFY_TEST_JOB_NAME: nextest archive
        with:
          token: ${{ secrets.MERGIFY_TOKEN }}
          job_name: nextest archive
          report_path: "junit-*.xml"

  flaky-smoke-rust-backtester:
    name: Backtester Rust Tests - Flaky Smoke (Shard ${{ matrix.shard }}, Run ${{ matrix.run_number }})
    runs-on: ${{ vars.CI_RUNNER_MANAGED_HEAVY }}
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        run_number: [1]
        shard: [1]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup environment
        id: setup
        uses: ./.github/actions/setup-environment
        with:
          just-version: ${{ env.JUST_VERSION }}
          include-nextest-version: "true"
          install-rust-linker: "true"
          build-jobs-key: flaky_test_smoke.flaky-smoke-rust-backtester

      - name: Resolve crate managed target dir
        id: crate_target
        run: |
          dir="$(python3 "${{ steps.setup.outputs.rust_verification_owner }}" target-dir --repo crates/backtesting-vertical-slice)"
          echo "dir=$dir" >> "$GITHUB_OUTPUT"

      - name: Compute BVS cache input hash
        id: bvs_cache_inputs
        shell: bash
        run: |
          echo "digest=$(python3 scripts/ci_input_sets.py hash backtester_cache)" >> "$GITHUB_OUTPUT"

      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          cache-on-failure: true
          cache-bin: false
          cache-targets: false
          shared-key: cargo-registry-git-bvs-v1
          save-if: false

      - name: Restore test target cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ steps.crate_target.outputs.dir }}
          key: managed-target-bvs-v3-${{ runner.os }}-${{ runner.arch }}-test-${{ steps.bvs_cache_inputs.outputs.digest }}
          restore-keys: |
            managed-target-bvs-v3-${{ runner.os }}-${{ runner.arch }}-test-

      - name: Install cargo-nextest
        uses: taiki-e/install-action@e49978b799e49ff429d162b7a30601a569ab6538 # v2.81.1
        with:
          tool: cargo-nextest@${{ steps.setup.outputs.nextest_version }}
          fallback: none

      - name: Resolve sccache eligibility
        id: sccache-eligible
        continue-on-error: true
        env:
          PR_READONLY_ROLE_ARN: ${{ vars.AWS_CI_CACHE_PR_READONLY_ROLE_ARN }}
          BUCKET: ${{ vars.CI_SCCACHE_BUCKET }}
          REGION: ${{ vars.CI_SCCACHE_REGION }}
          PREFIX: ${{ vars.CI_SCCACHE_S3_KEY_PREFIX }}
        run: |
          vars_present=true
          [[ -n "$PR_READONLY_ROLE_ARN" && -n "$BUCKET" && -n "$REGION" && -n "$PREFIX" ]] || vars_present=false
          location_matches=true
          [[ "$BUCKET" == "bolt-v2-ci-cache-675819144420-us-east-2" && "$REGION" == "us-east-2" && "$PREFIX" == "sccache/bolt-v2/arm64/root-nextest/" ]] || location_matches=false
          if [[ "$vars_present" == "true" && "$location_matches" == "true" ]]; then
            echo "eligible=true" >> "$GITHUB_OUTPUT"
          else
            echo "eligible=false" >> "$GITHUB_OUTPUT"
          fi
          echo "role_arn=$PR_READONLY_ROLE_ARN" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only vars_present=$vars_present location_matches=$location_matches"

      - name: Configure AWS credentials for sccache
        id: sccache-aws
        if: steps.sccache-eligible.outputs.eligible == 'true'
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6.2.0
        with:
          role-to-assume: ${{ steps.sccache-eligible.outputs.role_arn }}
          aws-region: ${{ vars.CI_SCCACHE_REGION }}

      - name: Install sccache
        id: sccache-install
        if: steps.sccache-eligible.outputs.eligible == 'true' && steps.sccache-aws.outcome == 'success'
        continue-on-error: true
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10
        with:
          version: "v0.10.0"

      - name: Resolve sccache enablement
        id: sccache
        continue-on-error: true
        run: |
          enabled=false
          if [[ "${{ steps.sccache-eligible.outputs.eligible }}" == "true" \
                && "${{ steps.sccache-aws.outcome }}" == "success" \
                && "${{ steps.sccache-install.outcome }}" == "success" \
                && -n "${SCCACHE_PATH:-}" ]]; then
            if "$SCCACHE_PATH" --start-server; then
              "$SCCACHE_PATH" --zero-stats || true
              enabled=true
            else
              echo "sccache server failed to start; building without cache"
            fi
          fi
          echo "enabled=$enabled" >> "$GITHUB_OUTPUT"
          echo "sccache enabled=$enabled"

      - name: Summarize sccache state
        if: always()
        shell: bash
        env:
          SCCACHE_ELIGIBLE: ${{ steps.sccache-eligible.outputs.eligible || 'false' }}
          SCCACHE_ENABLED: ${{ steps.sccache.outputs.enabled || 'false' }}
          SCCACHE_AWS_OUTCOME: ${{ steps.sccache-aws.outcome || 'skipped' }}
          SCCACHE_INSTALL_OUTCOME: ${{ steps.sccache-install.outcome || 'skipped' }}
        run: |
          echo "sccache read-only cache: eligible=${SCCACHE_ELIGIBLE} enabled=${SCCACHE_ENABLED} aws=${SCCACHE_AWS_OUTCOME} install=${SCCACHE_INSTALL_OUTCOME}" >> "$GITHUB_STEP_SUMMARY"
          if [[ "$SCCACHE_ENABLED" != "true" ]]; then
            echo "::notice::sccache read-only cache disabled; compiling without remote cache"
          fi

      - name: Configure nextest JUnit output
        run: |
          printf '%s\n' \
            '[profile.default.junit]' \
            'path = "junit-unit-${{ matrix.run_number }}.xml"' \
            'store-success-output = false' \
            'store-failure-output = true' \
            > "$RUNNER_TEMP/nextest-junit.toml"

      - name: Run tests
        env:
          BOLT_RUST_VERIFICATION_SCCACHE: ${{ steps.sccache.outputs.enabled == 'true' && '1' || '0' }}
        run: |
          log="$RUNNER_TEMP/flaky-smoke-backtester-${{ matrix.shard }}-${{ matrix.run_number }}.log"
          rc=0
          set +e
          bash .github/scripts/sccache-fail-open.sh "$log" just bte-test --config-file "$RUNNER_TEMP/nextest-junit.toml" --partition "count:${{ matrix.shard }}/4" -- --skip issue_789_first_real_free_data_taker_pl
          rc=$?
          set -e
          printf 'MERGIFY_TEST_EXIT_CODE=%s\n' "$rc" >> "$GITHUB_ENV"

      - name: Print sccache stats
        if: always() && steps.sccache.outputs.enabled == 'true'
        run: '"$SCCACHE_PATH" --show-stats || true'

      - name: Stage JUnit report
        run: cp "crates/backtesting-vertical-slice/target/nextest/default/junit-unit-${{ matrix.run_number }}.xml" "junit-unit-${{ matrix.run_number }}.xml"

      - name: Upload test results to Mergify
        if: success() || failure()
        uses: mergifyio/gha-mergify-ci@d01f69e6275942be9a9066fd22cda1c49b0c85e3 # v14
        env:
          MERGIFY_TEST_JOB_NAME: bvs-test archive
        with:
          token: ${{ secrets.MERGIFY_TOKEN }}
          job_name: bvs-test archive
          report_path: "junit-*.xml"

  flaky-smoke-rust-backtester-issue-789:
    name: Backtester Issue 789 - Flaky Smoke (Run ${{ matrix.run_number }})
    runs-on: ${{ vars.CI_RUNNER_MANAGED_HEAVY }}
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        run_number: [1]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup environment
        id: setup
        uses: ./.github/actions/setup-environment
        with:
          just-version: ${{ env.JUST_VERSION }}
          include-nextest-version: "true"
          install-rust-linker: "true"
          build-jobs-key: flaky_test_smoke.flaky-smoke-rust-backtester-issue-789

      - name: Resolve crate managed target dir
        id: crate_target
        run: |
          dir="$(python3 "${{ steps.setup.outputs.rust_verification_owner }}" target-dir --repo crates/backtesting-vertical-slice)"
          echo "dir=$dir" >> "$GITHUB_OUTPUT"

      - name: Compute BVS cache input hash
        id: bvs_cache_inputs
        shell: bash
        run: |
          echo "digest=$(python3 scripts/ci_input_sets.py hash backtester_cache)" >> "$GITHUB_OUTPUT"

      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          cache-on-failure: true
          cache-bin: false
          cache-targets: false
          shared-key: cargo-registry-git-bvs-v1
          save-if: false

      - name: Restore test target cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ steps.crate_target.outputs.dir }}
          key: managed-target-bvs-v3-${{ runner.os }}-${{ runner.arch }}-test-${{ steps.bvs_cache_inputs.outputs.digest }}
          restore-keys: |
            managed-target-bvs-v3-${{ runner.os }}-${{ runner.arch }}-test-

      - name: Install cargo-nextest
        uses: taiki-e/install-action@e49978b799e49ff429d162b7a30601a569ab6538 # v2.81.1
        with:
          tool: cargo-nextest@${{ steps.setup.outputs.nextest_version }}
          fallback: none

      - name: Resolve sccache eligibility
        id: sccache-eligible
        continue-on-error: true
        env:
          PR_READONLY_ROLE_ARN: ${{ vars.AWS_CI_CACHE_PR_READONLY_ROLE_ARN }}
          BUCKET: ${{ vars.CI_SCCACHE_BUCKET }}
          REGION: ${{ vars.CI_SCCACHE_REGION }}
          PREFIX: ${{ vars.CI_SCCACHE_S3_KEY_PREFIX }}
        run: |
          vars_present=true
          [[ -n "$PR_READONLY_ROLE_ARN" && -n "$BUCKET" && -n "$REGION" && -n "$PREFIX" ]] || vars_present=false
          location_matches=true
          [[ "$BUCKET" == "bolt-v2-ci-cache-675819144420-us-east-2" && "$REGION" == "us-east-2" && "$PREFIX" == "sccache/bolt-v2/arm64/root-nextest/" ]] || location_matches=false
          if [[ "$vars_present" == "true" && "$location_matches" == "true" ]]; then
            echo "eligible=true" >> "$GITHUB_OUTPUT"
          else
            echo "eligible=false" >> "$GITHUB_OUTPUT"
          fi
          echo "role_arn=$PR_READONLY_ROLE_ARN" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only" >> "$GITHUB_OUTPUT"
          echo "cache_mode=read_only vars_present=$vars_present location_matches=$location_matches"

      - name: Configure AWS credentials for sccache
        id: sccache-aws
        if: steps.sccache-eligible.outputs.eligible == 'true'
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6.2.0
        with:
          role-to-assume: ${{ steps.sccache-eligible.outputs.role_arn }}
          aws-region: ${{ vars.CI_SCCACHE_REGION }}

      - name: Install sccache
        id: sccache-install
        if: steps.sccache-eligible.outputs.eligible == 'true' && steps.sccache-aws.outcome == 'success'
        continue-on-error: true
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10
        with:
          version: "v0.10.0"

      - name: Resolve sccache enablement
        id: sccache
        continue-on-error: true
        run: |
          enabled=false
          if [[ "${{ steps.sccache-eligible.outputs.eligible }}" == "true" \
                && "${{ steps.sccache-aws.outcome }}" == "success" \
                && "${{ steps.sccache-install.outcome }}" == "success" \
                && -n "${SCCACHE_PATH:-}" ]]; then
            if "$SCCACHE_PATH" --start-server; then
              "$SCCACHE_PATH" --zero-stats || true
              enabled=true
            else
              echo "sccache server failed to start; building without cache"
            fi
          fi
          echo "enabled=$enabled" >> "$GITHUB_OUTPUT"
          echo "sccache enabled=$enabled"

      - name: Summarize sccache state
        if: always()
        shell: bash
        env:
          SCCACHE_ELIGIBLE: ${{ steps.sccache-eligible.outputs.eligible || 'false' }}
          SCCACHE_ENABLED: ${{ steps.sccache.outputs.enabled || 'false' }}
          SCCACHE_AWS_OUTCOME: ${{ steps.sccache-aws.outcome || 'skipped' }}
          SCCACHE_INSTALL_OUTCOME: ${{ steps.sccache-install.outcome || 'skipped' }}
        run: |
          echo "sccache read-only cache: eligible=${SCCACHE_ELIGIBLE} enabled=${SCCACHE_ENABLED} aws=${SCCACHE_AWS_OUTCOME} install=${SCCACHE_INSTALL_OUTCOME}" >> "$GITHUB_STEP_SUMMARY"
          if [[ "$SCCACHE_ENABLED" != "true" ]]; then
            echo "::notice::sccache read-only cache disabled; compiling without remote cache"
          fi

      - name: Configure nextest JUnit output
        run: |
          printf '%s\n' \
            '[profile.default.junit]' \
            'path = "junit-unit-${{ matrix.run_number }}.xml"' \
            'store-success-output = false' \
            'store-failure-output = true' \
            > "$RUNNER_TEMP/nextest-junit.toml"

      - name: Run tests
        env:
          BOLT_RUST_VERIFICATION_SCCACHE: ${{ steps.sccache.outputs.enabled == 'true' && '1' || '0' }}
        run: |
          log="$RUNNER_TEMP/flaky-smoke-backtester-issue-789-${{ matrix.run_number }}.log"
          rc=0
          set +e
          bash .github/scripts/sccache-fail-open.sh "$log" just bte-test --config-file "$RUNNER_TEMP/nextest-junit.toml" issue_789_first_real_free_data_taker_pl
          rc=$?
          set -e
          printf 'MERGIFY_TEST_EXIT_CODE=%s\n' "$rc" >> "$GITHUB_ENV"

      - name: Print sccache stats
        if: always() && steps.sccache.outputs.enabled == 'true'
        run: '"$SCCACHE_PATH" --show-stats || true'

      - name: Stage JUnit report
        run: cp "crates/backtesting-vertical-slice/target/nextest/default/junit-unit-${{ matrix.run_number }}.xml" "junit-unit-${{ matrix.run_number }}.xml"

      - name: Upload test results to Mergify
        if: success() || failure()
        uses: mergifyio/gha-mergify-ci@d01f69e6275942be9a9066fd22cda1c49b0c85e3 # v14
        env:
          MERGIFY_TEST_JOB_NAME: bvs-test issue-789
        with:
          token: ${{ secrets.MERGIFY_TOKEN }}
          job_name: bvs-test issue-789
          report_path: "junit-*.xml"
