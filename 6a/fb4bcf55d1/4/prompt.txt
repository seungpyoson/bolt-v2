### PR #1351 — encode the terminal transition structurally

  Rework PR #1351 around one typed terminal-settlement transition. Green CI and approval do not override the missing architecture or requirement.

  Worktree:
  cd /Users/spson/Projects/Claude/bolt-v2/.worktrees/fix-1349-settlement-currency-park

  Expected state:
  - Clean worktree
  - Local and GitHub head: 189f6d3779c3c9bdcbe6f7c5db67649d06aa8c94

  Convert the PR back to draft before pushing changes.

  Preserve D1/D2 only after confirming:
  - settlement currency derives from the non-enforcing capital pool
  - missing currency fails at load through the same production predicate
  - no submit admission is accidentally armed

  Rework terminal handling:
  - Introduce one typed terminal-eligibility result representing only:
    1. market expired, or
    2. recovery-only unknown interval that is unmanageable by construction.
  - A live-manageable, pending, or nonterminal position must be unable to construct that eligibility.
  - One canonical transition must atomically:
    - persist durable terminal evidence
    - emit the lifecycle transition
    - publish the real operator-health transition required by #1349
    - release exposure to Flat
  - Do not rely on callers to establish eligibility before an unconditional `Flat`.
  - Do not use the process-local dedupe set as health or safety state. Any log dedupe must be merely a reporter detail derived from the canonical transition.
  - Remove every discarded invariant result such as `let _ = enforce_one_position_invariant()`. Propagate or explicitly fail closed.
  - Do not add timers, fallback release paths, acknowledgement shortcuts, or another parallel latch.

  Evidence:
  - both valid terminal eligibility legs transition successfully
  - live-manageable/nonterminal positions cannot be released
  - durable evidence and health transition are emitted exactly once per transition
  - restart behavior is explicit and tested
  - invariant failures cannot be swallowed
  - log suppression follows actual state transitions, not nested special cases
  - config differential without/with the settlement pool remains green

  Use permitted local non-compile checks and remote-first Rust verification. Commit and publish with `just sandbox-safe-push`; keep the PR draft.

  Report the exact remote head, deleted fallback/convention paths, and evidence. Do not deploy, trade, request external review, queue, or merge.

---

conduct an internal adverserial review

---

address them with systematic solutions

---

Continue PR #1351 at 0cf630f9. Keep it draft.

  Two blockers remain:

  1. Live terminal handling persists lifecycle nested inside settlement_booking_error, while restart persists standalone order_lifecycle. Unify both paths
  under one canonical durable terminal record and one production consumer schema. Remove the test writer’s synthetic conversion that masks this difference.

  2. Operator-health emission can panic on a poisoned lock before exposure becomes Flat. Durable evidence must commit first; release must then complete
  regardless of health-reporting failure. Health failure must remain explicit and contextual, never silently ignored. Add a differential proving health-emitter
  failure cannot park exposure or duplicate evidence.

  Preserve backward-compatible reading and transition/source error context. Run permitted checks, push the draft, and report exact head. No review, queue,
  deployment, or trade.

  Next:

  - Run ready-state full CI for #1327, #1357, and #1358, respecting the two-concurrent-run ceiling.
  - After each exact head is green: external adversarial review and fresh required-owner approval.
  - Then submit them through one merge-queue wave if preflight returns queue_as_one_wave.

---

lets not use relay - so are you done with your implementation?

---

Own this PR through draft review closure.

  Use the external review output already available in this session. For every substantive finding:

  - Verify it against the exact code.
  - Fix the root cause within the PR’s declared scope, or rebut it with concrete evidence.
  - Do not add fallback chains, denylist conditions, duplicate paths, new analyzers, or parallel sources of truth.
  - Commit and publish fixes with `just sandbox-safe-push`.
  - Re-run external review at the new exact head.
  - Continue until the reviewer returns no substantive findings.

  If no review exists yet, obtain one adversarial external review now.

  Do not send raw transcripts to the global orchestrator. Return only:

  PR / exact remote head:
  Reviewer source and model:
  Findings by severity:
  Resolved findings:
  Final re-review result:
  Net diff and debt delta:
  Verification completed:
  Remaining blockers:
  Worktree clean:
  Requested next action:

  Keep the PR draft. Do not run full CI, mark ready, request `sp-reviewer`, queue, merge, deploy, or trade. The global orchestrator will coordinate those steps
  after review closure.

---

Grok: Adversarial review — PR #1351

Head verified: GitHub head = b861417339e1a8a1609165e068b65130fa62030a (matches requested; review proceeded).
Base: main @ 7095c6f5f250b99a453300fafd0c9046c3da4a5b
Scope: 20 files; issue #1349 / #1179 terminal-settlement + settlement-currency + log class.
PR body ## Head: still names 0cf630f9… — stale (as you noted); not trusted.

Reviewer source: Grok 4.5 (xAI) — interactive adversarial review-only agent (this session).
Exact configured model (this review): Grok 4.5.
Repo AI-review config (not used for this pass): ci/ai-review.toml → glm-5.2, kimi-k2.7-code, claude-opus-4-8.

No files edited; no comments posted; no CI/deploy/trade.

───

Blocking findings

B1 — Exact-once lifecycle assertion inspects an event production no longer emits
• File:line: src/strategies/binary_oracle_edge_taker/tests/adverse_path_harness.rs:1864-1890 (distinct_terminal_booking_error_keys_each_record_lifecycle_and_release_exposure)
• Failure scenario: Production apply_terminal_settlement_transition only calls record_terminal_settlement (mod.rs:1378-1380). It does not call record_order_lifecycle. The test builds terminal_position_ids only from RecordedDecisionEvidenceEvent::OrderLifecycle with SettlementBookingTerminal. That vector is always empty under the new writer, so assert_eq!(terminal_position_ids, vec![first…, second…]) fails as soon as exact-head Rust tests run.
• Required correction: Collect position IDs from TerminalSettlement.lifecycle (same rule as terminal_settlement_lifecycle_count at lines 2740-2756). Drop any assumption that standalone OrderLifecycle rows still appear for terminal settlement.

B2 — Restart re-appends terminal evidence for the same settlement key (violates “idempotent / no duplicate evidence”)
• File:line:
  • src/strategies/binary_oracle_edge_taker/mod.rs:2850-2870 (restart always applies transition when key present + open residual)
  • src/strategies/binary_oracle_edge_taker/mod.rs:1346-1413 (apply_terminal_settlement_transition always appends)
  • src/bolt_v3_decision_evidence.rs:4305-4309 (comment admits restart may re-confirm)
  • Reinforced by tests/bolt_v3_decision_evidence.rs:1748-1757 (explicitly expects 2 JSONL lines for live+restart)
• Failure scenario: Live terminal writes one terminal_settlement line and releases. Venue residual remains open. Every subsequent process restart: reload keys → still open in cache → apply_terminal_settlement_transition(..., booking_error: None) → another durable terminal_settlement line + another health transition (terminal_transition_count++). Booking-error is not re-written, but lifecycle/health evidence is duplicated unbounded across restarts.
• Required correction: Separate “release residual exposure” from “emit new durable terminal evidence.” If durable keys already contain the settlement key (from booking-error or prior terminal), restart should set Flat (and optionally surface health once per process) without another append. Add a second-restart test that asserts read_terminal_settlement_evidence length stays constant.

───

Non-blocking findings

N1 — Doc order of health vs release is wrong (code order is safer)
• File:line: src/strategies/binary_oracle_edge_taker/mod.rs:1342-1345 vs 1380-1412
• Failure scenario: Comment claims durable evidence → operator health → release. Actual order is durable → keys → Flat release → health emit (errors swallowed). Code satisfies “health cannot block release”; the comment misleads future edits into reordering health before Flat.
• Required correction: Align the comment with the real order (durable → release → health mutate/report).

N2 — Live non-recovery missing-interval_end has no durable latch and can error every tick
• File:line: mod.rs:1284-1313 (eligibility), 1416-1436 + 986-990 / 1106-1110 (missing-interval path uses ?)
• Failure scenario: A live-origin managed position with interval_end_ms() == None is ineligible (RecoveryUnknownInterval requires RecoveryBootstrap). record_settlement_booking_error fails before inserting settlement_booking_error_keys, so every outage/book tick re-fails with “ineligible,” leaves exposure Managed, and writes no terminal record. Rare if live lifecycle always carries an end; severe if instrument recovery ever drops the end on a live-origin position.
• Required correction: For ineligible missing-interval: either no-op with a one-shot loud log + durable non-terminal fail-closed record (no Flat), or route only recovery-origin through this helper and skip the live path entirely without Err spam.

N3 — Restart unit test does not exercise production single-writer JSONL path
• File:line: adverse_path_harness.rs:2110-2216 (restart_reconstructs_expired_terminal_transition_from_durable_booking_error)
• Failure scenario: Durable prior booking-error is written to a temp JSONL path; restart reconstruction writes terminal evidence only to the in-memory RecordingSequencedDecisionEvidenceWriter. Production uses one JSONL writer for both. The test correctly proves “no parallel booking-error,” but cannot catch dual-writer / same-file append bugs under real restart.
• Required correction: Drive restart through JsonlBoltV3DecisionEvidenceWriter on the recovery path (or assert read_terminal_settlement_evidence on the same file after bootstrap).

N4 — Stale PR description head (metadata debt, not code)
• File:line: PR body ## Head (still 0cf630f9…)
• Failure scenario: Reviewers/operators pin the wrong SHA; false confidence in probe CI at older heads.
• Required correction: Update ## Head to b861417339e1a8a1609165e068b65130fa62030a when next description edit is allowed.

───

Invariant checklist (verified against code, not PR prose)

┌───────────────────────────────────────────────────────┬───────────────────────────────────────────────────────┐
│ Invariant                                             │ Result                                                │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Terminal eligibility only MarketExpired /             │ Holds for live constructor (mod.rs:1284-1313);        │
│ RecoveryUnknownInterval                               │ private enum; apply path takes only that type         │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Pending / live-manageable / nonterminal cannot reach  │ Holds (live_manageable_… test + bail); pending is not │
│ Flat via terminal path                                │ a settlement candidate                                │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Eligibility structural, not convention                │ Holds (typed private enum + single apply)             │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Live + restart same canonical record kind/schema      │ Holds (terminal_settlement); restart omits nested     │
│                                                       │ booking-error by design                               │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Booking-error + lifecycle one append                  │ Holds live (record_terminal_settlement one line)      │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Test writers do not synthesize a second production-   │ Holds (record_terminal_settlement only); helpers      │
│ invisible event                                       │ accept legacy shapes                                  │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Old JSONL readable                                    │ Holds (legacy settlement_booking_error + nested       │
│                                                       │ terminal_lifecycle)                                   │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Restart idempotent / no duplicate evidence            │ Fails — B2                                            │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Health cannot panic/return early before release       │ Holds (Flat before health; health errors swallowed)   │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Poisoned lock / reporter failure cannot re-park       │ Holds (health_emitter_failure_… test pattern)         │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Safe order durable → release → health                 │ Holds in code; comment wrong — N1                     │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Failure after committed release cannot retry-         │ Holds in-process (keys early-return)                  │
│ duplicate                                             │                                                       │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Same settlement-currency predicate validation +       │ Holds                                                 │
│ registration                                          │                                                       │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Non-enforcing pool cannot arm submit admission        │ Holds (enforce_submit_admission = false; arming scans │
│                                                       │ enforcing pools only)                                 │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Missing settlement currency fails closed              │ Holds at load validation + runtime terminal when      │
│                                                       │ expired                                               │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ No fallback release / timer / ack shortcut / parallel │ Holds for this slice (removed unconditional Flat      │
│ latch / dual safety authority                         │ helper; durable keys only)                            │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Log dedupe reporting-only                             │ Holds for new block-reason set WARNs; entry-skip      │
│                                                       │ evidence dedupe was pre-existing                      │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ 20-file scope justified                               │ Holds for #1349/#1179 slice                           │
├───────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┤
│ Runtime-literal registrations consistent              │ Holds for new kinds (terminal_settlement, sources)    │
└───────────────────────────────────────────────────────┴───────────────────────────────────────────────────────┘

───

Verdict

Not code-complete for the declared #1349/#1179 slice under the stated invariants: B1 will fail exact-head tests; B2 violates restart non-duplication.

Exact-head full CI remains pending (draft/skipped gates observed; Rust full gate not treated as green here).

Minimum bar to clear: fix B1 (test), fix or explicitly re-specify B2 (restart must not re-append terminal evidence when the durable key already exists), then re-review at the new head.
GLM: I've completed a thorough adversarial review. Here is my assessment.
Reviewer source: opencode (interactive CLI tool)
Exact configured model: zai-coding-plan/glm-5.2
Head verification: GitHub PR #1351 headRefOid = b861417339e1a8a1609165e068b65130fa62030a — confirmed match. PR description ## Head is stale (0cf630f9...), as noted.
Findings
Blocking
None.
Non-blocking
N1 — recovered_terminal_settlement_eligibility omits RecoveryBootstrap origin check
src/strategies/binary_oracle_edge_taker/mod.rs:1352-1370
The live-path terminal_settlement_eligibility (line 1297) guards the RecoveryUnknownInterval variant with self.managed_position().is_some_and(|managed| managed.origin == ManagedPositionOrigin::RecoveryBootstrap && managed.position.position_id == position.position_id). The restart-path recovered_terminal_settlement_eligibility is a private static method with no &self access; it constructs RecoveryUnknownInterval for any position where interval_end_ms().is_none(), without verifying the position is recovery-bootstrapped.
Failure scenario: A future code change calls recovered_terminal_settlement_eligibility from a non-recovery context (e.g., a live tick path that refactors to use the static method). A live-entered position with missing instrument interval metadata would construct RecoveryUnknownInterval and release exposure to Flat, violating "Only market-expired and recovery-only unknown-interval positions can construct terminal eligibility."
Required correction: Pass ManagedPositionOrigin (or the managed-position reference) into recovered_terminal_settlement_eligibility and verify RecoveryBootstrap, or convert it to a &self method mirroring the live path's check. The private-method/single-call-site enforcement is sufficient today but is weaker than the live path's explicit guard.
N2 — BoltV3LiveNodeRuntime::operator_health_surface panics on poisoned lock
src/bolt_v3_live_node.rs:1427-1430
The on-demand operator_health_surface method uses .expect("settlement health lock poisoned") on self.settlement_health, while the production emission path (build_settlement_health_transition_emitter, line 836-842) and the emit_operator_health_surface closure (line 3131-3133) both use .map_err(|_| anyhow::anyhow!("settlement health lock poisoned")). The emit_operator_health_surface_transition method (line 1434-1436) calls operator_health_surface, so a poisoned lock would panic during a routine health surface emission, crashing the strategy.
Failure scenario: Any panic while holding the settlement_health mutex (e.g., a future code change to apply_transition or clone() that introduces a panic path) poisons the lock. The next call to emit_operator_health_surface_transition (called at startup, line 2652, and on other health transitions) panics via .expect(), crashing the node. The emission path correctly degrades to an explicit error; this path does not.
Required correction: Replace .expect() with .map_err(...)? and return a degraded or not_configured() settlement health on poison, consistent with the emission path. The risk is low today (only apply_transition and clone hold the lock, neither panics), but the inconsistency is a latent crash vector.
N3 — Restart appends a new terminal_settlement record on each restart for the same key
src/strategies/binary_oracle_edge_taker/mod.rs:2844-2857 (restart caller) → apply_terminal_settlement_transition (line 1342) → record_terminal_settlement (line 1376)
The live-path caller record_settlement_booking_error (line 1268) has a dedup guard: if self.settlement_booking_error_keys.contains(&settlement_key) { return Ok(()); }. The restart path in bootstrap_recovery_from_cache checks the key to decide whether to enter the terminal transition, but then calls apply_terminal_settlement_transition directly, which unconditionally calls record_terminal_settlement. Each restart appends a new terminal_settlement JSONL record for the same settlement key. The recovery reader read_settlement_booking_error_keys_for_recovery_scope deduplicates via BTreeSet, so functional correctness is preserved, but the durable file grows by one record per restart.
Failure scenario: A position with a prior booking-error key survives multiple node restarts (e.g., the venue still reports it as open). Each restart appends a new terminal_settlement record. Over many restarts, the JSONL file accumulates duplicate records for the same key, increasing recovery read latency. The test restart_reconstructs_expired_terminal_transition_from_durable_booking_error only verifies a single restart; it does not assert that a second restart produces no additional durable record.
Required correction: Either (a) add a guard at the top of apply_terminal_settlement_transition to skip the durable write when the key is already in settlement_booking_error_keys and the call is from the restart path (passing a flag or splitting the method), or (b) explicitly document in the invariant that terminal_settlement records are transition audit entries that may be replayed across restarts, and update the test to assert the multi-restart durable count.
Invariant assessment summary
Invariant	Status
Terminal eligibility structurally enforced	✓ Private enum, private constructors, lifecycle checks
Pending/live-manageable/nonterminal cannot reach Flat	✓ Test live_manageable_nonterminal_position_cannot_enter_terminal_settlement_transition
Live and restart persist same canonical schema	✓ Both use record_terminal_settlement → terminal_settlement kind
Booking-error + lifecycle in one append	✓ BoltV3TerminalSettlementEvidence is one record; test terminal_settlement_uses_one_canonical_durable_event
Test writers don't synthesize second event	✓ RecordingSequencedDecisionEvidenceWriter records one TerminalSettlement event
Old JSONL records remain readable	✓ #[serde(default)] on terminal_lifecycle; test legacy_nested_terminal_booking_error_remains_readable
Restart idempotent / no duplicate evidence	⚠ N3 — key set deduplicates, but durable file grows
Health emission cannot panic before release	✓ Emission is after exposure = Flat; emitter returns Result, errors logged
Poisoned lock cannot recreate parked exposure	✓ Emission path uses map_err; exposure already Flat (⚠ N2 for on-demand path)
Durable → release → health mutation → reporting order	✓ record_terminal_settlement → exposure = Flat → apply_transition → emit_surface
Failure after committed release cannot duplicate	✓ Key in settlement_booking_error_keys, early return on retry
Same settlement-currency predicate at validation and registration	✓ Both call settlement_currency_for_execution_account
Non-enforcing pool cannot arm admission	✓ enforce_submit_admission = false; test asserts
Missing settlement currency fails closed	✓ validate_settlement_currency_derivable; test edge_taker_validation_fails_without_capital_pool_settlement_currency
No fallback/timer/latch/dual path/swallowed error	✓ Removed apply_settlement_booking_terminal_release, settlement_booking_terminal_evidence_keys, let _ = enforce_one_position_invariant
Log dedup is reporting-only	✓ last_entry_block_reason_sets controls WARN emission only
20-file diff justified by #1349/#1179	✓ All files map to declared scopes (config, validation, terminal booking, log volume)
Runtime-literal registrations consistent	✓ New literals registered in audit TOML; stringify! outputs follow existing unregistered pattern
The three non-blocking findings do not violate any MUST rule or safety invariant at current head. They represent structural inconsistencies and a test gap that should be addressed before or during merge but do not block the declared #1349/#1179 slice.
Kimi: Head verified: PR #1351 headRefOid = b861417339e1a8a1609165e068b65130fa62030a (matches the requested exact head; the PR body's
   stale ## Head = 0cf630f9 was disregarded). Reviewed the actual tree at b861417 against main (7095c6f, = origin/main), AGENTS.md,
   issue #1349, and the #1179 D1–D4 slice. No files edited, no comments posted, no CI dispatched.

   Blocking findings

   B1 — Restart appends a duplicate canonical terminal_settlement record on every boot (violates "restart is idempotent and cannot
   duplicate evidence"; also contradicts the schema's own idempotency claim)

   • src/bolt_v3_decision_evidence.rs:4289-4311 — read_settlement_booking_error_keys_for_recovery_scope now seeds the recovered set
     from both legacy settlement_booking_error records and new terminal_settlement records (:4305 for evidence in
     read_terminal_settlement_evidence(path, max_bytes)?). The comment at :4308 ("Canonical terminal records are state transitions,
     so replay is idempotent") describes only the in-memory BTreeSet::insert, not the durable append.
   • src/strategies/binary_oracle_edge_taker/mod.rs:2847-2868 — recovery branch triggers on
     self.settlement_booking_error_keys.contains(&settlement_key) (:2851) and calls
     apply_terminal_settlement_transition(eligibility, None, "prior_booking_error_key_on_restart …") (:2863-2867).
   • src/strategies/binary_oracle_edge_taker/mod.rs:1346-1395 — apply_terminal_settlement_transition unconditionally calls
     .record_terminal_settlement(&terminal_evidence) (:1380); it has no "canonical record already exists for this key" guard, and
     the recovery caller bypasses the only in-process dedupe (the :1268 top-guard in record_settlement_booking_error).

   Concrete failure scenario: a live (or first-recovery) terminal booking error writes terminal_settlement(K). Booking failed, so
   nothing closes the venue position; it stays open across restart. On the next boot the reader re-seeds K from that very terminal
   record into settlement_booking_error_keys, the cache re-recovers the still-open position, the :2851 branch fires, and :1380
   appends a second terminal_settlement(K, booking_error=None). Each subsequent restart appends one more — unbounded duplicate
   canonical evidence for one settlement key. Any JSONL consumer that counts terminal transitions or replays the stream sees
   repeated terminal events for a single position.

   It is also untested: every recovery test (position_market_lifecycle_recovered_*, adverse_path_harness.rs:1089-1340;
   startup_settlement_recovery_replays_evidence_from_real_cache_positions, :2220) calls bootstrap_recovery_from_cache() exactly
   once and asserts terminal_settlement_lifecycle_count == 1; none performs a second bootstrap against the same durable file, so
   the count going to 2 on restart #2 is unasserted.

   Required correction: make the durable boundary idempotent. Do not drive a recovery canonical append from a key that already
   carries a terminal_settlement record — split the seeded set into legacy-only keys (trigger the one-time upgrade) versus
   already-terminal keys (suppress re-append; adopt Flat in memory and skip the write). Gate the :1380 append on "no existing
   canonical record for K", and add a two-bootstrap_recovery_from_cache() test over one evidence file asserting both
   terminal_settlement_lifecycle_count and settlement_booking_error_count remain 1 after the second boot.

   Non-blocking findings

   N1 (low) — New health-surface read path panics on a poisoned lock, inconsistent with the emitter's explicit-error design

   • src/bolt_v3_live_node.rs:1429 — the runtime health snapshot does self.settlement_health.lock().expect("settlement health lock
     poisoned").clone().
   • Contrast the emission paths added in the same PR, which map poison to an explicit error:
     build_settlement_health_transition_emitter (live_node.rs:836) and the emit_operator_health_surface closure (live_node.rs:3136)
     both use .lock().map_err(|_| anyhow!("settlement health lock poisoned"))?.

   Concrete scenario: if any thread ever panics while holding settlement_health (currently unreachable — the guard is only used for
   infallible apply_transition/.clone()), the mutex poisons and every subsequent health poll/snapshot panics the node via :1429,
   whereas the emission path would surface an explicit error and continue. This cannot recreate a parked-exposure condition
   (release precedes emission in the strategy), so it is not blocking, but it is a new crash surface introduced here and it
   contradicts the PR's own "poison remains explicit" intent.

   Required correction: mirror the emitter — replace the .expect at :1429 with map_err (or degrade to a last-known/NotConfigured
   surface) so a poisoned lock never panics the read path.

   Invariants verified as satisfied (no finding)

   • Terminal eligibility. TerminalSettlementEligibility (mod.rs:297-344) has exactly the two required variants with private
     fields/constructors; terminal_settlement_eligibility (:1284-1316) builds MarketExpired only on interval_ended_at and
     RecoveryUnknownInterval only for RecoveryBootstrap + missing interval, otherwise anyhow::bail!.
     record_settlement_booking_error (:1264-1283) and record_missing_interval_end_settlement_booking_error (:1416-1438) both route
     through the gate, so pending/live-manageable/nonterminal positions fail loud and never reach Flat.
     apply_terminal_settlement_transition is the sole consumer; no unconditional Flat helper exists.
   • Canonical persistence. Live and recovery both emit one terminal_settlement line carrying embedded booking_error + lifecycle
     via a single append_line (decision_evidence.rs:2003-2011, 3932-3983); legacy settlement_booking_error JSONL stays readable
     (terminal_lifecycle #[serde(default)], :1232-1236); the recording writer emits exactly one in-memory event per call
     (shared_fixture.rs:796-806), and the count helpers read the embedded fields rather than a synthesized second event
     (adverse_path_harness.rs:2724-2758). (B1 is the sole exception.)
   • Health & release ordering. Order is durable commit → key insert → exposure = Flat → health mutation → surface emit
     (mod.rs:1380→:1383-1388→health). Key is inserted before release, so a post-release emitter failure logs and returns without
     retry duplication; poison returns an explicit Err and cannot un-release.
   • Config & admission. settlement_currency_for_execution_account (archetype/…:524-537) is the single predicate used by both
     validation (:307-343) and registration (:475-477); the new POLYMARKET pool has enforce_submit_admission = false
     (config/root.toml:155-174) and admission arming still keys on that flag in unchanged code, so the non-enforcing pool supplies
     currency without arming submit; missing currency fails closed (validation error +
     edge_taker_validation_fails_without_capital_pool_settlement_currency, :595).
   • Debt & scope. No timer/gate/fallback-release/parallel latch; entry-block/entry-skip WARN dedupe is reporting-only and
     evidence-independent (mod.rs:3929-4156, 4157-4226); NT order-book spam filtered at Error (live_node_config.rs:29-36); new
     literals registered (runtime-literal-audit.toml:19415, :20216, :20927); the 20 files map cleanly to #1349 D1–D4 / #1179 with
     no adjacent work.

   Reviewer source: Kimi Code CLI · exact configured model: kimi-k2.7-code (per ci/ai-review.toml [kimi]). PR state at review:
   OPEN/draft, REVIEW_REQUIRED; exact-head CI (bvs-ci-policy, coverage-enforcer, actionlint) still IN_PROGRESS — full CI remains
   pending.

---

generate a review request prompt

---

Kimi:
 ● Head verified: PR #1351 headRefOid = 85c9093d7c2f4d0a308013d9a69509f6a78e80f5 (matches the requested exact head); main
   7095c6f5f250b99a453300fafd0c9046c3da4a5b is an ancestor of head. PR body ## Head now names
   85c9093d7c2f4d0a308013d9a69509f6a78e80f5. No files edited, no comments, no CI, no state change.

   Reviewer source and exact model: Kimi Code CLI · kimi-k2.7-code (per ci/ai-review.toml [kimi]).

   Blocking findings: No substantive findings.

   Non-blocking findings: No substantive findings.

   Resolved prior findings:
   • B1 (restart re-append of canonical evidence) — resolved. Recovery now seeds a dedicated terminal_settlement_keys set via
     read_terminal_settlement_keys_for_recovery_scope (decision_evidence.rs:4290-4305, consumed at mod.rs:2952-2997) BEFORE the
     recovery branch (mod.rs:2850 recover_settlement_bootstrap_from_scope(...), branch at :2866-2901). The branch picks
     TerminalEvidenceState::CanonicalAlreadyDurable when the key is already canonical and PersistCanonical only for legacy-only
     keys (mod.rs:2887-2892); apply_terminal_settlement_transition appends solely on PersistCanonical (mod.rs:1393-1398).
     Legacy-only → exactly one upgrade; canonical → no re-append.
   • N1 (poisoned-lock panic on read path) — resolved. operator_health_surface now returns Result and uses .map_err instead of
     .expect (live_node.rs:1417-1433); emit_operator_health_surface_transition matches and logs (:1435-1447). No .expect on
     settlement_health remains; failures are explicit/contextual ("settlement health lock poisoned", "operator health surface
     snapshot failed").

   Invariant assessment:
   • Terminal lifecycle assertions inspect the canonical record: distinct_terminal_booking_error_keys… reads
     evidence.lifecycle.position_id from TerminalSettlement events (adverse_path_harness.rs:1867-1871);
     terminal_settlement_uses_one_canonical_durable_event proves the terminal path does not use the standalone lifecycle writer
     (:1914-1961, via with_failing_standalone_order_lifecycle). (The terminal_settlement_lifecycle_count helper retains a
     standalone-OrderLifecycle arm at :2767-2786, but production never emits that form, so it is unreachable for terminal events.)
   • Legacy vs canonical restart distinction: separate readers/sets (read_terminal_settlement_keys_for_recovery_scope vs
     read_settlement_booking_error_keys_for_recovery_scope); the union in the latter (decision_evidence.rs:4330-4338) is harmless
     because the append gate is the dedicated terminal set, not the booking-error set.
   • One canonical upgrade / no re-append across restarts: proven by
     restart_reconstructs_expired_terminal_transition_from_durable_booking_error (adverse_path_harness.rs:2110-2245) using the
     production writer JsonlBoltV3DecisionEvidenceWriter::from_test_path (decision_evidence.rs:1924-1935); first bootstrap →
     read_terminal_settlement_evidence().len()==1 and legacy still 1 (no parallel record); second bootstrap → canonical count stays
     1.
   • RecoveryBootstrap is explicit for unknown-interval: recovered_terminal_settlement_eligibility(..., origin:
     ManagedPositionOrigin, ...) requires origin == RecoveryBootstrap && interval_end_ms().is_none() (mod.rs:1349-1383), caller
     passes RecoveryBootstrap (:2870).
   • Live/pending retain interval end: live_entered_and_pending_adopted_positions_retain_interval_end_boundary pins non-None
     interval end through pending → fill (StrategyEntry) and pending → on_position_opened (exposure.rs:3946-4018).
   • Commit → release → fallible health: documented (mod.rs:1340-1345) and actual (:1393-1398 append, :1405-1408
     exposure=Flat+sync+refresh, :1414-1435 fallible emitter logged on error).
   • One canonical schema, legacy readable: live_and_restart_terminal_settlement_share_one_canonical_schema and
     legacy_nested_terminal_booking_error_remains_readable (tests/bolt_v3_decision_evidence.rs:1733-1796).
   • Non-enforcing pool derives currency, no arming: enforce_submit_admission = false (config/root.toml:155-174), predicate ignores
     the flag (archetype/…:524-537), admission still arms only on enforce_submit_admission (unchanged).
   • No fallback chain / duplicate release / timer / ack shortcut / parallel latch / analyzer / second source of truth introduced:
     single binary TerminalEvidenceState gate (mod.rs:306-310), one append site, synchronous recovery, one health emitter.

   Verification gaps:
   • Exact-head Rust CI at 85c9093 is not green: the PR body states the current head has local non-compile proof only and "awaits
     the owner/reviewer exact-head Rust gate," with both Rust Probe slots consumed at a prior head
     (4aacef991a2ffe8b4c9d8486f7d6d63c465b6af2). I did not run cargo/nextest (review-only; local Rust compile is policy-gated).
     Compile/test success is asserted by the diff but unverified here.
   • The restart differential asserts durable-count stability and Flat across the second bootstrap but not health-transition-count
     stability (adverse_path_harness.rs:2232-2244); on a redundant same-object pass the CanonicalAlreadyDurable branch still
     re-announces health (mod.rs:1414-1435). Not production-reachable as duplication (recovery runs once per process with health
     rebuilt at zero), and not durable — noted for completeness, not a finding.
   • tests/config_parsing.rs (+16) and the src/bolt_v3_live_node/tests/* plumbing additions were not deeply inspected; the config
     behavior they cover is independently pinned by the archetype validation/derivation tests.

   Final verdict: No substantive findings — code-complete for the declared #1349/#1179 slice at
   85c9093d7c2f4d0a308013d9a69509f6a78e80f5; exact-head Rust CI remains pending.
GLM: I've completed a thorough adversarial re-review. Here is my assessment.
Head verified: 85c9093d7c2f4d0a308013d9a69509f6a78e80f5 — confirmed match against GitHub headRefOid. Main at 7095c6f5f250b99a453300fafd0c9046c3da4a5b.
Reviewer source and exact model: opencode (interactive CLI tool); zai-coding-plan/glm-5.2
Blocking findings:
No substantive findings.
Non-blocking findings:
None.
Resolved prior findings:
Prior finding	Resolution at 85c9093d
N1 — recovered_terminal_settlement_eligibility lacked RecoveryBootstrap origin check	Resolved. src/strategies/binary_oracle_edge_taker/mod.rs:1329-1346: function now takes origin: ManagedPositionOrigin and gates RecoveryUnknownInterval on origin == ManagedPositionOrigin::RecoveryBootstrap. Caller at line 2874 passes ManagedPositionOrigin::RecoveryBootstrap explicitly.
N2 — operator_health_surface used .expect() on poisoned lock	Resolved. src/bolt_v3_live_node.rs:1417-1432: method now returns Result<BoltV3OperatorHealthSurface> and uses .map_err(|_| anyhow::anyhow!("settlement health lock poisoned")). emit_operator_health_surface_transition (line 1436-1447) handles Result with match, logging on error. No .expect() or .unwrap() remains on settlement_health. Sole caller verified — no external callers.
N3 — Restart appended a new terminal_settlement record on each restart	Resolved. New TerminalEvidenceState enum (PersistCanonical / CanonicalAlreadyDurable) at line 312-315. New terminal_settlement_keys: BTreeSet<String> field tracks canonical-durable keys. apply_terminal_settlement_transition (line 1361) skips record_terminal_settlement when CanonicalAlreadyDurable. Restart path at line 2887-2890 checks self.terminal_settlement_keys.contains(&settlement_key) to select the state. Recovery populates terminal_settlement_keys via read_terminal_settlement_keys_for_recovery_scope (line 2949-2960). Test restart_reconstructs_expired_terminal_transition_from_durable_booking_error uses the production JSONL writer, performs two bootstraps, and asserts durable count stays at 1 (line 2234-2243).
Invariant assessment:
Invariant	Status	Evidence
Terminal lifecycle assertions inspect TerminalSettlement.lifecycle	✓	adverse_path_harness.rs:1867-1871: filters on TerminalSettlement(evidence) if evidence.lifecycle.transition == SettlementBookingTerminal, extracts evidence.lifecycle.position_id
Restart distinguishes legacy-only from already-canonical keys	✓	mod.rs:2887-2890: terminal_settlement_keys.contains determines PersistCanonical vs CanonicalAlreadyDurable
Legacy-only evidence receives at most one canonical upgrade	✓	Key not in terminal_settlement_keys → PersistCanonical → one write → key inserted. Second restart: key now in set → CanonicalAlreadyDurable → skip.
Existing canonical evidence not re-appended across restarts	✓	Test asserts read_terminal_settlement_evidence(&evidence_path, 100_000).len() == 1 after second bootstrap (line 2234-2243)
Restart differential uses production JSONL writer	✓	adverse_path_harness.rs:2115-2119: JsonlBoltV3DecisionEvidenceWriter::from_test_path(&evidence_path)
Recovered unknown-interval eligibility requires RecoveryBootstrap	✓	mod.rs:1341: origin == ManagedPositionOrigin::RecoveryBootstrap && position.lifecycle.interval_end_ms().is_none()
Live-entered and pending-adopted positions retain interval-end	✓	exposure.rs:3945-4015: live_entered_and_pending_adopted_positions_retain_interval_end_boundary
Durable commit → release → fallible health mutation/report order	✓	mod.rs:1394-1413: record_terminal_settlement → exposure = Flat → health emitter. Doc comment (line 1358-1361) documents this order.
Poisoned health locks cannot panic; failures explicit and contextual	✓	All three lock sites (operator_health_surface, build_settlement_health_transition_emitter, emit_operator_health_surface closure) use .map_err() with contextual messages. emit_operator_health_surface_transition logs on error.
Live and restart use one canonical terminal_settlement schema	✓	Both call record_terminal_settlement with BoltV3TerminalSettlementEvidence. Test live_and_restart_terminal_settlement_share_one_canonical_schema verifies round-trip with distinct keys.
Legacy JSONL remains readable	✓	#[serde(default)] on terminal_lifecycle; test legacy_nested_terminal_booking_error_remains_readable
Non-enforcing pool derives currency without arming admission	✓	config/root.toml: enforce_submit_admission = false; config_parsing.rs test asserts !settlement_pool.enforce_submit_admission
No fallback chain, duplicate path, timer, latch, or second source of truth	✓	terminal_settlement_keys and settlement_booking_error_keys serve distinct purposes (canonical evidence idempotency vs booking-error idempotency). TerminalEvidenceState is a state flag, not a parallel latch. No timers or fallback paths added.
PR metadata names exact current head	✓	## Head = 85c9093d7c2f4d0a308013d9a69509f6a78e80f5
Verification gaps:
1. The restart test (adverse_path_harness.rs:2231-2243) asserts the durable terminal-record count after the second bootstrap but does not assert the health transition count. In production this is not an issue — each restart reconstructs the settlement_health mutex from nominal(), so the count is always 1 per restart. In the test, the recording emitter accumulates across bootstraps within the same strategy instance, so a second bootstrap emits a second health transition. This is a test artifact, not a production defect, but the test could be strengthened by asserting health_transitions.len() after the second bootstrap to document the expected behavior.
2. Exact-head full CI (clippy, compile, nextest) has not run. Local non-compile gates (just fmt-check, just source-fence-static, just deny) are reported as passing. Per repo policy, draft full-CI dispatch is prohibited; exact-head Rust gate remains pending for the owner/reviewer.
Final verdict:
No substantive findings — code-complete for the declared #1349/#1179 slice at 85c9093d7c2f4d0a308013d9a69509f6a78e80f5; exact-head full CI remains pending.
Grok: Head verified:
85c9093d7c2f4d0a308013d9a69509f6a78e80f5 (GitHub PR head matches; base 7095c6f5f250b99a453300fafd0c9046c3da4a5b; PR body ## Head names the same SHA)

Reviewer source and exact model:
Grok 4.5 (xAI) — adversarial review-only session agent. Not a ci/ai-review.toml deliverable (glm-5.2 / kimi-k2.7-code / claude-opus-4-8).

Blocking findings:
No substantive findings.

Non-blocking findings:
1. Stale architectural claim in booking-error key reader
   • File:line: src/bolt_v3_decision_evidence.rs:4335-4336
   • Scenario: Comment still says restart “may durably confirm the same terminal key again” as state-transition replay, but production restart now uses TerminalEvidenceState::CanonicalAlreadyDurable (mod.rs:2887-2896, 1397-1401) and does not re-append.
   • Violated invariant: Stale architectural claim / dual mental model of restart evidence.
   • Smallest root-cause correction: Rewrite the comment to state that terminal keys seed the in-memory latch for no-append release, not durable re-confirmation.

Resolved prior findings:
┌──────────────────────────────┬────────────────────────────────────────────────────────────────────────────────┐
│ Prior                        │ Resolution at 85c9093                                                          │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Lifecycle asserts on         │ Fixed — adverse_path_harness.rs:1864-1871 projects TerminalSettlement          │
│ standalone OrderLifecycle    │ .lifecycle                                                                     │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Restart re-appended terminal │ Fixed — terminal_settlement_keys + CanonicalAlreadyDurable skip record_        │
│ every bootstrap              │ terminal_settlement (mod.rs:2887-2896, 1397-1401)                              │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ No legacy vs canonical       │ Fixed — separate settlement_booking_error_keys / terminal_settlement_keys load │
│ distinction                  │ (mod.rs:2940-2997)                                                             │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Legacy-only multi-upgrade    │ Fixed — first restart PersistCanonical once; later restarts no-append          │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Restart test not on          │ Fixed — JsonlBoltV3DecisionEvidenceWriter::from_test_path + two bootstraps +   │
│ production JSONL / no 2nd    │ read_terminal_settlement_evidence len == 1 (adverse_path_harness.rs:2115-2242) │
│ bootstrap pin                │                                                                                │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Recovered unknown-interval   │ Fixed — recovered_terminal_settlement_eligibility requires origin ==           │
│ without RecoveryBootstrap    │ RecoveryBootstrap (mod.rs:1344-1351)                                           │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Live/pending interval-end    │ Covered — exposure.rs test live_entered_and_pending_adopted_positions_retain_  │
│ boundary                     │ interval_end_boundary                                                          │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Health-before-release doc/   │ Fixed — comment + order durable → Flat → health (mod.rs:1359-1435)             │
│ order                        │                                                                                │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Poisoned health lock panic / │ Holds — map_err + strategy log after release; unit production_settlement       │
│ silent failure               │ _health_emitter_returns_context_for_poisoned_lock                              │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Dual schema / legacy         │ Holds — live+restart terminal_settlement; legacy booking-error readable        │
│ unreadable                   │                                                                                │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Non-enforcing pool arming    │ Holds — config/root.toml:165 enforce_submit_admission = false                  │
│ admission                    │                                                                                │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Fallback release / dual      │ Holds — single apply path; no restored unconditional Flat helper               │
│ latch / etc.                 │                                                                                │
├──────────────────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ Stale PR Head metadata       │ Fixed — body names 85c9093…                                                    │
└──────────────────────────────┴────────────────────────────────────────────────────────────────────────────────┘

Invariant assessment:
• Terminal eligibility: Only MarketExpired and recovery-bootstrap unknown-interval; private enum; single apply path.
• No unsafe Flat from pending/live-manageable nonterminal: eligibility bail + negative test.
• Canonical persistence: one terminal_settlement writer path; live nests booking-error; restart upgrade or no-append.
• Restart evidence growth: durable terminal count stable across second bootstrap (proven).
• Health order: commit → release → fallible health; failures explicit/contextual, do not re-park.
• Config/admission: shared settlement_currency_for_execution_account; non-enforcing pool does not arm submit admission.
• No second safety authority / fallback release / timer / ack shortcut / parallel latch introduced in this 20-file slice.
• Log dedupe: entry block-reason WARN is reporting-only set-change; not a second release latch.

Verification gaps:
• Exact-head full Rust CI not run (review-only; no compile/test execution here).
• Second bootstrap does not assert health-transition count remains 1 (only durable terminal length); in-process double bootstrap would still re-fire health under CanonicalAlreadyDurable. Production is single bootstrap per process.
• No unit covering legacy booking-error key + not-yet-expired residual → BlindRecovery via ineligible recovered terminal path.

Final verdict:
Prior blocking findings resolved at 85c9093d7c2f4d0a308013d9a69509f6a78e80f5.
No substantive findings for the declared #1349/#1179 slice against the listed invariants and the actual diff; exact-head full CI remains pending.

---

we just changed comment?

---

we need to resolve Ci failures

---

what was the error?

---

explain in plain english - make this concise

---

CI still fails - can you review carefully and keep polling until CI is actually green