Review request: bolt-v2 PR #1222 — FULL-PR adversarial review. Part of #437 context.

Repo: https://github.com/seungpyoson/bolt-v2
PR:   https://github.com/seungpyoson/bolt-v2/pull/1222
Base: main @ dc152ec5fd9db62ed189beba9280b5feb52011ba (merge-base; branch carries a clean merge
      of main through #1185)
Head:        628226aad8a1181bee8dc65580bc3407d2a53376iverse_operator_inputs.rs,
 .../backtesting_vertical_slice_source_universe_conversion_queue.rs)
+ 2 committed reference artifacts:
 specs/.../source-universe-execution-acceptance-ledgers/binance-bybit-pmxt-current/ledger/
   source-universe-execution-acceptance-ledger.json (ONE sha line changed)
 specs/.../source-universe-operator-inputs/binance-.../operator-inputs/
   source-universe-operator-inputs.json (trailing-newline rewrite only)

Context (root cause established by reproduction at pristine main — verify, don't re-litigate):
a "reproducibility" test ran the operator-inputs generator on the COMMITTED spec with no output
redirect, overwriting the committed artifact in the source tree at test time. Regenerated bytes
differ from committed bytes by exactly one trailing newline, so a downstream acceptance-ledger
test (which hashes the file raw) failed depending on test-execution order. Main stayed green(not hand-edited).

Evidence on file (verify, don't trust): parent-commit red proof quoted in the PR body (full
suite 515/516 failing on the acceptance-ledger test + `git status --porcelain` showing the
committed artifact modified after the run); post-fix green proof (516/516 + clean porcelain);
full CI dispatch 28709807377 and Backtester CI dispatch 28709811998, both green at exactly
head 628226aad. Note: the required `gate` context is queue-only by repo policy — the dispatch
runs above are the pre-merge proof, per repo convention.

Report findings as: severity / file + symbol anchor / concrete failure scenario.
"Looks fine" only per-guarantee, with reasoning stated.

This is the critical-path PR — once its panel comes back and I triage, it goes to sp-reviewer and the queue, which then un-pauses #1219 and unlocks Briefs 5–6.

---

#1222 round-2 review packet:

Review request: bolt-v2 PR #1222, round 2 — FULL-PR adversarial review at the revised head.

Repo: https://github.com/seungpyoson/bolt-v2
PR:   https://github.com/seungpyoson/bolt-v2/pull/1222
Head: b07b315135b9e1934d779fc65299952f2cca9fd8 (round-1 head was 628226aad; the branch carries a
merge of main — #1216/#1217 content in the range is merged main, not PR work; the PR shows
BEHIND because main moved again — the merge queue handles that)
Base: merge-base with origin/main.

What round 2 changed (implementing the round-1 panel findings):
1. F1 fixed: the Binance execution-pack's operator-inputs sha updated to 81886b67... with the
   acceptance ledger's execution-pack sha moved in lockstep. DEVIATION (declared, per the
   fix brief's conditional): the pack was NOT regenerated via its generator — the committed
   spec lacks record_limit and regeneration would regrow evicted execution-pack/runs/*
   artifacts — so this is a minimal recorded-sha edit. Verify the justification holds and the
   edit is exactly the two pinning fields (pack ref + ledger ref).
2. NEW CLASS GATE: a test walks every committed artifact_refs entry across execution packs and
   acceptance ledgers under specs/023-nt-research-analytics-platform/reference/ and asserts
   recorded sha256 == sha256 of the current committed file bytes (evicted paths asserted
   against EvictedFixtureIndex instead). Red proof on file: Backtester CI run 28711599503 at
   pre-fix head 3e963dbb fails quoting the exact stale pack ref (recorded 8df9e1f9... vs
   actual 81886b67...).
3. Lookup consolidation: the indexed_evicted_sha256 triplication is replaced by
   EvictedFixtureIndex::sha256_for / entry_for on the owning type.
4. The bybit test's masking `run_plan_ref.sha256 = ...` overwrite was removed (path rewrite
   kept), so the byte-compare now genuinely covers the stamped sha.

PRE-REGISTERED MISS (don't re-discover; do check for others like it): the test
..._materializes_every_pmxt_archive_index_object_without_sha256_claim was NOT renamed despite
now asserting a sha claim — it gets renamed in the post-panel fix pass.

Try to break:
1. The class gate's coverage: does it truly walk EVERY 
   packs + all acceptance ledgers, all universes — not just binance)? Any committed sha-pinning
   reference OUTSIDE those two families (work orders? completion ledgers? conversion queues?)
   that remains ungated?
2. The gate's evicted-path branch: could a stale sha on an EVICTED artifact pass (index sha vs
   recorded sha vs actual regenerated bytes — check the three-way consistency)?
3. The minimal-edit deviation: is the record_limit/regrowth justification real (check the
   committed spec + generator behavior)? Does the hand-edited pack now fail any OTHER
   consistency check (pack-internal digest, counts)?
4. Round-1 guarantees still hold at this head: no test writes into the committed tree;
   byte-compares are real; the 4 remaining trailing-newline Binance artifacts are a declared
   tracked follow-up AND the new gate stays green with them as-is (it compares recorded sha vs
   current bytes — both still carry the newline — confirm no contradiction).
5. FIX 3 consolidation: all former indexed_evicted_sha256 callers actually delegate to the
   EvictedFixtureIndex methods; no stale copy left anywhere.

Evidence on file (verify, don't trust): full CI dispatch 28712987067 and Backtester CI dispatch
28712447278, both green at exactly b07b315; red proof run 28711599503 at 3e963dbb; post-body
PR-event runs also green (28713372515, 28713372595).

Report findings as: severity / file + symbol anchor / concrete failure scenario.
"Looks fine" only per-guarantee, with reasoning stated.

---

Review request: bolt-v2 PR #1222, round 3 (final) — FULL-PR adversarial review.

Repo: https://github.com/seungpyoson/bolt-v2
PR:   https://github.com/seungpyoson/bolt-v2/pull/1222
Head: 2d5dad33b1068608bbf555feeeb41840a9446a07 (round-2 head was b07b315; round-3 delta:
the artifact-refs gate generalized from 2 typed family-walks to ONE generic recursive walk
(+230/−73), plus the pre-registered test rename ..._with_evicted_sha256_claim).

What round 3 changed (implementing the round-2 panel consensus):
1. The class gate now walks EVERY committed reference JSON under
   specs/023-nt-research-analytics-platform/reference/ and checks every {role, path, sha256}
   pin it finds — committed paths hashed against current bytes, evicted paths against
   EvictedFixtureIndex. Red proof: Backtester CI run 28714564871 at scratch head 2ac3a93eb —
   one flipped hex char in the binance operator-inputs' object-gates pin (a family UNGATED in
   round 2) caught with the exact recorded-vs-actual quote.
2. DEVIATION (per the fix brief's conditional): the Binance evicted work-order chain was NOT
   regenerated — its frozen internal ref pins the pre-fix operator-inputs sha; documented in
   the PR body as an eviction-model limitation with the stale chain quoted.

Try to break (focus = the generalized walk itself):
1. Walk completeness: does the recursive discovery genuinely reach every reference JSON
   (nested dirs, all universes, status files with committed_input_hashes-style blocks if
   shaped as {role,path,sha256})? Name any committed sha-pin the walk still cannot see.
2. Walk correctness: false positives/negatives in the generic traversal — non-artifact-ref
   objects that happen to have {path, sha256} keys; refs whose paths resolve outside
   reference/ (path traversal); duplicate-path handling; the evicted-vs-committed branch
   decision (what if a path is BOTH committed and indexed?).
3. The documented work-order limitation: is the wording accurate and complete (frozen internal
   chain, why regeneration is deferred, what would resolve it)? Is there any OTHER evicted
   artifact whose frozen internals pin a since-changed sha (same class as GPT's round-2
   finding) that should be listed in the same limitation note?
4. Round-1/2 guarantees at this head: no committed-tree mutation by tests; byte-compares
   intact; lookup consolidation intact; the 4 trailing-newline artifacts remain a declared
   follow-up and the gate stays green with them; the rename didn't lose any assertion.

Evidence on file (verify, don't trust): full CI dispatch 28715180165 + Backtester CI dispatch
28715133932, both green at exactly 2d5dad33b; red proof 28714564871 at 2ac3a93eb; the
verify-remote non-zero exit is the known queue-only-gate artifact (dispatch is the proof).

Report findings as: severity / file + symbol anchor / concrete failure scenario.
"Looks fine" only per-guarantee, with reasoning stated.

---

Review request: bolt-v2 PR #1222, round 5 (final) — FULL-PR adversarial review.

Repo: https://github.com/seungpyoson/bolt-v2
PR:   https://github.com/seungpyoson/bolt-v2/pull/1222
Head: a957df88e8242511d214461403cbcc189166b219 (round-4 head d67b5c1f3 was REJECTED by my
review for re-encoding both packs through a non-canonical serializer; round 5 redid it).

What round 5 changed:
1. Byte-surgical re-reconciliation: vs the round-3 baseline (2d5dad33b), the ONLY changed
   content in the packs/ledger is 7,890 execution_plan_sha256 values + 2 lockstep pack pins.
   Census quoted in the body (other_changed_lines=0, encoding_escape_changes=0) and
   independently reproduced by the requesting reviewer. Raw UTF-8 restored (zero \uXXXX
   escapes at head).
2. The PR body now explains the SYSTEMATIC cause of the 7,890 stale flat pins — verify the
   explanation is evidence-backed and consistent with what you can derive (why 100% of them,
   why the index side is the correct reconciliation target).
3. Gate scoping (±199 in the gate test): pins in dated status files targeting SOURCE paths
   (crates/, src/, scripts/) are treated as point-in-time attestations and excluded from
   current-bytes enforcement; reference/-tree targets remain fully enforced. Verify the
   exclusion is exactly that narrow (no reference-tree pin accidentally exempted) and
   documented in the gate's doc comment.

Try to break:
1. Reproduce the census independently: any changed byte in the two packs + ledger beyond the
   enumerated sha values is an automatic REQUEST_CHANGES.
2. The stale-pin cause narrative: is it proven or asserted? If asserted, does it at least
   falsifiably name the mechanism?
3. The source-path exclusion: enumerate what the gate now skips — confirm nothing under
   specs/023-.../reference/ is skipped, and the skip-list logic can't be fooled by path forms
   (../, absolute, repo:// prefix).
4. All prior-round guaran

Evidence (verify, don't trust): verify-remote 28729300054 + Backtester CI 28729300154, both
green at exactly a957df88e; round-5 census quoted in body; prior red proofs stand (round-3
1-hex flip run 28714564871; round-4 shape proofs 28716905474).

Report findings as: severity / file + symbol anchor / concrete failure scenario.
"Looks fine" only per-guarantee, with reasoning stated.

---

Review request: bolt-v2 PR #1222, round 6 (final) — focused review of the root-cause fix,
plus full-PR carry-over sanity.

Repo: https://github.com/seungpyoson/bolt-v2
PR:   https://github.com/seungpyoson/bolt-v2/pull/1222
Head: b496f841e0299f4f42da2e9b29d3e69d7cfd2c4f (round-5 head a957df88e was panel-reviewed:
3× zero findings + Claude's F1 = the generator root cause, which round 6 implements).

Round-6 delta (2 files): crates/backtesting-vertical-slice/src/backfill_execution_plan.rs —
the artifact's recorded hash is now sha256_hex(&bytes) of the PRETTY bytes actually written,
and the compact-serialization content_hash() helper is DELETED entirely; plus a new test
(retained_execution_plan_artifact_hash_matches_written_file_bytes).

Try to break:
1. The step-0 census claim (session-produced, independently verify): NO consumer anywhere
   relies on the OLD compact-serialization semantics of BackfillExecutionPlanArtifact
   .content_hash — grep every consumer of the field and of the artifact struct; a consumer
   that recomputes compact hashes for comparison would now mismatch.
2. Data neutrality: zero committed reference bytes changed in round 6 (the round-5 pins were
   already file-byte hashes, so the fixed generator must agree with them — confirm the two
   retained runs/00000 plans' pins match what the new code computes).
3. The deleted helper: nothing else called 
   still computes hash-of-compact-while-writing-pretty (the class, not just this instance —
   grep to_vec( vs to_vec_pretty( pairings near atomic_write).
4. The new test: asserts recorded hash == sha256 of on-disk bytes via a temp regeneration —
   confirm it fails on the old code (red proof run 28730630014 at scratch 5c8acdceb quoted
   with the exact hash pair) and cannot pass vacuously.
5. Carry-over sanity at this head: round-5 census still holds (no data changes since);
   gate scoping unchanged; deferred LOWs recorded in the body.

Evidence (verify, don't trust): full CI 28730645954 + Backtester CI 28730646429 green at
exactly b496f841e; red proof 28730630014 at scratch 5c8acdceb.

Report findings as: severity / file + symbol anchor / concrete failure scenario.
"Looks fine" only per-guarantee, with reasoning stated.