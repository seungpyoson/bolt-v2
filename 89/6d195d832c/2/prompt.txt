• PR #1232 updated and pushed.

  Head SHA: 28e9b7de5bb5616f5a0774759be9dfad73b253c2

  Changes:

  - Added hardcoded full-run-body SHA-256 pins:
      - ROOT_NEXTEST_ARCHIVE_PARTITIONS_RUN_SHA256
      - BVS_NEXTEST_ARCHIVE_PARTITIONS_RUN_SHA256

  - Digest errors name the constant to update for legitimate
    reviewed workflow edits.

  - Kept existing readable fragment/wrapper errors ahead of the
    digest guard.

  - Added per-lane self-tests for:
      - rewriting if [[ "$rc" -ne 0 ]] to a never-true
        condition

      - a one-character shell comment line edit inside the
        partition run body

  Verification:

  - python3 scripts/verify_ci_workflow_hygiene.py passed.
  - python3 scripts/test_verify_ci_workflow_hygiene.py passed.
  - python3 -m py_compile ... passed.
  - git diff --check passed.

  Worktree is clean and local HEAD matches origin/codex/1213-
  ci-debuggability.