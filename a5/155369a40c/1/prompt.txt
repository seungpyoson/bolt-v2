▎ #1351: independent reviewer round posted REQUEST_CHANGES at head 9d01ac6111b666659c04e994e2d583778f22a71a (https://github.com/seungpyoson/bolt-v2/pull/1351#issuecomment-4935971860). Your rounds 1–2 content carries over fully — I proved the current merge-only head is byte-identical to your round-2-verified ddc3efe98 (diff-of-diffs = 0 lines), and fmt/fences pass at head.
▎
▎ F1 (blocking, control-verified): config_parsing::bolt_v3_archetype_keeps_default_provider_market_exit_shape_permissive FAILS at head, PASSES at main tip a226ca2fb — the D2 settlement-currency validator fires on that pre-existing fixture (binary-oracle strategy, HYPERLIQUID venue + POLYMARKET-001 account, no pools). The 678-test scope filter didn't include all of platform_config; my wider run = 1086/1087. This is the #1238 K-2 class: a validation-domain change must re-run every test whose fixtures the new domain judges — full suite at head, not scope filters. Fix direction is your design call as round owner: (a) fixture is now invalid-by-design → give it the same non-enforcing pool declaration (likely correct — it's a settleable archetype), or (b) the owning predicate needs narrower applicability + an out-of-domain differential. Requirements for clearance: full suite at the fixed head with the count reported, and sweep for any other fixture the predicate now judges.
▎ Not a concern: the PR's failing gate check is a same-SHA carry-forward provenance chain bottoming out at a transient in_progress run — not code; it resolves on a clean run.
▎ External review ruling: run ONE external round (Grok/GLM/Kimi, full-PR scope, pinned to the F1-fixed head) after the fix — campaign PR, money-path-adjacent release semantics; the campaign pipeline expects it. Then sp-reviewer + queue. Deploy rider stands: this PR ships a root.toml change → v0.1.12 = binary + on-box config regeneration.