schema_version = 1

[[entries]]
id = "subscription-routing-regression"
description = "Replay fixture for subscription ownership and custom-data routing changes."
changed_paths = [
  "crates/common/src/msgbus.rs",
  "crates/adapters/polymarket/src/http/mod.rs",
]
expected_seams = ["subscription_custom_data_semantics"]
expected_result = "ambiguous"

[[entries]]
id = "persistence-event-shape-regression"
description = "Replay fixture for persistence and event-shape drift crossing sink and lake seams."
changed_paths = [
  "crates/persistence/src/backend/catalog.rs",
  "crates/model/src/events/mod.rs",
]
expected_seams = [
  "normalized_sink_persistence_lake",
  "shared_type_contract",
]
expected_result = "ambiguous"
