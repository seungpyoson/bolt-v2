schema_version = 1
venue = "polymarket"
adapter_version = "bolt-v2"

[streams.quotes]
capability = "supported"
policy = "required"
provenance = "native"

[streams.trades]
capability = "supported"
policy = "required"
provenance = "native"

[streams.order_book_deltas]
capability = "supported"
policy = "required"
provenance = "native"

[streams.order_book_depths]
capability = "unsupported"
reason = "Polymarket has no native OrderBookDepth10 stream; use delta-backed subscribe_book_at_interval snapshots instead"

[streams.index_prices]
capability = "unsupported"
reason = "not applicable to prediction markets"

[streams.mark_prices]
capability = "unsupported"
reason = "not applicable to prediction markets"

[streams.instrument_closes]
capability = "unsupported"
reason = "Polymarket has no close events; resolution is a separate concept"
