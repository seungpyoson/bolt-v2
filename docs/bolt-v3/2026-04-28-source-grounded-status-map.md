# Bolt-v3 Source-Grounded Status Map

Date: 2026-04-30

Status: review draft.

Purpose: make the Bolt-v3 roadmap auditable from the repo. This file is not a
product promise and not a merge checklist. A row is only marked implemented when
there is source evidence plus test/verifier evidence. Existing code that works
only for the current provider/archetype/family stack is marked partial, not done.
The row numbers are a dependency-ordered map, not a percentage-complete score:
later rows may have old or partial code before earlier architecture rows are
clean.
Citations of `src/bolt_v3_*` modules are not exhaustive; absence of a file from
this map is not evidence that the file is empty or irrelevant.
Upstream NautilusTrader history is referenced where relevant; verify upstream
commit identifiers against `https://github.com/nautechsystems/nautilus_trader.git`
before treating them as Bolt prerequisites.

## Current Read

Bolt-v3 is still in foundation work. The safest source-backed position is:

- Core TOML parsing, validation scaffolding, SSM direction, adapter mapping,
  client registration, controlled connect/disconnect, runtime capture
  (shared/legacy reuse), and pure instrument filter have code and tests.
- The provider-neutral architecture is not complete. The latest correction only
  removed closed provider/archetype/family identity dispatch from the first core
  layer. Adapter mapping, secret projection, client registration, discovery,
  cost/fee facts, strategy construction, and portfolio-allocation evidence still
  need explicit boundaries.
- The baseline must not be treated as ready for merge to `main` as a general
  Bolt-v3 foundation until the remaining provider-specific core-adjacent
  boundaries are addressed or explicitly accepted as scoped residuals.
- This branch pins NautilusTrader to upstream release `v1.226.0`
  (`38b912a8b0fe14e4046773973ff46a3b798b1e3e`). The pin-change evidence is
  recorded under `docs/bolt-v3/research/nt-pin-change/`. This keeps the
  upstream Polymarket CLOB V2 support in a tagged NT beta release, but it does
  not prove Bolt live CLOB V2 execution readiness.

## Documentation Authority

Use the docs in this order:

1. `docs/bolt-v3/2026-04-28-source-grounded-status-map.md`: status and next
   evidence-backed work.
2. `docs/bolt-v3/2026-04-28-nt-first-boundary-doctrine.md`: architecture
   boundary doctrine.
3. `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md`: detailed runtime
   contract, subject to reconciliation with the doctrine.
4. `docs/bolt-v3/2026-04-25-bolt-v3-schema.md`: config/schema contract, subject
   to reconciliation with the doctrine.

`docs/bolt-v3/research/` files are evidence. `docs/bolt-v3/archive/` files are
reference only. Prior AI responses and temporary audit packets are not
implementation inputs unless their conclusions have been source-checked and
folded into one of the active docs above.

## Roadmap State

| # | Area | Status | Source Evidence | Test / Verifier Evidence | Gap / Next Decision |
|---:|---|---|---|---|---|
| 1 | Rust NT-native LiveNode boundary | Partial | `src/bolt_v3_live_node.rs`, `src/bolt_v3_client_registration.rs`, `src/bolt_v3_strategy_registration.rs` | `tests/bolt_v3_production_entrypoint.rs`, `tests/bolt_v3_client_registration.rs`, `tests/bolt_v3_strategy_registration.rs`, `tests/bolt_v3_controlled_connect.rs` | Build/register/strategy-registration boundaries exist, but live-capital execution proof is still separate. |
| 2 | TOML-owned runtime configuration | Implemented for current source coverage | `src/bolt_v3_config.rs` carries root, venue, strategy, timeout, persistence, risk, complete explicit NT data-engine fields, complete explicit NT exec-engine fields, complete explicit NT risk-engine fields, complete pinned NT `LoggerConfig` fields, and top-level `LiveNodeConfig` disabled/false fields accepted by the Rust live runtime; `src/bolt_v3_live_node.rs` explicitly maps those values, while `data_clients` and `exec_clients` remain derived from configured venues through provider Adapter registration; legacy live-local config surfaces are retired from the current source tree | `tests/config_parsing.rs`; `src/bolt_v3_live_node.rs::live_node_config_maps_explicit_nt_runtime_defaults_from_v3_root`; `src/bolt_v3_live_node.rs::live_node_config_maps_explicit_logger_residuals_in_builder_path`; `tests/bolt_v3_production_entrypoint.rs::codebase_does_not_expose_dead_platform_runtime_actor_or_catalog_modules`; Rust struct literal exhaustiveness for `LiveNodeConfig` | Current source coverage is closed by the literal/default/provider/entrypoint fences; live deploy readiness remains tracked in later rows. |
| 3 | No Python runtime layer | Implemented as current source-scan gate | Repo rule requires pure Rust binary; `Cargo.toml`, `Cargo.lock`, and production `src/**/*.rs` carry no PyO3, maturin, cpython, or Python export bridge for the Rust binary; current Bolt-v3 runtime path stays in Rust source and depends on the Rust AWS SDK SSM crates | `scripts/verify_bolt_v3_pure_rust_runtime.py` scans Cargo metadata, production Rust source, and the current Bolt-v3 runtime source including runtime-capture and strategy modules for PyO3, maturin, Python runtime subprocesses, and AWS CLI subprocesses; `just source-fence` wires the verifier into CI | This proves the current Bolt-v3 runtime path has no Python runtime layer or AWS CLI secret subprocess. Python verifier scripts remain CI tooling only. |
| 4 | Config schema and parser | Implemented for current schema | `src/bolt_v3_config.rs` defines root/strategy config and `load_bolt_v3_config` | `tests/config_parsing.rs` | Schema can evolve, but current parser/validator exists. |
| 5 | Runtime values come from TOML | Implemented for current source coverage | `src/bolt_v3_config.rs` carries timeout, persistence, risk, venue, strategy, complete explicit NT data-engine fields, complete explicit NT exec-engine fields, complete explicit NT risk-engine fields, complete pinned NT logger fields, and top-level NT disabled/false fields; `data_clients` and `exec_clients` are derived from venues through provider Adapter registration instead of root-level NT client maps; `docs/bolt-v3/research/runtime-literals/bolt-v3-runtime-literal-audit.toml` classifies current candidate runtime literals across all `src/**/*.rs` production files, including runtime support and strategy modules | Config parsing tests cover selected parser/validator cases; `src/bolt_v3_live_node.rs::live_node_config_maps_explicit_nt_runtime_defaults_from_v3_root` asserts the NT mapping; `src/bolt_v3_live_node.rs::live_node_config_maps_explicit_logger_residuals_in_builder_path` asserts logger mapping; `scripts/verify_bolt_v3_runtime_literals.py`; `scripts/verify_bolt_v3_legacy_default_fence.py`; `scripts/verify_bolt_v3_strategy_policy_fence.py`; full-source scan reported zero unclassified literals | Current source coverage is closed for production literals/defaults/policy hardcodes. Single-value enums (`RuntimeMode::Live`, `OmsType::Netting`, `CatalogFsProtocol::File`, `RotationKind::None`) remain explicit current-mode constraints, not hidden defaults; widening them is a future product scope decision. |
| 6 | Canonical `just check` validation path | Missing | `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md` requires one structural/live validation entrypoint | No `just check` implementation found in `justfile`; `just live-check` exists but validates secret-config completeness only | Build this before claiming operator readiness. |
| 7 | Bolt-v3 binary / CLI entrypoint | Implemented for current binary path | `src/main.rs` `Command::Run` loads `load_bolt_v3_config`, builds `build_bolt_v3_live_node(&loaded)?`, and runs through `run_bolt_v3_live_node(&mut node, &loaded).await?` | `tests/bolt_v3_production_entrypoint.rs`; `scripts/verify_bolt_v3_status_map_current.py` | This proves the current `bolt-v2 -- run` binary path enters the gated Bolt-v3 runner. Release identity and deploy trust remain separate rows. |
| 8 | SSM-only secret source | Implemented for current providers | `src/bolt_v3_secrets.rs` resolves configured venue secret blocks through `ProviderBinding::resolve_secrets`; `src/bolt_v3_providers/mod.rs` defines provider-owned resolved-secret and redaction interfaces; `src/bolt_v3_providers/polymarket.rs` and `src/bolt_v3_providers/binance.rs` own provider-specific SSM fields, forbidden env vars, resolved-secret structs, and redaction values; `src/secrets.rs` owns the shared Rust SSM resolver session | Unit tests in `src/bolt_v3_secrets.rs`; config parsing tests; `scripts/verify_bolt_v3_provider_leaks.py`; `scripts/verify_bolt_v3_pure_rust_runtime.py` | Current secret resolution is SSM-only and provider-owned for configured providers. Adding a provider still requires adding a provider module plus binding-table entry. |
| 9 | Core provider identity is configured key, not closed enum | Implemented in first correction slice | `src/bolt_v3_config.rs` uses `ProviderKey` in the verified Bolt-v3 core boundary; provider-specific adapter, secret, and registration behavior routes through provider binding interfaces | `scripts/verify_bolt_v3_core_boundary.py`; `scripts/verify_bolt_v3_provider_leaks.py` | No `VenueKind` enum remains in the verified Bolt-v3 core boundary. Adding a provider still requires adding a provider module plus binding-table entry; see rows 11 and 14-17. |
| 10 | Core archetype identity is configured key, not closed enum | Implemented for current strategy archetype | `src/bolt_v3_config.rs` uses `StrategyArchetypeKey` in the verified Bolt-v3 core boundary; `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` owns the current archetype runtime binding and NT strategy registration | `scripts/verify_bolt_v3_core_boundary.py`; `tests/bolt_v3_strategy_registration.rs::bolt_v3_live_node_build_registers_configured_binary_oracle_strategy` | No `StrategyArchetype` enum remains in the verified Bolt-v3 core boundary. Adding an archetype still requires adding an archetype module plus binding-table entry. |
| 11 | Provider validation dispatch | Partial | `src/bolt_v3_providers/mod.rs`, `src/bolt_v3_providers/polymarket.rs`, `src/bolt_v3_providers/binance.rs` | `tests/config_parsing.rs`, `tests/bolt_v3_provider_binding.rs` | Per-provider validation modules exist; provider list is still statically registered in `mod.rs`. |
| 12 | Archetype validation and runtime dispatch | Implemented for current strategy archetype | `src/bolt_v3_archetypes/mod.rs` dispatches validation through `ArchetypeValidationBinding` and runtime registration through `StrategyRuntimeBinding`; `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` owns the current archetype runtime binding | `tests/config_parsing.rs`; `tests/bolt_v3_strategy_registration.rs` | Current dispatch has Module Interfaces for validation and runtime registration. Adding an archetype still requires adding an archetype module plus binding-table entry. |
| 13 | Market-family validation dispatch | Partial | `src/bolt_v3_market_families/mod.rs` dispatches through `MarketFamilyValidationBinding` | `tests/bolt_v3_instrument_filters.rs` | Only validation and instrument-filter config are covered; live readiness is not. |
| 14 | Provider-specific adapter mapping behind provider modules | Implemented for current providers | `src/bolt_v3_adapters.rs` routes each venue through `ProviderBinding::map_adapters`; concrete Polymarket/Binance NT configs and factories live in `src/bolt_v3_providers/polymarket.rs` and `src/bolt_v3_providers/binance.rs`; `BoltV3VenueAdapterConfig` is a provider-neutral struct | `tests/bolt_v3_adapter_mapping.rs`, `tests/bolt_v3_provider_binding.rs`, `scripts/verify_bolt_v3_provider_leaks.py` | Current provider adapter mapping has a provider-owned Module Interface. Adding a provider still requires adding a provider module plus binding-table entry. |
| 15 | Provider-specific secret handling behind provider modules | Implemented for current providers | `src/bolt_v3_secrets.rs` resolves via `ProviderBinding::resolve_secrets`; provider modules own forbidden env vars, secret field names, provider-specific resolved-secret structs, and redaction values; `ResolvedBoltV3VenueSecrets` is a provider-neutral trait object | Unit tests in `src/bolt_v3_secrets.rs`; `scripts/verify_bolt_v3_provider_leaks.py` | Current secret resolution has a provider-owned Module Interface. Adding a provider still requires adding a provider module plus binding-table entry. |
| 16 | Provider-specific client factory registration behind provider modules | Implemented for current providers | `src/bolt_v3_client_registration.rs` consumes provider-neutral `BoltV3AdapterConfigs`; concrete factory selection happens inside provider `map_adapters`; `BoltV3RegisteredVenue` records data/execution booleans, not provider variants | `tests/bolt_v3_client_registration.rs`, `tests/bolt_v3_provider_binding.rs`, `scripts/verify_bolt_v3_provider_leaks.py` | Current client-registration path has no concrete provider factory imports. Adding a provider still requires adding a provider module plus binding-table entry. |
| 17 | Provider-specific credential log suppression behind provider modules | Implemented for current provider registry | `src/bolt_v3_providers/mod.rs` exposes provider-owned credential log module bindings; `src/bolt_v3_providers/polymarket.rs` and `src/bolt_v3_providers/binance.rs` own their NT credential module paths; `src/bolt_v3_live_node.rs` consumes the provider binding surface instead of hardcoding provider module paths | `src/bolt_v3_providers/mod.rs::credential_log_modules_are_provider_owned`; `src/bolt_v3_live_node.rs::live_node_config_suppresses_nt_credential_module_logs_to_warn`; `tests/bolt_v3_credential_log_suppression.rs` | Current provider registry owns the credential-log suppression metadata. Adding a provider still requires adding a provider binding in `src/bolt_v3_providers/mod.rs`, but no live-node change is required for credential log suppression. |
| 18 | LiveNode assembly without running trading | Partial | `src/bolt_v3_live_node.rs` builds `LiveNode`, exposes the gated `run_bolt_v3_live_node` runner wrapper, and delegates the fail-closed pre-run contract to `src/bolt_v3_live_canary_gate.rs` | `tests/bolt_v3_production_entrypoint.rs`, `tests/bolt_v3_client_registration.rs`, `tests/bolt_v3_live_canary_gate.rs`, `tests/bolt_v3_controlled_connect.rs::live_node_module_only_runs_nt_after_live_canary_gate` | `LiveNodeBuilder::build` is not purely passive: it parses Polymarket private key material into an NT signer and performs internal NT engine/message-bus subscriptions. It opens no network socket and does not run the event loop. The approved runner wrapper enters the NT event loop only after the live canary gate accepts the loaded config and no-submit readiness report. |
| 19 | Controlled connect/disconnect boundary | Partial | `src/bolt_v3_live_node.rs` exposes bounded connect/disconnect functions | `tests/bolt_v3_controlled_connect.rs` mock-client tests | Boundary exists and mock tests pass. No real-adapter connect test proves production sockets; this does not prove market/instrument readiness. |
| 20 | Runtime capture off hot path | Partial (shared module) | `src/nt_runtime_capture.rs` is shared runtime-capture code reused by Bolt-v3 | `tests/nt_runtime_capture.rs`, `python3 scripts/verify_runtime_capture_yaml.py` | Capture scope is separate from trading readiness/execution. This is not a new Bolt-v3 architecture module. |
| 21 | Activated-scope broad NT evidence capture | Missing | `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md` requires capture of NT data, execution, order, position, account, report, and lifecycle streams for activated scope | No activated-scope end-to-end evidence test found | Required before canary so local evidence can reconstruct live behavior. |
| 22 | Local evidence / decision-event catalog round-trip | Missing | `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md` requires registered decision-event custom-data round trip and local catalog readiness | No Bolt-v3 decision-event catalog readiness test found | Must be proven before order-submission events can gate submit. |
| 23 | Pure instrument filter | Partial | `src/bolt_v3_instrument_filters.rs`, `src/bolt_v3_market_families/updown.rs` | `tests/bolt_v3_instrument_filters.rs` | Current family implementation is one family binding, not generic live selection. |
| 24 | Provider discovery/filter binding | Implemented for current provider/family path | `src/bolt_v3_adapters.rs` derives `InstrumentFilterConfig` from strategy TOML and passes it through `ProviderBinding::map_adapters`; `src/bolt_v3_providers/polymarket.rs` owns Polymarket market-slug filter construction and `new_market_filter` mapping | `tests/bolt_v3_provider_binding.rs`; `tests/bolt_v3_adapter_mapping.rs` | Current provider filter mapping has a provider-owned Module Interface for Polymarket updown targets. Broader discovery/activation policy remains separate. |
| 25 | NT instrument request/readiness | Missing | Runtime contract requires instrument/cache readiness through NT state | No Bolt-v3 readiness verifier/test found | Define readiness from NT cache/provider state before live market selection. |
| 26 | Selected live market target stack | Implemented for current strategy path; live proof still missing | `src/bolt_v3_market_families/mod.rs` exposes provider-neutral market-family selection through `select_binary_option_market_from_target`; `src/bolt_v3_market_families/updown.rs` implements current/next updown selection from NT `BinaryOption` instruments; `src/strategies/binary_oracle_edge_taker.rs` refreshes selection through the configured family key and applies active/idle snapshots | Tests in `src/bolt_v3_market_families/updown.rs`, `tests/bolt_v3_instrument_filters.rs`, and strategy tests in `src/strategies/binary_oracle_edge_taker.rs`; `scripts/verify_bolt_v3_provider_leaks.py` | Current code has a target-selection stack for `binary_oracle_edge_taker`; no real-adapter live cache/readiness proof yet. |
| 27 | Current `updown` price-to-beat anchor and Gamma validation | Contract corrected; implementation missing | #244 proves Gamma `eventMetadata.priceToBeat` is post-close only, Chainlink Data Streams REST is the runtime anchor, and sampled BTC/ETH 5m updown `event_page_slug` equals the CLOB market slug | #244 issue evidence; `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md` now states Chainlink runtime anchor and Gamma post-close validation | Runtime Chainlink anchor implementation, post-close Gamma validation implementation, and probe-script preservation remain open before current updown live orders. |
| 28 | Reference data through NT subscriptions | Implemented for current strategy path; live proof still missing | `src/strategies/binary_oracle_edge_taker.rs::on_start` subscribes configured reference quotes through NT; the runtime archetype projection writes configured `reference_venue` and `reference_instrument_id` into the strategy config | `tests/bolt_v3_strategy_registration.rs::binary_oracle_runtime_mapping_uses_configured_reference_data_role_key`; strategy reference-quote tests in `src/strategies/binary_oracle_edge_taker.rs` | Code path uses NT subscriptions and configured reference data for current strategy. Real provider subscription evidence is still required before live readiness. |
| 29 | Strategy instance scaling from TOML | Partial parser support only | `src/bolt_v3_config.rs` supports `strategy_files: Vec<String>` and duplicate checks | `tests/config_parsing.rs` | Loading many files is not the same as constructing/registering many NT strategies. |
| 30 | Concrete NT strategy construction | Implemented for current strategy archetype | `src/strategies/binary_oracle_edge_taker.rs` implements `BinaryOracleEdgeTakerBuilder`; `src/strategies/registry.rs` builds boxed NT strategies; `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` registers the built strategy on the NT `LiveNode` | `tests/bolt_v3_strategy_registration.rs::bolt_v3_live_node_build_registers_configured_binary_oracle_strategy`; builder tests in `src/strategies/binary_oracle_edge_taker.rs` | Current construction covers `binary_oracle_edge_taker`; additional archetypes need their own Module plus binding entry. |
| 31 | Strategy data subscriptions through NT | Implemented for current strategy path; live proof still missing | `src/strategies/binary_oracle_edge_taker.rs::on_start` subscribes reference quotes through NT and selection updates replace outcome book subscriptions through NT book-delta calls | Strategy unit tests cover reference quote handling and book subscription replacement events | Code path exists for current strategy, but there is no real-adapter live subscription proof yet. Must remain separate from live-readiness claims. |
| 32 | Decision math and structured decision events | Missing for Bolt-v3 | Runtime contract defines decision-event shapes; current archetype validation is not runtime decision logic | No Bolt-v3 decision-event/local-evidence tests found | Implement as archetype-owned, testable logic after data/readiness surfaces are stable. |
| 33 | Risk and sizing policy | Implemented for current strategy/admission path; live proof still missing | `src/strategies/binary_oracle_edge_taker.rs` sizes entries from configured `max_position_usdc`, `risk_lambda`, and `book_impact_cap_bps`; `src/bolt_v3_submit_admission.rs` enforces the TOML-derived live canary max-notional cap before NT submit; `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` validates strategy notional against root risk config | Strategy sizing tests in `src/strategies/binary_oracle_edge_taker.rs`; `tests/bolt_v3_submit_admission.rs`; config parsing tests | Current risk/sizing path exists for `binary_oracle_edge_taker` and live canary admission. It is not a full production risk engine, and live venue proof remains missing. |
| 34 | Order/fill lifecycle handling | Missing | Runtime contract assigns order/fill lifecycle truth to NT order/execution events | No Bolt-v3 order/fill lifecycle tests found | Required before live order submission. |
| 35 | Position and balance reconciliation | Missing | Runtime contract assigns account, position, balance, average-price, and exposure truth to NT Portfolio/cache/venue-confirmed state | No Bolt-v3 reconciliation tests found | Required before sizing and exit decisions can be trusted. |
| 36 | Post-restart reconciliation | Missing | Runtime contract requires recovery authority from NT state reconciliation and venue-confirmed state | No Bolt-v3 restart-reconciliation tests found | Required before canary/live restart safety. |
| 37 | Per-market rate/cooldown limits | Missing | Runtime contract defines retry/block timing concepts but no accepted runtime implementation was found | No Bolt-v3 per-market cooldown/rate-cap tests found | Needed before live retry loops can run safely. |
| 38 | Live-path observability / metrics | Missing | Runtime contract says NT-native observability first plus minimal structured decision events | No Bolt-v3 live-path observability tests found | Required for canary diagnosis and operator safety. |
| 39 | Order construction using NT-native IDs/types | Implemented for current strategy path; live proof still missing | `src/strategies/binary_oracle_edge_taker.rs` builds configured entry and exit NT orders from strategy `entry_order`/`exit_order` config and current market context | Strategy order construction tests in `src/strategies/binary_oracle_edge_taker.rs`; Phase 9 audit report T040 evidence | Current path constructs NT orders after decision evidence and submit admission. Real live accept/fill/reject proof remains missing. |
| 40 | Dry-run / no-trade audit mode | Missing | Runtime capture exists, but not strategy dry-run audit | No dry-run strategy tests found | Define before execution gate. |
| 41 | Execution gate / kill switch | Implemented for live canary submit gate; broader kill switch missing | `src/bolt_v3_live_canary_gate.rs` validates approval, no-submit readiness, order-count cap, and notional caps before runner entry; `src/bolt_v3_submit_admission.rs` enforces the armed gate report before NT submit | `tests/bolt_v3_live_canary_gate.rs`; `tests/bolt_v3_submit_admission.rs`; submit-admission strategy tests in `src/strategies/binary_oracle_edge_taker.rs` | Canary submit gate exists. A broader operator kill switch and production execution policy remain separate missing work before production live trading. |
| 42 | Paper/shadow mode on live data | Missing | No accepted Bolt-v3 live data shadow runner found | No shadow-mode tests found | Comes after readiness, strategy construction, and dry-run audit. |
| 43 | CI compile/test gating | Partial | `.github/workflows/ci.yml` runs top-level `check-aarch64`, runs `just source-fence` before the four-shard `just test -- --partition count:<shard>/4` matrix, includes both lanes in the aggregate `gate`, and still runs `just fmt-check`, `just deny`, `just clippy`, and conditional `just build`; `justfile` defines those recipes and `just ci-lint-workflow` for workflow-hygiene proof | `scripts/verify_ci_workflow_hygiene.py`; `scripts/test_verify_ci_workflow_hygiene.py`; `just source-fence` runs the Bolt-v3 runtime-literal, provider-leak, core-boundary, naming, status-map-current, and pure-Rust-runtime verifiers plus targeted structural cargo test filters; `just ci-lint-workflow` proves exact CI jobs, gate/deploy needs, fmt-check/detector decoupling, managed target-dir opt-ins, split host/aarch64 checks, four fail-closed nextest shards, LiveNode nextest serialization, and prebuilt CI build-tool install policy for `cargo-deny`, `cargo-nextest`, and `cargo-zigbuild` | Source-fence and workflow-hygiene checks prove deterministic source-boundary and CI-topology/build-tool-install contracts, not live trading, readiness, production sockets, or broader roadmap completeness. |
| 44 | Release identity and deploy trust | Missing | Contract ledger marks release identity/deploy trust as accepted contract requiring evidence | No deploy-trust evidence found in this status pass | Required before canary or production. |
| 45 | Panic gate and service policy | Missing | Contract ledger marks panic gate/systemd policy as requiring issue evidence | No panic-gate evidence found in this status pass | Required before live capital. |
| 46 | Polymarket CLOB V2 readiness gate | Partial; live gate still blocked | Contract ledger marks CLOB V2 readiness as blocker; this branch pins NT to release `v1.226.0` (`38b912a8b0fe14e4046773973ff46a3b798b1e3e`), which contains upstream Polymarket CLOB V2 migration support | Historical pin-change audit/probe: `docs/bolt-v3/research/nt-pin-change/2026-04-28-clob-v2-pin-audit.md` and `docs/bolt-v3/research/nt-pin-change/2026-04-28-clob-v2-pin-probe.md`; v1.226.0 bump evidence: `docs/bolt-v3/research/nt-pin-change/2026-04-30-nt-v1-226-pin-bump.md`; compile and focused Bolt-v3 tests passed | Upstream support blocker is reduced, not closed. Bolt still needs live CLOB V2 signing/order/fill/fee validation, runtime-contract updates for pUSD/fee behavior, dependency review, and explicit production approval before this gate can close. |
| 47 | Tiny live canary trade | Missing | No live trade path accepted | No live trade verification found | Only after readiness, reconciliation, audit/local-evidence, execution-gate, deploy-trust, panic-gate, provider-signing, and explicit user approval. |
| 48 | Production live trading | Missing | No production deployment path accepted for Bolt-v3 | No production verification found | Not in scope until canary/reconciliation/audit are proven. |
| 49 | Provider-leak verifier for adapter/secrets/registration | Implemented as current source-scan gate | `scripts/verify_bolt_v3_provider_leaks.py` scans provider-neutral production Rust source under `src/**/*.rs`, excluding only concrete provider and market-family binding modules, for provider-specific or family-specific leakage with no active residual allowances | `scripts/test_verify_bolt_v3_provider_leaks.py`; `python3 scripts/verify_bolt_v3_provider_leaks.py`; `scripts/verify_bolt_v3_status_map_current.py`; `just source-fence` | The verifier is present and wired into CI source-fence. Provider-boundary refactor work remains tracked in rows 14-17. |
| 50 | Provider-neutral cost and fee fact contract | Missing | Doctrine R31 names the missing fact contract | No fact model, freshness gate, or hot-path snapshot test found | Select a typed cost/fee fact contract before strategy edge decisions depend on fees. |
| 51 | Broad discovery with explicit trading activation | Missing | Doctrine R32 names the discovery/activation split | No broad discovery/classification implementation or activation gate found | Discovery may observe/classify broadly, but execution must activate only explicit provider + family + archetype bindings. |

## What This Means

The project is not at a numbered completion point like "85 of 100." The source
evidence says Bolt-v3 has useful foundation pieces, but the accepted path to live
trading is still before strategy construction, readiness, risk/sizing, and
execution.

The most accurate status is:

- Source/test evidence exists for TOML parsing, SSM-only direction, NT adapter
  config mapping, NT client registration, controlled connect, runtime capture,
  and pure instrument filter.
- Architecture-clean provider neutrality is incomplete.
- The provider-leak verifier is implemented and wired through `just source-fence`;
  the next implementation work should continue provider-boundary refactor work
  for adapter mapping, secret projection, and client registration. It should
  not be a new trading feature.
- Cost/fee facts, broad discovery/classification, and portfolio-allocation
  evidence are architecture work items, but they should follow the provider
  boundary because they need the same provider-owned binding surface.

## Immediate Review Questions

Reviewers should not score this document on optimism. They should check:

1. Does every "implemented" row have source evidence and test/verifier evidence?
2. Does any "partial" row overclaim completion?
3. Does any "missing" row actually have accepted Bolt-v3 code/tests in the repo?
4. Are provider/product/archetype-specific current paths named as residuals rather
   than hidden?
5. Is the recommended next move narrow enough to avoid another broad rewrite?
