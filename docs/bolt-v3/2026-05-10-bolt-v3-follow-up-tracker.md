# Bolt-v3 Follow-Up Tracker

Date: 2026-05-11
Branch context: `codex/bolt-v3-order-lifecycle-proof`

This tracker separates accepted local idle-tracer work from remaining bolt-v3 production gates.
It is not a broad roadmap. Each item should become one narrow issue or PR only when selected.

## Status Legend

- `unverified`: not proven in current branch
- `partial`: some local proof exists, but stated production gate remains incomplete
- `verified-local`: proven by local tests in current branch, not production evidence
- `reserved`: accepted concept, intentionally unsupported now
- `blocked`: needs prior decision or evidence

## Follow-Ups

| ID | Item | Status | Required Proof | Non-Goals |
| --- | --- | --- | --- | --- |
| F1 | Multi-strategy idle verification | verified-local | `tests/bolt_v3_strategy_registration.rs` proves root TOML with 2 `strategy_files` builds one `LiveNode`, registers both strategy IDs from TOML, reaches `NodeState::Idle`, and source-fences registration wiring from submit/subscribe calls | live run, order lifecycle, many-client scale |
| F2 | Reference role naming | blocked | Decide whether `reference_data.primary` is correct role language; update schema/tests only after decision | fused-price policy, producer wiring |
| F3 | ETH/USD reference contract | verified-local | Root TOML defines `[reference_streams.eth_usd]` with topic, input source, source type, instrument ID, weight, stale window, and disable window; existing strategy TOML selects it with `parameters.reference_stream_id`; `tests/bolt_v3_strategy_registration.rs` and `tests/config_parsing.rs` prove lookup and validation | live orders, order admission, producer wiring |
| F4 | Fused-price policy | partial | `tests/bolt_v3_reference_policy.rs` proves v3 root reference streams project into the existing fusion algorithm: configured source IDs, source types, weights, freshness windows, disabled inputs, topic, existing strategy stream, and config-derived age/disable source-health map before fusion. Existing strategy source proves the strategy-specific fast-feed policy is already config-driven by `lead_agreement_min_corr` and `lead_jitter_max_ms`: `PricingState::observe_reference_snapshot` filters fast venues through `arbitrate_lead_reference`, falls back to fused reference when no fast venue qualifies, and marks fast-venue incoherence for no-action evidence. Remaining decision: whether a cross-source disagreement/convergence policy is needed; no such policy is accepted yet, and no hardcoded disagreement thresholds or broad fail-closed behavior belong in the fuser | client implementation |
| F5 | Reference producer wiring | partial | `tests/bolt_v3_adapter_mapping.rs` proves a v3 Chainlink client maps stream feed metadata into `ChainlinkReferenceClientConfig`; `tests/bolt_v3_reference_producer.rs` proves the existing eth stream builds an existing `ReferenceActorConfig`/`ReferenceVenueEntry` plan from v3 TOML without legacy `Config.reference`; `tests/bolt_v3_reference_actor_registration.rs` proves the v3 LiveNode build path registers one selected `ReferenceActor` and remains idle; `tests/bolt_v3_reference_delivery.rs` proves mock-only NT `LiveNode::start` drives the registered actor to publish `ReferenceSnapshot`; `tests/bolt_v3_chainlink_external_delivery_boundary.rs` proves the selected Chainlink reference client resolves configured SSM paths before adapter mapping, redacts resolved values in debug output, and refuses adapter mapping without resolved SSM secrets. Remaining proof: real provider delivery through accepted production start/run gate | changing strategy signal logic, public/no-secret Chainlink external canary claims |
| F6a | Instrument selected-market cache resolution | verified-local | `tests/bolt_v3_instrument_readiness.rs` proves v3 TOML strategy targets plan into per-client cache checks; loaded NT `BinaryOption` instruments resolve one `selected_market`; stale time windows fail closed; missing target instruments remain blocked | `request_instruments`, reference price facts, strategy activation, submit/cancel/fill |
| F6b | Instrument readiness gate integration | verified-local | `tests/bolt_v3_instrument_gate.rs` proves a built bolt-v3 `LiveNode` stays `NodeState::Idle` while a pre-start diagnostic reports `Blocked` for missing selected-market instruments and `Ready` for loaded selected-market instruments from NT cache; `tests/bolt_v3_start_readiness_gate.rs` proves the canonical start-readiness surface composes that instrument diagnostic, `require_bolt_v3_start_readiness_gate` rejects blocked reports when explicitly invoked, accepts loaded selected-market instruments, and remains source-fenced from `LiveNode::start`, `LiveNode::run`, subscriptions, and order APIs; `tests/bolt_v3_production_run_gate.rs` proves the explicit bolt-v3 production runner wrapper no longer enforces selected-market instrument cache readiness before `LiveNode::run` | `request_instruments`, submit/cancel/fill |
| F6c | Instrument load through NT startup sequence | verified-local; external-data-canary | NT v1.226.0 `LiveNode::start` performs data-client connect, private pending-data flush, execution-client connect, reconciliation, then trader start. `tests/bolt_v3_instrument_gate.rs` proves both a mock NT data client and the real pinned NT `PolymarketDataClientFactory` can emit selected-market `InstrumentAny` data events during startup and the NT cache becomes ready without bolt-v3 writing instruments directly. The real-Polymarket local proof uses local Gamma and WebSocket endpoints configured through the v3 client TOML shape, provider-installed market-slug filters, and no execution client. The ignored external public-data canary `external_polymarket_start_loads_selected_market_instruments_through_nt_data_events` passed once on 2026-05-11 against public Polymarket Gamma and WebSocket, parsed 4 instruments, and reached ready after `LiveNode::start`; it used no secrets, no execution client, and no live orders. Current decision: do not fetch provider instruments directly in bolt-v3, keep Polymarket ad-hoc missing-instrument auto-load disabled by explicit TOML, and enforce instrument readiness before order admission rather than as a first-boot pre-run cache requirement. Remaining proof: repeatable CI/release acceptance, submit/cancel/fill, and restart reconciliation | direct provider fetch into cache, reimplementing NT runner flush, Python, submit/cancel/fill |
| F7 | Decision-event persistence | verified-local | Durable event before submit intent, with config IDs, target facts, reference facts, computed decision, and no-action reasons. The nullable-field contract uses registered NT custom-data with fixed `event_facts: Params`; `tests/nt_custom_data_catalog_integration.rs` proves explicit JSON null survives local catalog round-trip. `tests/bolt_v3_decision_event_handoff.rs` proves `market_selection_result`, `entry_evaluation`, `exit_evaluation`, entry/exit order submission, and entry/exit pre-submit rejection events write through the canonical NT catalog handoff and return write errors; it proves same-timestamp events for the same type and target are preserved by batching overlapping writes into one NT catalog file; it also proves market-selection failure reasons, entry/exit pre-submit rejection reasons, and book-state no-action reasons are allowlisted to the runtime contract. `tests/bolt_v3_order_intent_gate.rs` proves entry/exit order submit closures are not invoked when order-submission event handoff fails. `tests/bolt_v3_decision_event_context.rs` proves common fields derive from loaded v3 TOML plus supplied release identity, without strategy or venue literal dispatch. `tests/bolt_v3_release_identity.rs` proves release identity loads only from configured manifest path, verifies config hash against loaded TOML, and verifies NT revision against `Cargo.toml`. `src/platform/polymarket_catalog.rs` unit coverage proves Gamma candidate translation carries selected-market slug, question ID, selected-market observed timestamp, end timestamp, price-to-beat value, price source, and price observed timestamp into `CandidateMarket`; runtime selection snapshots carry that same `CandidateMarket` shape. `tests/eth_chainlink_taker_runtime.rs` proves the existing strategy writes accepted market-selection, all allowed failed market-selection reasons, accepted entry-evaluation, and entry order-submission events through `StrategyBuildContext` with decision evidence before NT submit; also proves insufficient-edge, missing-reference, fee-rate-unavailable, stale-reference, fair-probability-unavailable, zero-configured-capacity position-limit, market-not-started mechanical-rejection, market-ended mechanical-rejection, selected-market-open-orders mechanical-rejection, and active-book-not-priced no-action entry-evaluation without unintended NT submit. The selected-open-orders proof uses existing pending-entry state plus NT cache open-order lookup. The same runtime file also proves open-entry capacity deduction from NT cache accepted buy orders (`leaves_qty * price`) before a second submit, filled-entry capacity deduction from NT cache open positions (`position.quantity * position.avg_px_open`), forced-flat, fail-closed, and EV-hysteresis accepted exit-evaluation before exit order-submission with the same decision trace, rejected exit-evaluation for `exit_bid_unavailable`, actual entry pre-submit rejection for `instrument_missing_from_cache` and `invalid_quantity` without NT submit, actual exit pre-submit rejection for `exit_price_missing`, `invalid_quantity`, and `exit_quantity_exceeds_sellable_quantity` without NT submit, exit order-submission event wiring, and failed decision-evidence write blocking entry submit. Ownership evidence: contract-allowed entry `invalid_price` is not currently reachable as an existing-strategy runtime pre-submit path because bad/missing entry price fails before side/submit selection; `exceeds_order_notional_cap` belongs to v3 config validation/F8 admission ownership, not this F7 existing-strategy runtime path. No currently identified F7 local runtime decision-evidence gap remains for the existing strategy. | sentinel null encoding, JSON blob fallback, second writer path, hardcoded event common fields, env/git/cwd release identity |
| F8 | Risk/order admission gate | verified-local | Root TOML `risk.default_max_notional_per_order` now enters the existing strategy through `StrategyBuildContext` from v3 registration, validation rejects nonpositive root caps, and `tests/eth_chainlink_taker_runtime.rs` proves accepted entry sizing plus submitted notional stay at or below that configured cap before NT submit. Root TOML `risk.trading_state` uses NT `TradingState`; v3 strategy registration passes it into `StrategyBuildContext`, and runtime tests prove `HALTED` blocks entry and exit before NT execution while `REDUCING` blocks entry and allows exit. `src/strategies/eth_chainlink_taker.rs` maps the existing per-market cooldown gate to `entry_no_action_reason = "market_cooling_down"`, recovery-mode gate to `entry_no_action_reason = "recovery_mode"`, one-position invariant gate to `entry_no_action_reason = "one_position_invariant"`, active-book-not-priced gate to `entry_no_action_reason = "active_book_not_priced"`, metadata-mismatch gate to `entry_no_action_reason = "metadata_mismatch"`, thin-book forced-flat gate to `entry_no_action_reason = "thin_book"`, fast-venue-incoherent forced-flat gate to `entry_no_action_reason = "fast_venue_incoherent"`, and freeze forced-flat gate to `entry_no_action_reason = "freeze"`, and `tests/bolt_v3_decision_event_handoff.rs` proves those reasons are accepted by the event contract. Entry pricing blocks now map to accepted no-action reasons: fee blocks to `fee_rate_unavailable`, executable-cost blocks to `active_book_not_priced`, and probability/input-math blocks to `fair_probability_unavailable`; unit coverage enumerates every `EntryPricingBlockReason`, and runtime coverage proves realized-vol-not-ready no-action without NT submit. Selected-market capacity now reads NT cache without current-strategy filtering; runtime tests prove other-strategy accepted buy orders and other-strategy open positions on the selected market consume capacity and block submit. Unit coverage proves nonpositive sized notional maps to accepted `insufficient_edge` no-action evidence; missing selected-side instrument ID maps to accepted `instrument_id_missing` pre-submit rejection evidence; missing entry price/cost maps to accepted `invalid_price` pre-submit rejection evidence. No currently identified existing-strategy F8 admission reason remains unmapped locally. | venue-specific order lifecycle, direct venue calls, Python, bypassing NT risk/execution, hardcoded strategy-local policy, production start/run evidence |
| F9 | Order lifecycle proof | partial | `tests/bolt_v3_order_lifecycle_tracer.rs` proves a mock-only v3 `LiveNode::run` path can load v3 TOML, register mock NT clients, register selected reference actors, register the existing strategy, load selected instruments into NT cache, publish configured reference/selection/book facts, persist entry order-submission events, and route submits through NT to the mock execution client. It also proves a controlled NT `OrderRejected` event sent to `ExecEngine.process` reaches the strategy and clears pending-entry state so a later configured signal can submit a new order with a new client order ID. It proves controlled NT `OrderAccepted` and `OrderFilled` events sent to `ExecEngine.process` materialize the same-session entry as a managed strategy position, then a configured freeze signal emits durable exit order-submission evidence and routes a sell submit through NT to the mock execution client. It also proves controlled NT `OrderCanceled` for that pending exit restores managed-position exposure and allows a replacement exit sell submit with new client order ID. The same test file now proves a local real-adapter path: v3 TOML maps to the pinned NT `PolymarketExecutionClient`, local TOML-derived execution endpoints replace live CLOB endpoints, mock data clients remain in place, the existing strategy submits through NT, NT signs the order, POST `/order` reaches the local CLOB fixture, and `CancelAllOrders` reaches DELETE `/orders`. `tests/bolt_v3_polymarket_external_execution_boundary.rs` proves external Polymarket execution is not public no-capital infrastructure: bolt-v3 execution mapping requires resolved SSM secrets, pinned NT user WebSocket requires credentials, user subscribe sends API key/secret/passphrase, and execution connect starts user-channel/account-state machinery. `tests/bolt_v3_production_run_gate.rs` proves the explicit production run wrapper no longer performs selected-market cache readiness before NT `LiveNode::run`; trading-readiness enforcement belongs before order admission, not as a pre-run cache gate. This is local/source proof only. Remaining proof: approved external venue canary, real fills/user-channel handling, restart reconciliation, and many-strategy scale. Local prerequisite proof also removes one endpoint hardcode: `src/bolt_v3_providers/polymarket.rs` passes configured `[clients.<id>.execution].base_url_http` into NT `PolymarketClobHttpClient`, and `tests/bolt_v3_polymarket_fee_provider.rs` proves fee warm uses the configured local HTTP base URL instead of NT's default CLOB URL | direct venue calls, Python, bypassing NT risk/execution, multi-client scale, live venue capital |
| F10 | Reconciliation/restart proof | partial; local-mock verified | Restart observes external orders/fills/positions and avoids duplicate submit. `tests/bolt_v3_reconciliation_restart.rs` proves bolt-v3 maps TOML reconciliation settings into NT `LiveExecEngineConfig`, pinned NT startup reconciliation registers reconciled external orders with execution clients, and pinned NT Polymarket can generate mass status while its external-order registration hook remains empty. It now also proves a mock-only v3 `LiveNode::start` path loads v3 TOML, registers mock NT clients plus the existing strategy, emits configured startup instruments through the mock data client, returns one configured external open order through mock execution mass-status, lets pinned NT startup reconciliation import that order into NT cache, calls the execution client's `register_external_order` hook, then blocks a fee-ready strong entry signal from creating duplicate entry-submit evidence or mock execution submission because the selected market has an open NT-cache order. The same file also proves a clean two-node local stop/restart cycle: the first mock `LiveNode` imports the external open order and stops; the restarted mock `LiveNode` re-imports that external open order from mass status and blocks the same fee-ready strong entry signal from duplicate submit. Remaining proof: real adapter external-order registration behavior, fills/positions, crash/process-death restart duplicate-submit prevention, and approved external/private venue canary. | bolt-owned portfolio, direct venue reconciliation, strategy-owned reconciliation store |
| F11 | Fixed-instrument target | reserved | Schema, validation, planner, event facts, and idle test for `market_selection_type = "fixed_instrument"` | rotating-market refactor |
| F12 | Scale/process model | partial; local-mock verified; multi-client decision blocked | `tests/bolt_v3_scale_process.rs` proves one mock-only `LiveNode::start` path loads v3 root TOML with two strategy TOMLs from `root_multi.toml`, registers both existing-strategy instances, publishes one configured runtime-selection snapshot only to the first strategy's TOML-derived selection topic, persists one market-selection decision for the addressed target, persists zero market-selection decisions for the unaddressed target, and produces no mock execution submits. Hard evidence blocks the next multi-client step without a design decision: `src/bolt_v3_validate.rs` currently enforces one `[clients.<id>]` block per NT venue because NT routing/subscriptions key on typed venues, while `src/bolt_v3_strategy_registration/eth_chainlink_taker.rs` only accepts the Polymarket venue for this strategy. Remaining proof: many more strategies, many clients/venues, process sharding, panic behavior, restart discipline under scale, and approved live/canary evidence | changing trading logic, bypassing current one-client-per-venue validation, registering this strategy on non-Polymarket venues |
| F13 | Hardcode elimination phase | partial | Separate acceptance phase before any live-capital approval: scan runtime, tests, fixtures, and docs for provider, market, strategy, quantity, timeout, feed, slug, wallet, venue, and topic literals; classify each as schema/contract constant, fixture-only value, or unacceptable runtime hardcode; add or extend verifiers so future sessions cannot reintroduce runtime hardcodes. Provider/family boundary verifier is now green: `python3 scripts/verify_bolt_v3_provider_leaks.py` passes, and `python3 scripts/test_verify_bolt_v3_provider_leaks.py` passes. This slice removed the old provider-leak allowlist, moved Polymarket+Updown glue into `src/bolt_v3_provider_family_bindings/polymarket_updown.rs`, kept Polymarket provider binding free of Updown imports, and moved Chainlink reference input metadata behind provider-owned `provider_config`. Follow-up F13b removed the adapter mapper's hardcoded `0_i64` no-identity clock sentinel; no-identity mapping now passes no market-selection clock, and Polymarket requires a clock only when target references exist. Follow-up F13c refreshed stale client/venue audit entries and classified provider-owned NT venue dispatch keys plus diagnostic labels without runtime behavior changes. Follow-up F13d removed the bolt-v3 Chainlink provider's fake legacy `ReferenceConfig.min_publish_interval_ms = 1` bridge value and tuple-index adapter unpacking by calling a narrower Chainlink client builder helper. Follow-up F13e classified release-identity and decision-event common-context literals as schema, manifest, Cargo metadata, normalization, diagnostic, and SHA-256 syntax constants; no runtime behavior changed. Follow-up F13f moved Polymarket/updown timestamp divisors into named unit constants and classified remaining non-event config, market-family, provider-family, readiness, strategy-registration, and validation literals. Follow-up F13g exactly classified the remaining decision-event literals as NT catalog type names, event dataset keys, event fact keys, reason allowlist values, catalog path segments, rewrite sequence counters, and contract checks; `python3 scripts/verify_bolt_v3_runtime_literals.py` now passes with 0 unclassified and 0 stale classified production literals. Follow-up F13h adds `python3 scripts/verify_bolt_v3_inline_toml_fixtures.py`, which rejects inline v3 TOML raw-string fixtures in `tests/bolt_v3*.rs`; the current inline Polymarket execution-without-secrets root moved to `tests/fixtures/bolt_v3/root_missing_polymarket_execution_secrets.toml`. Follow-up F13i starts fixture-selector cleanup with `python3 scripts/verify_bolt_v3_fixture_client_ids.py`, enforced on `tests/bolt_v3_client_registration.rs`, `tests/bolt_v3_adapter_mapping.rs`, `tests/bolt_v3_provider_binding.rs`, `tests/bolt_v3_market_identity.rs`, `tests/bolt_v3_reference_producer.rs`, `tests/bolt_v3_decision_event_context.rs`, `tests/bolt_v3_instrument_readiness.rs`, `tests/bolt_v3_readiness.rs`, and `tests/bolt_v3_scale_process.rs`; those tests now derive expected client IDs and SSM secret paths from loaded TOML instead of embedding fixture instance IDs. Follow-up F13j starts protocol-mock payload cleanup with `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py`, enforced on `tests/bolt_v3_polymarket_fee_provider.rs` and `tests/bolt_v3_order_lifecycle_tracer.rs`; static and templated Polymarket fee-rate, balance/allowance, empty page, unexpected-request, order-success, and cancel-success response bodies now live in `tests/fixtures/bolt_v3_protocol_payloads/`, and the verifier rejects inline protocol JSON templates. Follow-up F13k adds `python3 scripts/verify_bolt_v3_decision_event_test_literals.py`, enforced on `tests/bolt_v3_decision_event_handoff.rs`; event fact keys and persisted decision-event type values now use exported contract constants, and archetype-metrics JSON fixtures live in `tests/fixtures/bolt_v3_decision_events/`. Follow-up F13l adds `python3 scripts/verify_bolt_v3_toml_fixture_inventory.py`; every bolt-v3 TOML fixture under `tests/fixtures/bolt_v3*`, `tests/fixtures/bolt_v3_reference_policy`, and `tests/fixtures/bolt_v3_reference_producer` must be listed with purpose in `tests/fixtures/bolt_v3_fixture_inventory.toml`. Follow-up F13m extends `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` to reject inline decision-event reason values in `tests/bolt_v3_decision_event_handoff.rs`; reason values now use exported event-contract constants and the runtime-literal audit is refreshed for those constants. Follow-up F13n adds `python3 scripts/verify_bolt_v3_reference_policy_literals.py`, enforced on reference-policy, producer, adapter, actor-registration, strategy-registration, validate, and scale-process surfaces, so reference stream IDs, publish topics, source IDs, instrument IDs, parameter keys, auto-disable reason text, reference-policy scenario values, and manual disable reasons are derived from loaded config/fixtures/constants instead of embedded in Rust tests or source. Follow-up F13o extends `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` to `tests/bolt_v3_order_intent_gate.rs`, `tests/bolt_v3_decision_event_context.rs`, `tests/bolt_v3_decision_event_handoff.rs`, and `tests/eth_chainlink_taker_runtime.rs`; order-intent/handoff common fields now derive from v3 TOML plus release identity, order-submission facts load from JSON fixtures, decision-event context identity/trace/target expectations derive from v3 TOML plus the configured release manifest, and existing-strategy runtime decision-context identity derives from v3 TOML plus the strategy config under test instead of Rust literals. Follow-up F13p adds `python3 scripts/verify_bolt_v3_scale_process_literals.py`, enforced on `tests/bolt_v3_scale_process.rs`, so scale/process scenario suffixes, timing, price, order-acceptance, and liquidity values must come from the TOML fixture instead of Rust literals. Follow-up F13q adds `python3 scripts/verify_bolt_v3_fixture_strategy_targets.py`, enforced on `tests/bolt_v3_instrument_gate.rs` and `tests/bolt_v3_instrument_readiness.rs`, so fixture strategy IDs and configured target IDs must derive from loaded TOML or planned target facts instead of Rust string literals. Follow-up F13r adds `python3 scripts/verify_bolt_v3_instrument_fixture_literals.py`, enforced on `tests/bolt_v3_instrument_gate.rs` and `tests/bolt_v3_instrument_readiness.rs`, so synthetic selected-market condition IDs, question IDs, slugs, token IDs, and instrument IDs must come from `tests/fixtures/bolt_v3_existing_strategy/updown_selected_markets.toml` instead of Rust string literals. Follow-up F13s adds `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py`, enforced on `tests/eth_chainlink_taker_runtime.rs`, so the existing-strategy runtime test strategy ID and reference topic must flow through canonical fixture helpers rather than repeated Rust literals. Follow-up F13t extends the same verifier so default ETH up/down instrument IDs must flow through `eth_up_instrument_id()` and `eth_down_instrument_id()` instead of repeated Rust literals. Follow-up F13u extends `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` so existing-strategy runtime decision reason values in `tests/eth_chainlink_taker_runtime.rs` must use exported decision-event contract constants instead of repeated Rust string literals. Follow-up F13v extends `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` so existing-strategy runtime test-node client, account, venue, trader, node-name, and event-slug literals must derive from `tests/fixtures/eth_chainlink_taker_runtime/test_node.toml` instead of Rust literals. Follow-up F13w extends the same verifier so existing-strategy runtime archetype selector literals use `ETH_CHAINLINK_TAKER_KIND` instead of repeated Rust strings. Follow-up F13x extends the same verifier so existing-strategy runtime market-selection context fields derive from v3 strategy TOML instead of literal `BoltV3MarketSelectionContext` field values. Follow-up F13y extends the same verifier so existing-strategy runtime selected-market condition IDs, question IDs, market slugs, token IDs, and instrument IDs derive from `tests/fixtures/bolt_v3_existing_strategy/updown_selected_markets.toml` instead of Rust literals. Follow-up F13z extends the same verifier so existing-strategy runtime selection ruleset IDs derive from v3 strategy TOML instead of Rust literals. Follow-up F13aa extends the same verifier so existing-strategy runtime reference stream topics, source IDs, and instrument IDs derive from v3 root TOML; runtime reference snapshots now mirror selected reference stream source IDs, source types, and weights instead of ad-hoc venue labels. Current F13 local gate set is green; new hardcode classes must add a verifier before live-capital approval. | production behavior changes, hiding real hardcodes by moving them into core constants, replacing TOML/SSM with Rust constants, blanket allowlisting without classification |

F13 CI note: `just fmt-check` now depends on the F13 test-hardcode verifier group, including inline TOML, fixture client IDs, protocol payloads, decision-event literals, fixture inventory, reference stream fixture literals, scale-process scenario fixture literals, fixture strategy/target IDs, selected-market instrument fixture literals, existing-strategy runtime strategy/archetype/topic/default-instrument/test-node/market-selection-context/selected-market/ruleset/reference-stream literals, and existing-strategy runtime decision reason literals. F13n currently enforces `tests/bolt_v3_reference_policy.rs`, `tests/bolt_v3_reference_producer.rs`, `tests/bolt_v3_adapter_mapping.rs`, `tests/bolt_v3_reference_actor_registration.rs`, `tests/bolt_v3_strategy_registration.rs`, `tests/bolt_v3_scale_process.rs`, and `src/bolt_v3_validate.rs`. F13o/F13u currently enforce `tests/bolt_v3_order_intent_gate.rs`, `tests/bolt_v3_decision_event_context.rs`, `tests/bolt_v3_decision_event_handoff.rs`, and `tests/eth_chainlink_taker_runtime.rs`. F13p currently enforces `tests/bolt_v3_scale_process.rs`. F13q/F13r currently enforce `tests/bolt_v3_instrument_gate.rs` and `tests/bolt_v3_instrument_readiness.rs`. F13s/F13t/F13v/F13w/F13x/F13y/F13z/F13aa currently enforce `tests/eth_chainlink_taker_runtime.rs`.

F13ab note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now also rejects existing-strategy runtime resolution-basis fixture literals. `tests/eth_chainlink_taker_runtime.rs` derives the resolution-basis key from the selected v3 root reference stream oracle input plus its configured data-client venue instead of hardcoding a `source_symbol` string.

F13ac note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now rejects existing-strategy runtime selection freeze reason literals. The freeze reason string is owned by `src/platform/ruleset.rs` as `SELECTION_FREEZE_WINDOW_REASON`; the freeze threshold remains TOML/ruleset-config-driven.

F13ad note: `polymarket_gamma_market_anchor` is classified as a Polymarket-catalog-owned evidence source label, not operator policy. `src/platform/polymarket_catalog.rs` now owns it as `POLYMARKET_GAMMA_MARKET_ANCHOR_SOURCE`; decision-event and existing-strategy runtime tests use that constant, and both `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` and `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` reject repeated inline provider-source-label literals.

F13ae note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline market-selection event fact keys in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests use exported decision-event contract constants for market-selection type, timestamps, outcome, failure reason, target facts, selected-market facts, and price-to-beat facts.

F13af note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline entry-evaluation event fact keys in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests use exported decision-event contract constants for entry decision, no-action reason, up/down side, mechanical outcome/rejection, capacity facts, selected-open-order facts, and archetype metrics.

F13ag note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline order fact keys in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests use exported decision-event contract constants for order type, time-in-force, instrument ID, side, price, quantity, quote/post/reduce flags, and client order ID.

F13ah note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline entry/exit pre-submit rejection reason fact keys in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests use exported decision-event contract constants for those reason-key lookups.

F13ai note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline exit-evaluation fact keys in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests use exported decision-event contract constants for authoritative/sellable/open/uncovered exit quantities, exit mechanical outcome/rejection reason, exit decision, and exit decision reason.

F13aj note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline decision-event value literals in `tests/eth_chainlink_taker_runtime.rs`. Runtime tests and source validation use exported decision-event contract constants for market-selection outcomes, entry/exit decisions, mechanical outcomes, order side/type/time-in-force values, up/down side values, and exit-evaluation reason values.

F13ak note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now rejects existing-strategy runtime target fixture values in `tests/eth_chainlink_taker_runtime.rs`. Market-selection decision-event target facts now derive from the v3 strategy TOML through `BoltV3MarketSelectionContext` instead of repeated Rust literals.

F13al note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now rejects existing-strategy runtime selected-market fixture names. Runtime tests select default, rotation, and recovery market fixtures through each market's `runtime_role` in `tests/fixtures/bolt_v3_existing_strategy/updown_selected_markets.toml` instead of repeated Rust string constants.

F13am note: `python3 scripts/verify_bolt_v3_instrument_fixture_literals.py` now rejects selected-market fixture names in instrument gate/readiness tests. Those tests select current, stale, and ambiguous fixture markets through each market's `readiness_role` in `tests/fixtures/bolt_v3_existing_strategy/updown_selected_markets.toml` instead of Rust string literals.

F13an note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects order-lifecycle protocol fixture string values in `tests/bolt_v3_order_lifecycle_tracer.rs`. Local Polymarket accepted order ID, token IDs, fee-request count, and local NT execution timeouts now live in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13ao note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects local fee-provider protocol fixture string values in `tests/bolt_v3_polymarket_fee_provider.rs`. Local fee-provider bind address and fee-token suffix now live in `tests/fixtures/bolt_v3_existing_strategy/polymarket_fee_provider.toml`; order-lifecycle local bind address now lives in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13ap note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now also rejects fixture-owned local order-lifecycle scenario values in `tests/bolt_v3_order_lifecycle_tracer.rs`. Selected binary-option price increment, size increment, and book-level quantity now live in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13aq note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects selected numeric order-lifecycle scenario values in `tests/bolt_v3_order_lifecycle_tracer.rs`. Selected-market price-to-beat, liquidity, and reference-orderbook half-spread now live in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`; numeric matching is lexical-normalized to avoid catching unrelated unit multipliers or prices.

F13ar note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects order-lifecycle `Price::new(..., <literal precision>)` calls in `tests/bolt_v3_order_lifecycle_tracer.rs`. Book-delta price precision now derives from the selected binary-option `price_increment` fixture value.

F13as note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects literal-only `Duration::from_secs(...)` and `Duration::from_millis(...)` calls in `tests/bolt_v3_order_lifecycle_tracer.rs`. Local CLOB wait timeout, poll interval, post-order cancel delay, post-initial-market delay, and fee-request receive timeout now live in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13at note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects `spawn_fee_rate_server(<literal>)` in `tests/bolt_v3_order_lifecycle_tracer.rs`. Fee-provider server expected request counts and fee-request receive loops now derive from `local_polymarket.fee_requests_per_binary_market` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13au note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects literal polling loop bounds like `for _ in 0..<number>` in `tests/bolt_v3_order_lifecycle_tracer.rs`. Submit polling attempt count now derives from `test_timing.submit_poll_attempts` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13av note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects literal timeout margins inside `Duration::from_secs(...)` / `Duration::from_millis(...)` expressions in `tests/bolt_v3_order_lifecycle_tracer.rs`. Real-adapter and mock lifecycle run-timeout margins now derive from `test_timing.real_execution_run_timeout_margin_seconds` and `test_timing.mock_lifecycle_run_timeout_margin_seconds` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13aw note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects `start_ts_ms + <literal>` timestamp offsets in `tests/bolt_v3_order_lifecycle_tracer.rs`. Order-lifecycle scenario timeline offsets now derive from `[timeline_offsets_milliseconds]` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13ax note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects order-lifecycle scenario price literals in `tests/bolt_v3_order_lifecycle_tracer.rs`. Opening, entry, exit, and book-delta scenario prices now derive from `[scenario_prices]` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13ay note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects local CLOB HTTP method/path literals in `tests/bolt_v3_order_lifecycle_tracer.rs`. Local Polymarket protocol methods, paths, and fee-rate query prefixes now derive from `[local_polymarket]` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml` and `[local_fee_provider]` in `tests/fixtures/bolt_v3_existing_strategy/polymarket_fee_provider.toml`.

F13az note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects inline local CLOB positions response body literals in `tests/bolt_v3_order_lifecycle_tracer.rs`. The local Polymarket empty positions response now lives in `tests/fixtures/bolt_v3_protocol_payloads/polymarket_positions_empty.json`.

F13ba note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects raw millisecond/nanosecond conversion literals in `tests/bolt_v3_order_lifecycle_tracer.rs`. Order-lifecycle tests now use named conversion helpers backed by `Duration` instead of repeated `1_000_000` multipliers/dividers.

F13bb note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects literal `UnixNanos::from(...)` timestamps in `tests/bolt_v3_order_lifecycle_tracer.rs`. Synthetic binary-option created/updated timestamps now derive from `[selected_binary_option]` in `tests/fixtures/bolt_v3_existing_strategy/order_lifecycle_tracer.toml`.

F13bc note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects inline local HTTP response/status literals in enforced protocol tests. Local fee, order-lifecycle, and reconciliation HTTP responders now use `tests/support/mod.rs` `local_http_json_response` plus `LocalHttpStatus` instead of per-test response framing strings.

F13bd note: `python3 scripts/verify_bolt_v3_protocol_mock_payloads.py` now rejects inline local HTTP request-parser literals in enforced protocol tests. Local fee, order-lifecycle, and reconciliation HTTP responders now use `tests/support/mod.rs` request reader/body helpers instead of per-test buffer/header-delimiter/content-length parsing.

F13be note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct entry-evaluation fact fixture construction in `tests/bolt_v3_decision_event_handoff.rs`. Entry-evaluation facts now load from `tests/fixtures/bolt_v3_decision_events/entry_evaluation_facts.json` through `tests/support/mod.rs`.

F13bf note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct exit-evaluation fact fixture construction in `tests/bolt_v3_decision_event_handoff.rs`. Exit-evaluation facts now load from `tests/fixtures/bolt_v3_decision_events/exit_evaluation_facts.json` through `tests/support/mod.rs`.

F13bg note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct market-selection fact fixture construction in `tests/bolt_v3_decision_event_handoff.rs`. Market-selection success and failed facts now load from JSON fixtures through `tests/support/mod.rs`.

F13bh note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct decision-event timestamp literals in `tests/bolt_v3_decision_event_handoff.rs`. Handoff event/init timestamps now load from `tests/fixtures/bolt_v3_decision_events/event_timestamps.json`.

F13bi note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects inline decision-value literals in `tests/bolt_v3_decision_event_handoff.rs`. Supported decision values now use exported event-contract constants; unsupported negative-case values now load from `tests/fixtures/bolt_v3_decision_events/negative_cases.json`.

F13bj note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct pre-submit rejection fact construction in `tests/bolt_v3_decision_event_handoff.rs`. Entry/exit pre-submit rejection facts now load from JSON fixtures through `tests/support/mod.rs`.

F13bk note: `python3 scripts/verify_bolt_v3_decision_event_test_literals.py` now rejects direct decision-event timestamp literals in `tests/bolt_v3_order_intent_gate.rs`. Order-intent gate event/init timestamps now reuse `tests/fixtures/bolt_v3_decision_events/event_timestamps.json`.

F13bl note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now rejects direct `UnixNanos::from(<literal>)` timestamp construction in `tests/eth_chainlink_taker_runtime.rs`. Existing-strategy runtime synthetic instrument/event timestamps now load from `tests/fixtures/eth_chainlink_taker_runtime/timestamps.toml`.

F13bm note: `python3 scripts/verify_bolt_v3_existing_strategy_runtime_literals.py` now rejects literal `sleep(Duration::from_millis(...))` and `sleep(Duration::from_secs(...))` calls in `tests/eth_chainlink_taker_runtime.rs`. Existing-strategy runtime async wait/settle durations now load from `tests/fixtures/eth_chainlink_taker_runtime/timing.toml`.

F13bn note: `python3 scripts/verify_bolt_v3_reference_policy_literals.py` now enforces `tests/bolt_v3_reference_delivery.rs` and rejects literal reference-observation prices there. Mock reference-delivery observation price now loads from `tests/fixtures/bolt_v3_reference_delivery/observation.toml`.

F13bo note: `python3 scripts/verify_bolt_v3_reference_policy_literals.py` now rejects raw millisecond-to-nanosecond multiplier literals in `tests/bolt_v3_reference_delivery.rs`. Mock reference-delivery `UnixNanos` construction now uses a `Duration`-based helper instead of `1_000_000`.

## Reference-Producer Narrow Proof

The reference-producer slice proved only:

```text
root reference stream TOML -> existing ReferenceActor plan -> selected ReferenceActor registered on idle LiveNode -> mock-only NT start publishes ReferenceSnapshot
```

This did not prove production readiness, live orders, automatic instrument loading, accepted source-health behavior, real-provider runtime delivery, automatic production start/run execution, decision persistence, order lifecycle, reconciliation, or scale.

## Instrument Blocker

F6c is verified locally and has one manual external public-data canary pass. The accepted NT path that populates the cache before execution clients is inside `LiveNode::start`. That path also runs reconciliation and starts the trader. NT source evidence at pinned `nautilus_trader@38b912a`: `crates/live/src/node.rs` startup sequencing comments, `LiveNode::start`, and private `flush_pending_data`; `crates/live/src/runner.rs` routes `DataEvent::Instrument` into `DataEngine`; `crates/data/src/engine/mod.rs` processes `InstrumentAny` into cache; `crates/adapters/polymarket/src/data.rs` bootstraps Gamma instruments and emits `DataEvent::Instrument` during `PolymarketDataClient::connect`. Local proof now covers both a mock data client and the real pinned NT `PolymarketDataClientFactory` emitting selected-market instruments through that NT data-event path during startup. The real-provider local test uses local Gamma and WebSocket endpoints, provider-installed market-slug filters, and no execution client or order path. The ignored external canary `cargo test --test bolt_v3_instrument_gate external_polymarket_start_loads_selected_market_instruments_through_nt_data_events -- --ignored --test-threads=1` passed once on 2026-05-11 against public Polymarket Gamma and WebSocket, parsed 4 instruments, and reached ready after `LiveNode::start`; it used no secrets, no execution client, and no live orders. This is not CI acceptance, release evidence, submit/cancel/fill evidence, or restart/reconciliation evidence.

Do not work around this by fetching venue instruments directly in bolt-v3 and writing them into the NT cache. That creates a second production instrument-loading path.

Do not rely on Polymarket ad-hoc missing-instrument auto-load in the current scope. `[clients.<id>.data].auto_load_missing_instruments` must be explicit TOML and must remain `false`; `[clients.<id>.data].auto_load_debounce_milliseconds` is explicit TOML mapped to NT, but operationally unused while auto-load is disabled.

The current start-readiness surface is intentionally pre-start diagnostic only. It composes the accepted NT-cache instrument check and returns a report while the `LiveNode` remains idle. `require_bolt_v3_start_readiness_gate` upgrades a blocked report into an error only when explicitly invoked; `run_bolt_v3_live_node` no longer uses that selected-market instrument cache check before entering NT `LiveNode::run`. This diagnostic does not call `LiveNode::start`, connect real providers, load venue instruments, or approve production launch.

The production trading-readiness gate belongs immediately before order admission. The pre-run gate remains wiring/config safety only; selected instrument readiness, reference freshness, market tradability, and duplicate/open-order conflicts must be checked before any order intent reaches NT risk/execution.

## Reference Blocker

F3 local contract is no longer blocked: bolt-v3 root TOML defines the logical reference stream, and the existing eth tracer selects it by `parameters.reference_stream_id`. The strategy still receives the NT-owned `StrategyBuildContext.reference_publish_topic`; the topic is now resolved from root TOML, not strategy-local policy.

F4 is partial. The configured stream now projects into the existing fusion path, which computes weighted fair value, confidence, stale exclusion, disabled-source exclusion, and config-derived age/disable source-health map before fusion. Existing strategy fast-feed selection is strategy-local and config-driven by `lead_agreement_min_corr` and `lead_jitter_max_ms`; `PricingState::observe_reference_snapshot`, `LeadVenueSignal::is_eligible`, and `arbitrate_lead_reference` apply that policy before fast spot can override fused reference. Existing tests `pricing_state_applies_lead_quality_thresholds`, `task4_lead_arbitration_falls_back_to_chainlink_when_no_fast_venue_is_eligible`, and `fast_venue_incoherent_entry_gate_maps_to_no_action_reason` prove fallback/no-action behavior. No cross-source disagreement/convergence policy is accepted yet. Any future policy must reuse the disabled-source map before fusion rather than hardcoding disagreement behavior inside the fuser.

F5 is partial. v3 TOML can now build an existing `ReferenceActor` plan when each source names a configured data client. The existing eth Chainlink stream now names `chainlink_reference`, and the v3 Chainlink provider maps feed metadata into the existing Chainlink reference data client. The v3 LiveNode build path now registers one selected `ReferenceActor` while remaining idle. A mock-only `LiveNode::start` test proves the registered actor can receive Chainlink custom data and publish `ReferenceSnapshot`. `tests/bolt_v3_chainlink_external_delivery_boundary.rs` proves external Chainlink delivery is a credentialed SSM-resolved path, not a public no-secret canary: selected Chainlink SSM paths are resolved before mapping, resolved values stay redacted, and adapter mapping fails without resolved Chainlink secrets. The remaining gap is real-provider delivery after an accepted production start/run gate.

## Decision-Event Status

F7 is no longer blocked by the nullable-field encoding mismatch. Pinned NT `#[custom_data]` still rejects `Option<String>`, so bolt-v3 must not use nullable top-level custom-data fields. The runtime contract now uses registered NT custom-data types with a required fixed `event_facts: Params` field for nullable event facts and event-specific fact groups.

Source evidence: pinned NT `#[custom_data]` supports `Params`, `Params` stores `serde_json::Value`, and `tests/nt_custom_data_catalog_integration.rs` proves explicit JSON null survives write/read through `ParquetDataCatalog`.

This now proves the market-selection, evaluation, order-submission, pre-submit-rejection event subset, direct catalog handoff, a gate helper that refuses to call entry/exit submit closures when order-submission event handoff fails, common event-field construction from v3 TOML plus supplied release identity, release-identity loading from a configured deploy-written manifest, contract-complete selected-market fact carriage in `CandidateMarket`, actual existing-strategy accepted market-selection event emission, all allowed failed market-selection reason event emission, actual accepted entry-evaluation and entry order-submission wiring with one decision trace before NT submit, insufficient-edge, missing-reference, fee-rate-unavailable, stale-reference, fair-probability-unavailable, zero-configured-capacity position-limit, market-not-started mechanical-rejection, market-ended mechanical-rejection, selected-market-open-orders mechanical-rejection no-action entry-evaluation without unintended NT submit, open-entry capacity deduction from NT cache accepted buy orders, filled-entry capacity deduction from NT cache open positions, forced-flat, fail-closed, and EV-hysteresis accepted exit-evaluation before exit order-submission with the same decision trace, rejected exit-evaluation for `exit_bid_unavailable`, actual entry pre-submit rejection for `instrument_missing_from_cache` and `invalid_quantity` without NT submit, actual exit pre-submit rejection for `exit_price_missing`, `invalid_quantity`, and `exit_quantity_exceeds_sellable_quantity` without NT submit, and exit order-submission wiring. Do not force entry `invalid_price` or `exceeds_order_notional_cap` into this existing-strategy runtime seam without a contract change: `src/strategies/eth_chainlink_taker.rs` derives entry side and entry price from the same positive best-ask path before submit, and `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` owns `order_notional_target <= risk.default_max_notional_per_order` validation. The next F7 implementation slice should be opened only for newly identified decision-evidence gaps; otherwise proceed to F8 risk/order admission, not catalog-only tests.

## Risk Gate Blocker

F8 is now locally complete above the NT RiskEngine for currently identified existing-strategy admission reasons. Pinned NT RiskEngine provides pre-trade checks (`nautilus_trader@38b912a:crates/risk/src/engine/mod.rs:81`, `:721`, `:777`), and bolt-v3 maps root TOML into `LiveRiskEngineConfig` (`src/bolt_v3_live_node.rs:425`) with `nt_bypass = false` validation (`src/bolt_v3_validate.rs:271`). The existing-strategy v3 registration also passes root `risk.default_max_notional_per_order` and NT `risk.trading_state` into `StrategyBuildContext`; runtime proof shows accepted entry orders stay below that configured cap before NT submit, `HALTED` blocks entry and exit before NT execution, `REDUCING` blocks entry, and `REDUCING` allows exit. Per-market cooldown, recovery mode, one-position invariant, active-book-not-priced, metadata-mismatch, thin-book, fast-venue-incoherent, and freeze now have accepted no-action reasons in the decision-event contract. Entry pricing blocks now produce accepted no-action evidence instead of disappearing: fee blocks map to `fee_rate_unavailable`, executable-cost blocks map to `active_book_not_priced`, and probability/input-math blocks map to `fair_probability_unavailable`; runtime proof covers realized-vol-not-ready without NT submit. Selected-market capacity now counts other-strategy open buy orders and open positions from NT cache. Entry/exit pre-submit rejection reasons are contract-allowlisted before evidence persistence; unit coverage now also closes nonpositive sizing, missing selected-side instrument ID, and missing entry price/cost mappings. This is not live-order proof and does not start F9 order lifecycle.

Do not add one-off submit guards inside `eth_chainlink_taker` as the production solution. The accepted gate now enters the existing strategy through `StrategyBuildContext`, with v3 registration supplying the decision-evidence object and legacy paths leaving it absent.

## Order Lifecycle Blocker

F9 is partial, not production-complete. `tests/bolt_v3_order_lifecycle_tracer.rs` is the first v3 submit/reject/fill/exit/cancel-shaped proof through NT `LiveNode::run`: v3 TOML drives mock NT clients, selected reference actor registration, existing strategy registration, selected instruments in NT cache, configured reference/selection/book facts, durable entry/exit order-submission events, submits captured by the mock execution client, one controlled NT `OrderRejected` event sent through `ExecEngine.process` that clears pending-entry state before a second submit, controlled NT `OrderAccepted` plus `OrderFilled` events sent through `ExecEngine.process` that materialize the same-session entry before a configured freeze signal emits durable exit order-submission evidence and routes a sell submit through NT, and controlled NT `OrderCanceled` for that pending exit that restores managed-position exposure before replacement exit submit. This is still local proof only. The existing strategy can construct and submit NT orders and handle order events (`src/strategies/eth_chainlink_taker.rs:2827`, `:2978`, `:3324`, `:3410`, `:3435`, `:4082`, `:4168`, `:4193`). Pinned NT routes submitted orders through RiskEngine to ExecutionEngine (`nautilus_trader@38b912a:crates/risk/src/engine/mod.rs:496`, `:1764`; `crates/execution/src/engine/mod.rs:1625`) and processes order events through `ExecEngine.process` (`nautilus_trader@38b912a:crates/live/src/runner.rs:402`; `crates/execution/src/engine/mod.rs:181`). The pinned Polymarket adapter exposes submit/cancel methods (`crates/adapters/polymarket/src/execution/mod.rs:792`, `:1036`). F9 local prerequisite proof also shows the bolt-v3 Polymarket fee provider is config-driven for HTTP endpoint selection: `build_fee_provider` passes `[clients.<id>.execution].base_url_http` into NT `PolymarketClobHttpClient`, and `tests/bolt_v3_polymarket_fee_provider.rs` proves fee warm reaches a configured local fee-rate server. `tests/bolt_v3_polymarket_external_execution_boundary.rs` source-verifies that external Polymarket execution requires resolved SSM secrets, authenticated user WebSocket credentials, and private account-state machinery; there is no public no-capital external execution lifecycle path comparable to the F6c public data canary.

This does not accept live order lifecycle. Remaining F9 work must still prove real venue adapter behavior through an explicitly approved external/private canary, accepted production instrument loading, and no direct venue/Python bypass. F8 bolt-owned order admission is locally complete for currently identified existing-strategy admission reasons.

## Reconciliation / Restart Blocker

F10 must stay NT-owned. Bolt-v3 already maps TOML into NT reconciliation config (`src/bolt_v3_live_node.rs:392-406`), and pinned NT startup reconciliation queries execution mass status, reconciles it, and registers external orders (`nautilus_trader@38b912a:crates/live/src/node.rs:495`, `:532`, `:547`, `:569`). Pinned NT reconciliation also deduplicates already-synced or closed reconciliation orders (`crates/live/src/manager.rs:354`, `:396`, `:407`, `:425`). `tests/bolt_v3_reconciliation_restart.rs` source-verifies this map against the pinned NT checkout and now locally proves a mock v3 `LiveNode::start` reconciliation import: fixture-derived mass status, fixture-derived startup instruments, mock account context, existing strategy registered from v3 TOML, external open order imported into NT cache, and external order handed back to the execution client registration hook. The same test file now proves that a fee-ready strong entry signal after that reconciliation import does not create duplicate entry order-submission evidence or mock execution submission; it persists entry-evaluation no-action evidence with the selected-open-order reason instead. It also proves a clean two-node local stop/restart cycle re-imports the same external open order after restart and still blocks duplicate entry submit.

That is not bolt-v3 restart readiness. Current local proof covers same-run duplicate-submit blocking and a clean two-node local stop/restart cycle, but not crash/process death, real adapter registration behavior, fills, or positions. For Polymarket specifically, the adapter can generate mass status (`crates/adapters/polymarket/src/execution/mod.rs:1513`), but `register_external_order` is currently empty (`:1283`); `tests/bolt_v3_reconciliation_restart.rs` source-verifies both facts. Therefore external-order tracking after reconciliation still needs upstream source change or canary evidence before acceptance.

Accepted unblocker: after F6c, F7, F8, and F9, prove one NT-owned restart scenario end to end under bolt-v3: start, submit or import one known order state, stop/crash boundary, restart, startup reconciliation, no duplicate submit, and correct order/fill/position state in NT.
