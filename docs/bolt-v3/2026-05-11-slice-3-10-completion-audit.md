# Bolt-v3 Slice 3-10 Completion Audit

Date: 2026-05-11
Branch context: `codex/bolt-v3-order-lifecycle-proof`
Base chain head: `origin/codex/bolt-v3-entry-sizing-admission`

This audit checks the requested F3-F10 work against actual artifacts. It is not a production-readiness approval and does not open or merge PRs.

## Objective Restatement

Work F3-F10 sequentially with hard evidence, TDD discipline where implementation is attempted, NT-aligned vocabulary/ownership, no hardcodes, no dual paths, no Python production path, and one narrow branch per selected slice.

## Prompt-To-Artifact Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Use fresh branches/worktrees from origin | Current branch `codex/bolt-v3-exit-evaluation-reasons` was created in `.worktrees/bolt-v3-exit-evaluation-reasons` from pushed origin ref `origin/codex/bolt-v3-risk-trading-state-config` | met |
| Commit and push each slice | Prior slices remain pushed separately; current exit-evaluation reason slice is scoped to this branch; remote SHA verification must be checked after push | met-local |
| Do not merge without approval | No merge command used; no PR opened from these branches | met |
| F3 ETH/USD reference contract | Root TOML now defines `[reference_streams.eth_usd]`; existing strategy TOML selects it with `parameters.reference_stream_id`; validation rejects missing stream IDs and invalid stream values; strategy registration resolves selected stream to NT context `reference_publish_topic` | verified-local |
| F4 fused-price policy | `tests/bolt_v3_reference_policy.rs` proves v3 root reference streams use the existing fusion algorithm for configured source IDs, source types, weights, freshness windows, disabled inputs, and topic; no disagreement fail-closed policy exists yet | partial |
| F5 reference producer wiring | `tests/bolt_v3_adapter_mapping.rs` proves v3 Chainlink adapter mapping from stream feed metadata; `tests/bolt_v3_reference_producer.rs` proves existing eth stream builds a Chainlink `ReferenceActor` plan from TOML; `tests/bolt_v3_reference_actor_registration.rs` proves selected `ReferenceActor` registration on an idle v3 LiveNode; `tests/bolt_v3_reference_delivery.rs` proves mock-only NT start delivery to `ReferenceSnapshot` | partial |
| F6 instrument readiness | `codex/bolt-v3-instrument-gate` added verified-local readiness tests; this branch keeps `src/bolt_v3_start_readiness.rs` and `tests/bolt_v3_start_readiness_gate.rs` as a pre-start diagnostic report surface over the instrument gate; `require_bolt_v3_start_readiness_gate` rejects blocked reports when explicitly invoked while accepting loaded selected-market instruments; `tests/bolt_v3_production_run_gate.rs` proves `run_bolt_v3_live_node` no longer calls that selected-market cache gate before NT `LiveNode::run`; `tests/bolt_v3_instrument_gate.rs` now proves mock NT startup data events and the real pinned NT `PolymarketDataClientFactory` can populate selected-market instruments through NT cache without bolt-v3 direct cache writes; the ignored external public-data canary passed once against public Polymarket Gamma and WebSocket, parsed 4 instruments, and reached ready after `LiveNode::start` with no secrets, no execution client, and no live orders | met-local; F6c verified-local plus manual external-data canary |
| F7 decision-event persistence | Follow-up TDD probes prove registered custom data with `event_facts: Params` preserves explicit JSON null through NT local catalog; `tests/bolt_v3_decision_event_handoff.rs` proves market-selection, entry/exit evaluation, entry/exit order submission, and entry/exit pre-submit rejection events write through the canonical NT catalog handoff, preserves same-timestamp events for the same type and target by batching overlapping writes into one NT catalog file, and now rejects market-selection failure reasons plus entry/exit pre-submit rejection reasons outside the runtime contract; it also proves book-state no-action reasons are accepted by the runtime contract. `tests/bolt_v3_order_intent_gate.rs` proves entry/exit submit closures are blocked when order-submission event handoff fails; `tests/bolt_v3_decision_event_context.rs` proves common fields derive from loaded v3 TOML plus supplied release identity; `tests/bolt_v3_release_identity.rs` proves release identity loads from configured manifest path with config-hash and NT-pin verification; `src/platform/polymarket_catalog.rs` unit coverage proves Gamma candidate translation carries selected-market slug, question ID, selected-market observed timestamp, end timestamp, price-to-beat value, price source, and price observed timestamp into `CandidateMarket`; `tests/eth_chainlink_taker_runtime.rs` proves existing-strategy accepted market-selection, all allowed failed market-selection reasons, accepted entry-evaluation, and entry order-submission events are written without unintended NT submit, proves insufficient-edge, missing-reference, fee-rate-unavailable, stale-reference, fair-probability-unavailable, zero-configured-capacity position-limit, market-not-started mechanical-rejection, market-ended mechanical-rejection, selected-market-open-orders mechanical-rejection, and active-book-not-priced no-action entry-evaluation, open-entry capacity deduction from NT cache accepted buy orders, and filled-entry capacity deduction from NT cache open positions emit without unintended NT submit, proves forced-flat, fail-closed, and EV-hysteresis accepted exit-evaluation emits before exit order-submission with the same decision trace, proves rejected exit-evaluation for `exit_bid_unavailable`, proves actual entry pre-submit rejection for `instrument_missing_from_cache` and `invalid_quantity` emits without NT submit, proves actual exit pre-submit rejection for `exit_price_missing`, `invalid_quantity`, and `exit_quantity_exceeds_sellable_quantity` emits without NT submit, proves exit order-submission event wiring, and proves failed decision-evidence write blocks entry submit | selected-market fact carriage, accepted market-selection event emission, all allowed failed market-selection reason event emission, accepted entry-evaluation, insufficient-edge, missing-reference, fee-rate-unavailable, stale-reference, fair-probability-unavailable, zero-configured-capacity position-limit, market-not-started mechanical-rejection, market-ended mechanical-rejection, selected-market-open-orders mechanical-rejection no-action entry-evaluation, active-book-not-priced no-action entry-evaluation, open-entry capacity deduction from NT cache, filled-entry capacity deduction from NT cache, forced-flat accepted exit-evaluation, rejected exit-evaluation for `exit_bid_unavailable`, actual entry pre-submit rejection for `instrument_missing_from_cache` and `invalid_quantity`, actual exit pre-submit rejection for `exit_price_missing`, `invalid_quantity`, and `exit_quantity_exceeds_sellable_quantity`, and order-submission wiring verified; metadata-mismatch no-action mapping is unit-verified because standalone runtime mismatch is not cleanly reachable through the subscribed book-delta path; entry `invalid_price` is not a current runtime pre-submit seam, `exceeds_order_notional_cap` belongs to v3 validation/F8 admission ownership, and the currently identified existing-strategy exit-evaluation reason set is verified locally |
| F8 risk/order admission | Root `risk.default_max_notional_per_order` now flows through v3 strategy registration into the existing strategy context; validation rejects nonpositive root caps; runtime proof shows accepted entry sizing and submitted notional stay under the configured root cap before NT submit; root `risk.trading_state` uses NT `TradingState`, flows through v3 strategy registration into the existing strategy context, blocks entry/exit when `HALTED`, blocks entry when `REDUCING`, and permits exit when `REDUCING`; existing per-market cooldown, recovery mode, one-position invariant, active-book-not-priced, metadata-mismatch, thin-book forced-flat, fast-venue-incoherent forced-flat, and freeze forced-flat now map to accepted no-action evidence; every `EntryPricingBlockReason` now maps to an accepted no-action reason, and runtime proof covers realized-vol-not-ready without NT submit; selected-market capacity now reads NT cache without current-strategy filtering, and runtime proof shows other-strategy accepted buy orders plus other-strategy open positions consume capacity and block submit; entry/exit pre-submit rejection reasons are contract-allowlisted; unit proof maps nonpositive sized notional to accepted `insufficient_edge`, missing selected-side instrument ID to accepted `instrument_id_missing`, and missing entry price/cost to accepted `invalid_price`. No currently identified existing-strategy F8 admission reason remains unmapped locally | verified local proof |
| F9 order lifecycle | `tests/bolt_v3_order_lifecycle_tracer.rs` proves local mock-only v3 `LiveNode::run` submit-shaped flow from v3 TOML through mock NT clients, selected reference actors, existing strategy registration, selected instruments in NT cache, configured selection/reference/book facts, durable entry order-submission evidence, and submit capture by the mock execution client. It also proves a controlled NT `OrderRejected` event sent through `ExecEngine.process` reaches the strategy and clears pending-entry state before a second submit with a new client order ID. It proves controlled NT `OrderAccepted` and `OrderFilled` events sent through `ExecEngine.process` materialize the same-session entry as a managed strategy position; a configured freeze signal then emits durable exit order-submission evidence and routes a sell submit through NT to the mock execution client. It also proves controlled NT `OrderCanceled` for that pending exit restores managed-position exposure and allows a replacement exit sell submit with a new client order ID. The same file now proves a local real-adapter path through the pinned NT `PolymarketExecutionClient`: v3 TOML maps local execution endpoints into NT, mock data clients stay in place, the existing strategy submits through NT, NT signs the order, POST `/order` reaches the local CLOB fixture, and `CancelAllOrders` reaches DELETE `/orders`. `tests/bolt_v3_production_run_gate.rs` proves the explicit production run wrapper no longer performs selected-market cache readiness before NT `LiveNode::run`; trading-readiness enforcement belongs before order admission, not as a pre-run cache gate. `tests/bolt_v3_polymarket_fee_provider.rs` also proves configured local fee-rate delivery through TOML `base_url_http` | partial; local mock lifecycle plus local real-adapter submit/cancel HTTP proof only, not live/reconciliation |
| F10 reconciliation/restart | Tracker records NT reconciliation primitives and bolt-v3 config mapping, but no v3 restart proof and Polymarket external-order registration hook is empty | met as blocker evidence |
| No Python production path | All F3-F10 tracker entries reject Python or avoid it; no Python runtime artifact added | met |
| No direct venue bypass | F6c, F9, and F10 tracker entries reject direct provider/venue bypasses | met |
| No hardcoded runtime values added | Production path derives stream/topic/source/client/freshness values from TOML and release identity from the configured manifest path; this branch's touched capacity tests derive expected notional from named test helpers instead of duplicating inline quantities and prices. Existing scattered test runtime literals, including fixture topics, remain tracked as F13 cleanup, not solved here | met locally for this branch |
| Verification before claims | Prior slice checks remain recorded from their branches. Current branch additionally passes `cargo test --test bolt_v3_order_lifecycle_tracer -- --test-threads=1`, `cargo test --test bolt_v3_production_run_gate -- --test-threads=1`, `cargo test --test bolt_v3_polymarket_fee_provider -- --test-threads=1`, ignored external-data canary `cargo test --test bolt_v3_instrument_gate external_polymarket_start_loads_selected_market_instruments_through_nt_data_events -- --ignored --test-threads=1`, `cargo fmt --check`, `git diff --check`, and changed-file marker scan locally | met-local |

## Result

F3-F10 are not production-complete. They are classified with evidence:

- F3: verified locally for logical root reference stream contract and strategy selection.
- F4: partial local proof for v3 stream projection into existing fusion policy; disagreement fail-closed policy remains unaccepted.
- F5: partial local proof for v3 Chainlink adapter mapping, TOML-to-ReferenceActor planning, selected ReferenceActor registration on an idle LiveNode, and mock-only NT start delivery to `ReferenceSnapshot`; real-provider runtime delivery remains unverified.
- F6a-F6b: verified locally, including canonical pre-start diagnostic report composition over the instrument gate and an explicit blocked-report error surface when the diagnostic gate is invoked directly. `run_bolt_v3_live_node` no longer enforces selected-market cache readiness before NT `LiveNode::run`, because first boot lets NT data clients populate instruments during startup.
- F6c: verified-local proof now covers mock data-client `InstrumentAny` delivery and real pinned NT `PolymarketDataClientFactory` delivery through NT startup data events into the NT cache. The real-provider local proof uses local Gamma and WebSocket endpoints, provider-installed market-slug filters, and no execution client or order path. The ignored external public-data canary passed once on 2026-05-11 against public Polymarket Gamma and WebSocket, parsed 4 instruments, and reached ready after `LiveNode::start`; it used no secrets, no execution client, and no live orders. This is not CI acceptance, release evidence, submit/cancel/fill evidence, or restart/reconciliation evidence.
- F7: nullable-field encoding unblocked by NT `Params` proof; `market_selection_result`, `entry_evaluation`, `exit_evaluation`, entry/exit order submission, entry/exit pre-submit rejection, direct catalog handoff, order-submission gate helper, common event-field context, release-identity manifest loading, selected-market fact carriage, market-selection failure-reason allowlist enforcement, entry/exit pre-submit rejection reason allowlist enforcement, book-state no-action reason contract acceptance, actual existing-strategy accepted market-selection event emission, all allowed failed market-selection reason event emission, accepted entry-evaluation plus entry order-submission wiring, insufficient-edge, missing-reference, fee-rate-unavailable, stale-reference, fair-probability-unavailable, zero-configured-capacity position-limit, market-not-started mechanical-rejection, market-ended mechanical-rejection, selected-market-open-orders mechanical-rejection no-action entry-evaluation without unintended NT submit, active-book-not-priced no-action entry-evaluation without unintended NT submit, open-entry capacity deduction from NT cache accepted buy orders without unintended NT submit, filled-entry capacity deduction from NT cache open positions without unintended NT submit, forced-flat, fail-closed, and EV-hysteresis accepted exit-evaluation before exit order-submission with the same decision trace, rejected exit-evaluation for `exit_bid_unavailable`, actual entry pre-submit rejection for `instrument_missing_from_cache` and `invalid_quantity` without NT submit, actual exit pre-submit rejection for `exit_price_missing`, `invalid_quantity`, and `exit_quantity_exceeds_sellable_quantity` without NT submit, and exit order-submission wiring are verified locally. Open-entry capacity proof reports accepted buy order `leaves_qty * price`; filled-entry capacity proof reports open position `position.quantity * position.avg_px_open`; exit sellable proof reports open sell leaves reducing sellable quantity before submit. Metadata-mismatch no-action mapping is unit-verified because standalone runtime mismatch is not cleanly reachable through the subscribed book-delta path. Entry `invalid_price` is contract-allowed but not currently reachable as an existing-strategy runtime pre-submit path because `src/strategies/eth_chainlink_taker.rs` derives entry side and entry price from the same positive best-ask path before submit; `exceeds_order_notional_cap` belongs to v3 config validation/F8 admission ownership because `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` validates `order_notional_target <= risk.default_max_notional_per_order`. The currently identified existing-strategy exit-evaluation reason set is verified locally.
- F8: verified local proof for root notional-cap admission before NT submit, NT `TradingState` admission behavior, cooldown, recovery-mode, one-position-invariant, active-book-not-priced, metadata-mismatch, thin-book, fast-venue-incoherent, freeze no-action evidence, complete `EntryPricingBlockReason` to contract no-action mapping, realized-vol-not-ready runtime no-action evidence, cross-strategy selected-market capacity through NT cache, entry/exit pre-submit rejection reason contract allowlists, nonpositive sized-notional no-action mapping, missing selected-side instrument ID pre-submit rejection mapping, and missing entry price/cost pre-submit rejection mapping. This is local proof only; it is not F9 order lifecycle or live capital evidence.
- F9: partial local submit/reject/fill/exit/cancel-shaped proof now exists through v3 `LiveNode::run`, durable entry/exit order-submission evidence, mock NT execution capture, controlled NT `OrderRejected` processing through `ExecEngine.process` that lets the existing strategy submit again after clearing pending-entry state, controlled NT `OrderAccepted` plus `OrderFilled` processing through `ExecEngine.process` that lets a configured freeze signal submit an exit sell, controlled NT `OrderCanceled` processing through `ExecEngine.process` that lets the existing strategy replace the pending exit with a new sell submit, and explicit production run-wrapper proof that selected-market cache readiness is not enforced before NT `LiveNode::run`. Local real-adapter proof now exists for the pinned NT `PolymarketExecutionClient`: TOML-derived local endpoints, NT signing, POST `/order`, and DELETE `/orders` through a local CLOB fixture. This is not live order lifecycle; external venue canary, real fills/user-channel handling, trading-readiness enforcement before order admission, restart reconciliation, and many-strategy scale remain unproven. Polymarket fee-rate HTTP endpoint selection is config-driven through TOML `base_url_http`, not NT's default CLOB URL.
- F10: blocked until F6c-F9 unblock and Polymarket external-order tracking is proven.

## Residual Acceptance Gates

External reviews remain pending and are intentionally not requested here. CI on these branches has not been used as acceptance evidence. Merge approval remains required.

The next implementation decision should stay inside F9 or explicit F10 blocker evidence, not assume F10 is ready. Controlled cancel proof, mock startup instrument delivery, real-provider local startup instrument delivery, one manual external public-data canary, local real-adapter submit/cancel HTTP proof, and the pre-start readiness diagnostic are not production approval; the next credible slice is external no-capital venue lifecycle evidence through the accepted NT startup path or a source-backed reconciliation blocker proof, with no live orders unless an explicit live venue gate is approved.
