# PR #1429 Review Closure Plan

> **For Codex:** Execute this plan task-by-task with evidence-driven verification. Rust compilation for `bolt-v2` remains remote-first; targeted tests in the separate Nautilus Trader repository are allowed under that repository's policy.

**Goal:** Close the remaining end-to-end identity, provider-provenance, novelty-registry, and observability gaps without weakening fail-closed trading behavior.

**Architecture:** Reject missing `negRisk` at the production provider parser before an instrument exists; retain mandatory taker evidence identity. Make the novelty registry total for every strategy-produced entry-block reason and freeze each ID together with its owner and semantic state. Move canonical identity checks to construction/load boundaries and give distinct failures distinct diagnostics.

**Scope:** PR #1429 / issue #1354 only: stable episode identity, provider metadata required by it, finite novelty classification, and producer diagnostics. No execution/admission changes.

---

## Task 1: Fail closed at the Polymarket provider boundary

**Files (Nautilus Trader fork):**
- Modify: `crates/adapters/polymarket/src/http/parse.rs`
- Modify/add tests beside the Gamma parser tests

1. Add regressions proving `neg_risk: None` is rejected and `Some(false)` / `Some(true)` remain distinct.
2. Run the smallest targeted NT test and confirm the missing-value case fails before implementation.
3. Replace `unwrap_or(false)` with an explicit parse error for absent `negRisk`.
4. Re-run the targeted test and relevant formatter/lint if available.
5. Commit and publish the NT fork branch; record the immutable commit SHA.

**Evidence:** targeted NT parser tests; exact committed NT SHA.

## Task 2: Repin and prove the production boundary in bolt-v2

**Files:**
- Modify: all governed Nautilus revision surfaces found by the pin verifier
- Modify: provider boundary registry/evidence only if the verifier requires it
- Modify: taker selection regressions

1. Add/adjust a taker-level regression proving incomplete evidence identity fails closed with a specific enrichment diagnostic, not `target_market_not_found`.
2. Repin every governed Nautilus surface to the new immutable SHA, including lockfiles and vertical-slice manifests.
3. Run pin census, boundary-evidence verifier, source-fence static checks, and targeted non-compile tests.

**Evidence:** pin census; boundary verifier; taker behavior regression in exact-head remote Rust verification.

## Task 3: Make entry-skip classification total and registry ownership immutable

**Files:**
- Modify: `config/evidence-novelty.toml`
- Modify: `scripts/verify_bolt_v3_evidence_novelty.py`
- Modify: `scripts/test_verify_bolt_v3_evidence_novelty.py`
- Modify: generated novelty Rust
- Modify: edge-taker reason enum/mapping/state conversion and tests

1. Add failing verifier mutations proving every registered ID is frozen as `(id, producer_kind, semantic_state)` and every strategy-produced entry-block reason is classified.
2. Register malformed, balance, and unchanged-book unfillable rejects as IDs 173–175.
3. Extend the enum, string mapping, and canonical-state mapping; update the bound from 29 to 32.
4. Include the concrete reason in the truly-unclassified diagnostic and prevent per-tick ambiguity.
5. Regenerate artifacts through the governed generator and run all verifier self-tests.

**Evidence:** verifier mutation tests; generated byte equality; exact-count Rust regressions via remote verification.

## Task 4: Close checked-identity and diagnostic gaps

**Files:**
- Modify: `src/bolt_v3_target_identity.rs`
- Modify: maker archetype validation
- Modify: edge-taker config/load validation
- Modify: `src/bolt_v3_validate.rs`
- Modify: selection outcome diagnostics and tests

1. Add regressions for padded `strategy_id` and `order_id_tag`, while accepting legitimate internal whitespace where applicable.
2. Validate both fields at their earliest load/construction boundaries.
3. Make checked-ID errors field-neutral so reuse for `market_key` is accurate.
4. Remove the unreachable maker empty-key branch now guaranteed by the checked type.
5. Separate evidence-enrichment failure from genuine market-not-found reporting.

**Evidence:** focused Rust tests via exact-head remote verification; local format/static checks.

## Task 5: Align lasting documentation and PR scope

**Files:**
- Modify: canonical design/plan documents and PR body

1. Update the finite bound and registered reasons from 29 to 32.
2. Disclose the implemented lifecycle/evidence projection split and remove it from remaining scope.
3. Document provider-boundary rejection for absent `negRisk` and the immutable NT revision.
4. Keep transient SHA/CI receipts out of the PR body.

**Evidence:** targeted text searches; internal adversarial review; PR body inspection.

## Task 6: Verify, publish, and request exact-head review

1. Run `just fmt-check`, `just deny`, Python verifiers/self-tests, `just source-fence-static`, workflow lint, and `git diff --check`.
2. Conduct an internal adversarial review of the complete base-to-head diff and resolve every substantive finding.
3. Commit the coherent slice and publish with `just sandbox-safe-push`.
4. Run `just verify-remote` and report the exact head; do not wait locally for CI.
5. Update the stable PR body, request the required native reviewer, and leave merge blocked pending governed approval.

**Evidence:** clean local gates; exact pushed SHA; remote check record; required-review request.
