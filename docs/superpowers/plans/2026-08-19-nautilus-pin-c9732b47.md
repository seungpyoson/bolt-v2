# NautilusTrader Pin `c9732b47` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move every Bolt NautilusTrader dependency and governed source-capability reference from `e4167fd1ed5ce9db06b43a81417ab4096b8b84b6` to exact official `develop` commit `c9732b4704a09f2dbc8a4d442fb78da27039bf04`, preserving Bolt's fail-closed runtime boundaries.

**Architecture:** Keep NautilusTrader as the single protocol and lifecycle owner. Adapt Bolt only at existing typed boundaries: the dependency/source registry, WebSocket wrapper, NT configuration constructors, Polymarket economics and collateral evidence, and the isolated backtesting workspace. The closed #1545 branch is reference-only; no commit is cherry-picked and no fork-only source is retained.

**Tech Stack:** Rust, Cargo git dependencies, NautilusTrader Rust API, Tokio, rust_decimal, existing Bolt integration tests, GitHub advisory CI.

**Spec:** `AGENTS.md`, the user-approved exact upstream target `c9732b4704a09f2dbc8a4d442fb78da27039bf04`, and the existing source registry `ci/nautilus-source-capabilities.toml`.

## Global Constraints

- Use only `https://github.com/nautechsystems/nautilus_trader.git` at exact revision `c9732b4704a09f2dbc8a4d442fb78da27039bf04`.
- All manifests, lockfiles, source-capability evidence, tests, and compatibility documentation must agree on that one revision.
- Preserve the official upstream Polymarket allowance behavior merged by NautilusTrader PR #4760; do not recreate the closed fork patch.
- Missing, malformed, duplicate, noncanonical, unexpected, negative, or out-of-range collateral allowance evidence fails closed.
- WebSocket reconnect callbacks run once per new nonzero connection epoch and only after the reconnect control message is forwarded.
- No strategy-specific policy enters shared registration, execution, networking, or admission modules.
- Do not add a second dependency path, compatibility layer, source-scanning test, runtime default, TODO, or conditional-census work.
- Local verification is smallest-sufficient; exact-head advisory CI is the full workspace evidence.

---

### Task 1: Pin identity and lockfiles

**Files:**
- Modify: `tests/nautilus_source_capabilities.rs`
- Modify: `Cargo.toml`
- Modify: `Cargo.lock`
- Modify: `crates/backtesting-vertical-slice/Cargo.toml`
- Modify: `crates/backtesting-vertical-slice/Cargo.lock`
- Modify: `ci/nautilus-source-capabilities.toml`

**Interfaces:**
- Consumes: the existing build-generated `NAUTILUS_SOURCE_CAPABILITIES` registry.
- Produces: one exact official revision across every reachable manifest, lockfile, and governed capability record.

- [x] **Step 1: Update only the behavior-test expectation**

  Change `OFFICIAL_NAUTILUS_REVISION` in `tests/nautilus_source_capabilities.rs` to `c9732b4704a09f2dbc8a4d442fb78da27039bf04`.

- [x] **Step 2: Run the focused test and verify RED**

  Run `cargo test --locked --features test-current-evidence-inspection --test platform_config nautilus_source_capabilities::generated_registry_exposes_the_exact_official_source_facts -- --exact`.
  Expected: failure because the generated registry still reports `e4167fd1ed5ce9db06b43a81417ab4096b8b84b6`.

- [x] **Step 3: Update every direct dependency and governed source reference**

  Replace the old revision in the root and isolated manifests and in `ci/nautilus-source-capabilities.toml`. Keep the official repository URL and existing feature sets unchanged. Move the isolated workspace's exact `futures` and `futures-util` pins together from `0.3.33` to NT 0.62's required `0.3.34`; the resolver failure is the compatibility evidence for this change.

- [x] **Step 4: Regenerate both lockfiles through Cargo**

  Starting from each base lockfile, run `cargo update -p nautilus-common` in the root workspace and `cargo update -p nautilus-analysis` in `crates/backtesting-vertical-slice`. A package-scoped update moves the shared NT source graph while retaining unrelated locked dependency versions. Do not run an unscoped `cargo update` or hand-edit lockfile package graphs.

- [x] **Step 5: Verify the pin boundary**

  Run:

  ```bash
  python3 .github/scripts/verify-nautilus-pin.py --self-test
  python3 .github/scripts/verify-nautilus-pin.py
  cargo test --locked --features test-current-evidence-inspection --test platform_config nautilus_source_capabilities::generated_registry_exposes_the_exact_official_source_facts -- --exact
  ```

  Expected: 50 verifier controls pass, every NT reference resolves to the one official merged revision, and the registry test passes.

### Task 2: Preserve WebSocket and live-node behavior on the new API

**Files:**
- Modify: `src/bolt_v3_wire_boundary.rs`
- Modify: `src/bolt_v3_client_registration.rs`
- Modify: `src/bolt_v3_live_node/live_node_config.rs`
- Modify: `src/bolt_v3_live_node/tests/live_node_config.rs`
- Modify: `src/bolt_v3_providers/chainlink/strike_source.rs`

**Interfaces:**
- Consumes: NT `EpochMessageHandler`, `RECONNECTED`, `RateLimiter`, `LiveNodeConfig`, provider client configs, and `Data::IndexPrice`.
- Produces: the unchanged Bolt `connect_websocket` and `WireMessageHandler` interface with epoch-safe reconnect callback ordering.

- [x] **Step 1: Add boundary tests before changing the wrapper**

  Add unit cases in `src/bolt_v3_wire_boundary.rs` proving: epoch zero never invokes the reconnect callback; a duplicate reconnect sentinel for one epoch invokes it once; a later epoch invokes it once; ordinary messages are forwarded without the callback; and a reconnect callback without an application message handler is rejected.

- [x] **Step 2: Run the focused unit tests and verify RED**

  Run `cargo test --locked --lib bolt_v3_wire_boundary::tests -- --nocapture`.
  Expected: compile failure because the current wrapper still targets NT's removed post-reconnection argument and has no epoch adapter.

- [x] **Step 3: Adapt the wrapper minimally**

  Convert the Bolt handler to `EpochMessageHandler`, track the highest callback epoch with `AtomicU64::fetch_max`, construct the NT `RateLimiter` from existing quotas, and call `WebSocketClient::connect_with_rate_limiter_and_epoch_handler`. Preserve message-before-callback ordering and require an application message handler.

- [x] **Step 4: Satisfy additive NT configuration fields**

  Set the new client and node fields explicitly to their inert values: Polymarket `compute_effective_deltas: false` and live-node `queue_monitor: None`. Keep WebSocket liveness operator-owned by requiring positive TOML `heartbeat_timeout_secs` values for Chainlink and PolyResearch rather than inheriting NT's derived default. Extend the existing live-node residual test to assert `queue_monitor`. Rename `Data::IndexPriceUpdate` to `Data::IndexPrice` at the Chainlink boundary.

- [x] **Step 5: Verify the focused boundaries**

  Run the wire-boundary unit tests and the existing live-node/client-registration/Chainlink test targets selected by `cargo test --locked --lib` filters.

### Task 3: Preserve Polymarket fee and collateral authority

**Files:**
- Modify: `src/bolt_v3_providers/polymarket/fee_behavior_source.rs`
- Modify: `src/bolt_v3_providers/polymarket/economics.rs`
- Modify: `src/bolt_v3_providers/polymarket/provider_collateral_allowance_runtime_source.rs`
- Modify: `src/bolt_v3_providers/polymarket.rs`
- Modify: `tests/bolt_v3_polymarket_provider_collateral_allowance.rs`
- Modify: `tests/config_parsing.rs` only if the pinned canonical spender set changes a real configuration result.

**Interfaces:**
- Consumes: NT decimal `compute_commission`, `instrument_fee_exponent`, strict plural `BalanceAllowance::allowances`, and `COLLATERAL_APPROVAL_TARGETS`.
- Produces: decimal fee evidence and one conservative scalar collateral allowance bounded by provider balance and every canonical spender allowance.

- [x] **Step 1: Write/port fail-closed behavior tests**

  Cover the real boundary with literal inputs: plural allowances reduce to the minimum; uint256-max values are capped by balance; values above uint256 fail; missing, malformed, negative, duplicate/noncanonical, missing-spender, extra-spender, and wrong-spender evidence fail; configured spender identities equal the pinned canonical NT set.

- [x] **Step 2: Run the provider target and verify RED**

  Run `cargo test --locked --test bolt_v3_polymarket_provider_collateral_allowance -- --nocapture`.
  Expected: compile failure from the changed NT `BalanceAllowance` shape and/or failing assertions because the current code trusts the removed singular allowance.

- [x] **Step 3: Adapt fee calculations**

  Pass the schedule exponent to NT `compute_commission`, consume its `Decimal` directly, and keep evidence serialization decimal-exact. Update the existing economics rounding test call with exponent `1.0`; do not introduce a second fee formula.

- [x] **Step 4: Adapt collateral reduction**

  Reject the legacy singular field as authority, require exactly the configured canonical spender set, validate every wire allowance as uint256 before Decimal saturation, and reduce `balance` plus every canonical allowance to their minimum before pUSD conversion. Import the canonical approval targets from NT for configuration validation; do not encode the current target count in Rust types.

- [x] **Step 5: Verify the provider boundaries**

  Run the collateral integration target, Polymarket economics unit tests, the Decimal fee-behavior verification test (including its pinned-exponent mutation check), and config parsing tests.

### Task 4: Adapt the isolated backtesting workspace

**Files:**
- Modify: `crates/backtesting-vertical-slice/src/pmxt_one_off_backfill_projection.rs`
- Modify: `crates/backtesting-vertical-slice/src/polymarket_nt_surface_proof.rs`
- Modify: `crates/backtesting-vertical-slice/src/runner.rs`

**Interfaces:**
- Consumes: NT's current Polymarket book/quote parser signatures and `AccountAny::Wallet`.
- Produces: the same historical projection and terminal-account artifacts, now compiled against the pinned NT API.

- [x] **Step 1: Add the wallet terminal-capture behavior test**

  Construct a real `WalletAccount`, capture its terminal record, and assert the wallet account type and instrument/currency lock tuple survive.

- [x] **Step 2: Run the focused test and verify RED**

  Run the focused runner unit test in `crates/backtesting-vertical-slice`.
  Expected: compile failure until the new `AccountAny::Wallet` variant is handled.

- [x] **Step 3: Adapt parser and account surfaces**

  Supply new snapshot fields, pass `price_increment` into quote parsing, pass quote references plus both timestamps into delta parsing, collect per-delta results, update the NT surface proof function types, handle `AccountAny::Wallet`, and make the historical Gamma fixture's known `negRisk` value explicit.

- [x] **Step 4: Verify the isolated workspace**

  Run its focused projection and wallet tests, then `cargo clippy --locked --lib --bins -- -D warnings` in the isolated workspace.

### Task 5: Compatibility documentation and exact-head evidence

**Files:**
- Modify: `docs/bolt-v3/2026-04-25-bolt-v3-runtime-contracts.md`
- Modify: `docs/bolt-v3/2026-04-25-bolt-v3-schema.md`
- Modify: `docs/bolt-v3/2026-04-28-nt-first-boundary-doctrine.md`
- Modify: this plan's checkboxes as work completes.

**Interfaces:**
- Consumes: verified implementation and pinned upstream source.
- Produces: durable compatibility disclosure without transient CI/head status in the PR body.

- [x] **Step 1: Update exact revision and changed boundary facts**

  Record the official merged pin, current verification date, queue-monitor/client-config additions, decimal fee exponent behavior, strict plural/canonical collateral allowance evidence, and the explicit backtesting identity changes. Remove every stale statement that the selected revision predates upstream PR #4760.

- [x] **Step 2: Run targeted static checks**

  Run `rg` for the old and fork-only SHAs across tracked manifests, lockfiles, governed source records, and current compatibility documents; only historical research/plan records may retain them. Run `git diff --check` and formatting checks.

- [x] **Step 3: Run smallest-sufficient local verification**

  Run the root affected integration/unit targets, root workspace Clippy for lib/bins with `-D warnings`, isolated Clippy, root and isolated formatting, and both pin-verifier commands. Do not substitute local partial checks for advisory CI.

- [x] **Step 4: Perform internal adversarial review**

  Re-read the complete base-to-head diff for scope, fail-closed allowance behavior, reconnect ordering, exact official pin consistency, no alternate dependency path, and no stale #1545 claims. Resolve every local finding before publication.

- [x] **Step 5: Publish for exact-head review**

  Commit, push, open a fresh non-draft PR from current `main`, request `sp-reviewer` (node `U_kgDOEZMFhA`), and post the exact head SHA plus local evidence. Advisory CI supplies full root/BTE test and build evidence; do not merge without explicit user authorization and required approval.
