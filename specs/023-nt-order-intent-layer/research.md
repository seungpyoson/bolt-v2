# Research: NT Order Intent Layer

> **Historical research record — not current authority or work direction.**
> Current `main`, `AGENTS.md`, and tracked issues are authoritative.

## Current Anchors

- Worktree: `/Users/spson/Projects/Claude/bolt-v2/.worktrees/maker-order-replay`
- Branch: `codex/maker-order-proof-clean`
- Head at research start: `5978dc1cde84210f1a293d0b5a667aaa577945b3`
- Pinned NT checkout inspected: `/Users/spson/.cargo/git/checkouts/nautilus_trader-3c6af4345b4d438b/7c2aafb`
- no-mistakes: daemon running, but active run was for `refactor/386-bolt-v3-nt-vocab-alignment`, not this branch

## NT Core Evidence

- NT `OrderType` includes Market, Limit, StopMarket, StopLimit, MarketToLimit, MarketIfTouched, LimitIfTouched, TrailingStopMarket, and TrailingStopLimit at `crates/model/src/enums.rs:1542`.
- NT `TimeInForce` includes GTC, IOC, FOK, GTD, Day, AtTheOpen, and AtTheClose at `crates/model/src/enums.rs:1877`.
- NT `OrderAny` variants include limit, market, stop, touched, trailing, and market-to-limit orders at `crates/model/src/orders/any.rs:31`.
- NT `Order` trait exposes side, type, quantity, TIF, expire, price, trigger, post-only, reduce-only, quote-quantity, display quantity, offsets, emulation trigger, exec algorithm, and tags at `crates/model/src/orders/mod.rs:281`.
- NT `OrderInitialized` stores the order submit payload, including order side/type/TIF/post-only/reduce-only/quote-quantity/price/trigger/expire/display/emulation fields at `crates/model/src/events/order/initialized.rs:55`.
- NT `OrderInitialized::from(order)` copies the order trait fields into the initialized event at `crates/model/src/orders/mod.rs:530`.
- NT `SubmitOrder::from_order` carries `OrderInitialized`, `client_id`, optional `position_id`, and command metadata at `crates/common/src/messages/execution/submit.rs:76`.
- NT `Strategy::submit_order` accepts `OrderAny`, optional `position_id`, optional `client_id`, and optional params, then routes emulator/algo/risk at `crates/trading/src/strategy/mod.rs:109`.
- NT risk rejects expired GTD orders at `crates/risk/src/engine/mod.rs:743`.
- NT execution resolves the execution client, checks order venue versus client venue, checks OMS/position compatibility, checks instrument presence, and calls `ExecutionClient::submit_order` at `crates/execution/src/engine/mod.rs:1733`.

## NT Factory Evidence

- `OrderFactory::market` preserves side, quantity, TIF, reduce-only, quote quantity, exec algorithm params, tags, and optional client order id at `crates/common/src/factories/order.rs:119`.
- `OrderFactory::limit` preserves price, TIF, expire time, post-only, reduce-only, quote quantity, display quantity, emulation trigger, trigger instrument, exec params, tags, and optional client order id at `crates/common/src/factories/order.rs:164`.
- `OrderFactory` exposes stop-market at `crates/common/src/factories/order.rs:221`, stop-limit at `:278`, market-if-touched at `:339`, limit-if-touched at `:394`, and trailing-stop-market at `:459`.
- `OrderFactory` does not expose direct single-order `market_to_limit` or `trailing_stop_limit` methods in this pinned checkout. Direct constructors exist in NT model code, but using them would duplicate factory responsibilities for IDs, timestamps, exec spawn ids, and defaults.

## NT Model Invariant Evidence

- `check_time_in_force` rejects GTD without `expire_time` at `crates/model/src/orders/mod.rs:190`.
- `MarketOrder::new_checked` rejects GTD market orders at `crates/model/src/orders/market.rs:89`.
- `LimitOrder::new` wraps `new_checked` and panics on invalid model input at `crates/model/src/orders/limit.rs:183`.
- `MarketToLimitOrder::new_checked` also requires GTD expire time if GTD is used at `crates/model/src/orders/market_to_limit.rs:60`.
- `TrailingStopLimitOrder::new_checked` requires price, trigger, offsets, and GTD expiry when relevant at `crates/model/src/orders/trailing_stop_limit.rs:73`.

## Adapter Evidence

- Polymarket limit validation rejects reduce-only, non-limit, quote quantity, missing price, unsupported TIF, invalid side, and post-only outside GTC/GTD at `crates/adapters/polymarket/src/execution/order_builder.rs:164`.
- Polymarket submit maps `expire_time` into expiration and carries `post_only` into the request at `crates/adapters/polymarket/src/execution/submitter.rs:327`.
- Binance Spot maps limit plus post-only to `LIMIT_MAKER` and rejects GTD TIF at `crates/adapters/binance/src/spot/enums.rs:103`.
- Binance Futures derives venue `position_side` from hedge mode, order side, and reduce-only at `crates/adapters/binance/src/futures/execution.rs:400`.
- Binance Futures reads submit params such as `close_position` and `price_match` from `SubmitOrder.params` at `crates/adapters/binance/src/futures/execution.rs:427`.
- Deribit supports limit, market, stop, and touched order mapping, maps GTD to `good_til_day`, and warns custom `expire_time` is ignored at `crates/adapters/deribit/src/execution.rs:169`.
- Deribit product config can include Future, Option, and Spot at `crates/adapters/deribit/src/config.rs:47`.

## Initial Bolt Narrowing Evidence

- At research start, `OrderParams` mixed order fields and `position_side` at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:110`.
- At research start, archetype validation hardcoded entry tuples to buy/long limit FOK taker or buy/long limit GTC post-only at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:805`.
- At research start, archetype validation hardcoded exit tuples to sell/long market IOC taker or sell/long limit GTC post-only at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:844`.
- At research start, the runtime parser only accepted order type `limit|market` and TIF `gtc|fok|ioc` at `src/strategies/binary_oracle_edge_taker.rs:4703`.
- At research start, the order builder called only `core.order_factory().limit(...)` or `core.order_factory().market(...)` and passed optional NT fields as `None` at `src/strategies/binary_oracle_edge_taker.rs:4727`.
- At research start, decision evidence recorded strategy id, intent kind, instrument id, client order id, side, price, and quantity, but not the compiled order type/TIF/post-only/reduce-only/quote/expiry/trigger fields at `src/bolt_v3_decision_evidence.rs:31`.
- Existing mixed maker/taker long-side config coverage was already present in `tests/config_parsing.rs:689`. The initial short-side acceptance target was later superseded by Phase 28 after no-mistakes found the current strategy economics are long-side only.

## Multi-Agent Review Findings To Carry Forward

- Minimalism review: do not use direct NT constructors; use `OrderFactory` only or add/upstream NT factory support.
- Minimalism review: delete the current dual parse path by normalizing order config once.
- Venue/market review: `position_side` is not an NT order field and must remain strategy contract metadata.
- Venue/market review: compile output cannot be just `OrderAny` if submit params, client id, or position id affect execution; use a placement plan.
- End-to-end review: GTD is not globally enabled by setting TIF to GTD; it needs expiry config and adapter-proven semantics.
- End-to-end review: passive maker exit is not a forced-flat guarantee.

## Decisions

1. Bolt will model `StrategyPositionContract`, `NtOrderTemplate`, `OrderBuildInputs`, and optional `SubmitContext` as separate concepts.
2. `NtOrderTemplate` will not contain `position_side`, `client_id`, `position_id`, or submit params.
3. Bolt will use NT `OrderFactory` for construction and will not call direct order model constructors without a separate approved design.
4. Bolt will validate NT model crash-prevention invariants locally before factory calls.
5. Bolt will not own a runtime venue capability matrix.
6. Adapter legality claims require NT adapter source evidence, strategy-free smoke, or live-submit proof.
7. First implementation slice will be a TDD vertical slice that removes tuple whitelist narrowing without adding broad venue policy.

## Pre-Implementation Review Findings

Minimalism review:

- `NtOrderTemplate` must not become a Bolt order DSL. Enabled fields must be limited to the slice under test, with factory-reachable variants listed only as future evidence obligations.
- `trigger_price_source` was not NT vocabulary and has been removed from the model.
- Crash-prevention validation must stay limited to enabled variants.
- Submit context and evidence must not duplicate NT `Strategy::submit_order` or NT `OrderInitialized`.

Venue/market review:

- `client_id` must be optional because NT can route by explicit client id, venue routing map, or default client.
- Factory-supported non-limit/market variants need positive construction/admission tests before support can be claimed.
- Adapter proof cannot be hardcoded to Polymarket/Binance/Deribit; the adapter set must follow the claim.
- Concrete submit params belong to provider bindings or strategy config, not the generic order layer.

End-to-end review:

- Submit context must be proven at the real NT submit boundary, not only documented.
- GTD needs a positive expiry path before GTD support is claimed.
- OMS/position and reduce-only behavior need concrete tests before support is claimed.
- Admission must be computed from the compiled order view, not pre-build strings.
- Forced exit needs its own task slice or explicit residual.
- Strategy-free proof must be required for execution claims or an explicit residual.

## TDD Slice 1 Evidence

- T010 baseline green: `cargo test bolt_v3_archetype_accepts_mixed_maker_taker_order_configs -- --nocapture` passed.
- T011 red: `cargo test bolt_v3_archetype_accepts_coherent_short_side_order_contract -- --nocapture` failed because startup validation still required buy/long entry and sell/long exit.
- T012/T013 green at the time: `cargo test bolt_v3_archetype_accepts_coherent_short_side_order_contract -- --nocapture` passed after replacing long-only tuple checks with strategy position-contract validation. Phase 28 later superseded this acceptance and restored short-side rejection until strategy-owned short economics exist.
- T014 focused verification: `cargo test bolt_v3_archetype_ -- --nocapture` passed 8 config parsing tests; `cargo fmt -- --check` passed after rustfmt; `git diff --check` passed.

## Mid-Implementation Multi-Agent Review Findings

- Minimalism review recommended GTD expiry for currently enabled `Limit` orders before stop/touched/trailing expansion. Evidence cited: current path already uses NT enums and `OrderFactory::limit`, current validation still blocks GTD without expiry, and NT `OrderFactory::limit` already accepts `expire_time`.
- Venue/market review recommended GTD expiry first, then `StopMarket` as the lowest-risk future factory variant. Evidence cited: GTD expiry is an NT model invariant, while adapter GTD behavior remains venue-specific and must not become a Bolt capability matrix.
- End-to-end review recommended forced-exit semantics as the highest execution-risk slice. Evidence cited: forced-flat exit currently can reuse configured passive maker exit behavior, and exit submit currently has to prove position-aware context.
- Decision: defer Phase 7 factory variant expansion until after GTD expiry and forced-exit/position gates. T027-T029 remain support-claim gates for future non-limit/market variants.

## TDD Slice 5 Evidence

- T030 red: `cargo test bolt_v3_archetype_accepts_gtd_limit_order_with_expiry -- --nocapture` and `cargo test gtd_limit_order_objects_preserve_nt_expire_time -- --nocapture` failed because the order config had no `expire_time_unix_nanos` field.
- T030 green: `cargo test bolt_v3_archetype_accepts_gtd_limit_order_with_expiry -- --nocapture` passed after adding optional TOML-owned `expire_time_unix_nanos`.
- T030 green: `cargo test gtd_limit_order_objects_preserve_nt_expire_time -- --nocapture` passed after threading `expire_time_unix_nanos` into NT `OrderFactory::limit`.
- T030 negative guard: `cargo test bolt_v3_archetype_rejects_gtd_time_in_force_until_expiry_policy_exists -- --nocapture` still passed, proving GTD without expiry remains rejected.
- T030 focused verification: `cargo test bolt_v3_archetype_ -- --nocapture` passed 9 config parsing tests.

## TDD Slice 6 Evidence

- T032 red: `cargo test forced_flat_exit_uses_market_exit_config_when_normal_exit_is_post_only -- --nocapture` failed with missing `ExitSubmissionDecision` order-semantic fields, proving forced-flat exit could not expose whether it would submit the normal post-only limit exit or the market-exit TOML fields.
- T032 green: `cargo test forced_flat_exit_uses_market_exit_config_when_normal_exit_is_post_only -- --nocapture` passed after adding a private exit execution config selected from normal exit TOML or forced-flat market-exit TOML, then using that same config for exit order construction.
- Submit-boundary guard: `cargo test binary_oracle_edge_taker_exit_submit_threads_managed_position_id_to_nt -- --nocapture` passed after exit submission threaded the managed `PositionId` through `SubmitContext` to NT `submit_order`.
- Regression guard: `cargo test post_only_exit_submission_price_uses_passive_book_price -- --nocapture` passed after preserving normal post-only exit pricing separately from forced-flat market-exit pricing.
- Adjacent forced-flat checks: `cargo test task6_exit_submission_decision_forced_flat_submits_for_open -- --nocapture` passed.
- Submit-admission checks: `cargo test submit_admission -- --nocapture` passed.

## TDD Slice 7 Evidence

- NT source evidence: `OmsType` has `Unspecified`, `Netting`, and `Hedging` at `nautilus_trader/.../crates/model/src/enums.rs:1095`; NT execution accepts custom `position_id` for non-Netting and only validates the netting shape in `crates/execution/src/engine/mod.rs:2062`; NT determines Hedging position ids separately from Netting at `crates/execution/src/engine/mod.rs:2123`.
- T031 red: `cargo test strategy_core_accepts_nt_hedging_oms_type -- --nocapture` failed because runtime parsing accepted only `netting`.
- T031 red: `cargo test bolt_v3_strategy_oms_type_accepts_nt_variants -- --nocapture` failed because bolt-v3 validation rejected `Hedging`.
- T031 green: `cargo test strategy_core_accepts_nt_hedging_oms_type -- --nocapture` passed after runtime OMS parsing delegated to NT `OmsType`.
- T031 green: `cargo test bolt_v3_strategy_oms_type_accepts_nt_variants -- --nocapture` passed after removing the Bolt-only Netting validator restriction.
- T031 reduce-only guard: `cargo test forced_flat_exit_order_object_preserves_market_reduce_only_config -- --nocapture` passed, proving the forced-flat market-exit order object preserves `TimeInForce::Ioc`, `OrderType::Market`, and `is_reduce_only=true`.
- Residual: These are source/unit proofs. They do not prove live adapter-specific position behavior.

## Adapter-Proof Boundaries

- Adapter source evidence requirement: each venue/market support claim must name the exact NT adapter source path and the exact mapping for `OrderType`, `TimeInForce`, `post_only`, `reduce_only`, `position_id`, and submit params. Existing examples are Binance Futures at `crates/adapters/binance/src/futures/execution.rs`, Deribit at `crates/adapters/deribit/src/execution.rs`, Polymarket at `crates/adapters/polymarket/src/execution/mod.rs`, and OKX at `crates/adapters/okx/src/factories.rs`.
- Strategy-free smoke boundary: a strategy-free proof may build the live node, load TOML, resolve SSM secrets, register NT clients, register strategies, warm reference data, build NT orders, record decision evidence, and stop before any exchange submit. It must not consume submit admission or call live exchange submit.
- Live-submit boundary: live-submit proof remains blocked without explicit user approval, exact branch/head, exact config checksum, submit-admission approval evidence, and post-run decision/admission/order lifecycle artifacts.
- Residual: no strategy-free or live-submit artifact has been produced for this branch/head in this slice, so current claims remain source/unit-test claims only.

## Post-Implementation Review Findings

- Gemini custom-review job `6959020f-dcbc-4934-93ff-0dbf94d80c24` completed with `Verdict: APPROVE`. Source was sent for exactly five files: `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs`, `src/strategies/binary_oracle_edge_taker.rs`, `src/bolt_v3_validate.rs`, `tests/config_parsing.rs`, and `tests/bolt_v3_decision_evidence.rs`. Audit manifest recorded head `5978dc1cde84210f1a293d0b5a667aaa577945b3`, 5 files, 603207 bytes, and 15710 lines.
- Claude custom-review job `8ec493b3-f150-46a6-b5e6-aa54bff13dcc` ran with subscription/OAuth mode, completed with `Verdict: REQUEST_CHANGES`, and sent the same five scoped files at head `5978dc1cde84210f1a293d0b5a667aaa577945b3`.
- Claude blocking finding: a resting maker/GTD exit could partially fill, receive a `PositionChanged` residual open-position event, then have the remainder expire/cancel. The previous state machine kept `ExitPending` because `fill_received=true`, leaving an open residual position with no working exit order and blocking future exits.
- Resolution: `PendingExitState` now distinguishes terminal order events from authoritative residual open-position events. A filled exit remains pending when a stale terminal event races before `PositionClosed`, but after NT reports an open residual position and the exit order is terminal, exposure returns to `Managed` with the residual position.
- Kimi custom-review job `2c2bce72-c0c6-48f4-b84f-50de593e2b21` was skipped per the user timeout rule. Source was sent, but no review result arrived after more than 15 minutes. The plugin result still reported `status: running`; a later `ps -p 37734 -o pid,ppid,command` check returned no process row.
- Claude post-fix custom-review job `f5707fda-418c-4035-a1a4-52839289f32e` ran with subscription/OAuth mode against `src/strategies/binary_oracle_edge_taker.rs` at head `5978dc1cde84210f1a293d0b5a667aaa577945b3` and returned `Verdict: APPROVE`. It found no blocking issues and confirmed the partial-exit wedge is fixed without regressing stale full-fill terminal handling. Non-blocking comments were the load-bearing NT event-order invariant, duplicated transition logic around `on_position_closed`, and a pre-existing concern if NT emitted `PositionChanged` after `PositionClosed`.
- Gemini post-fix custom-review job `8026b59a-2412-4462-b0cb-6f5cc2cbccae` reviewed the same one-file scope at head `5978dc1cde84210f1a293d0b5a667aaa577945b3` and returned `Verdict: APPROVE` with no blocking issues. It agreed the state machine fix is conservative: if the residual position event never arrives, the strategy stays `ExitPending` rather than falsely flattening.
- Post-fix resolution: the event-order invariant is now documented in code at the point where `PositionChanged` after an exit fill is treated as authoritative residual exposure. The duplicated transition helper and post-close `PositionChanged` concern are recorded residual maintenance risks, not current blockers.

## TDD Slice 8 Evidence

- T044 RED: `cargo test partial_exit_fill_then_expire_restores_managed_residual_position -- --nocapture` failed before the fix with `assertion failed: pending_exit_ref(&strategy).is_none()`, proving the partial-exit expiry wedge.
- T045 GREEN: the same command passed after tracking exit terminal receipt and residual open-position observation separately.
- Post-review comment-only follow-up: after documenting the NT event-order invariant, `cargo fmt -- --check`, `git diff --check`, `cargo test partial_exit_fill_then_expire_restores_managed_residual_position -- --nocapture`, and `cargo test exit_pending -- --nocapture` passed.
- Regression guard: `cargo test exit_pending -- --nocapture` passed 5 tests, including stale filled-exit cancel/reject/expire behavior and normal cancel/reject/expire recovery.
- Formatting and diff checks: `cargo fmt -- --check` passed; `git diff --check` passed.
- Full verification: `cargo test` passed. The library test target ran 260 tests; `tests/config_parsing.rs` ran 62 tests; doc-tests ran 0 tests.

## No-Mistakes Evidence

- Prior TDD Slice 8 worktree branch: `codex/maker-order-proof-clean`.
- Prior TDD Slice 8 worktree head: `5978dc1cde84210f1a293d0b5a667aaa577945b3`.
- `no-mistakes status` is not proof for this branch/head. It reported active run `01KS04Q4BJ3HVN9T8580MK9N9E` on branch `refactor/386-bolt-v3-nt-vocab-alignment`, head `4d6f4ab0`, status `running`.
- `no-mistakes rerun` is also not proof. It failed with `fatal: ambiguous argument 'refs/heads/codex/maker-order-proof-clean^{commit}'`, so the gate repo could not resolve the current worktree branch ref.

## TDD Slice 9 Evidence

- T048 RED: PR #434 exact pushed head `6ef656139b4f96275ac604b4ef535f417673fd98` had `fmt-check` and `source-fence` failures. Local `just fmt-check` and `just source-fence` reproduced the runtime-literal allowlist failure in `scripts/test_verify_bolt_v3_runtime_literals.py::test_allowlist_exactness`.
- T048 RED follow-up: after the runtime-literal audit update, `just source-fence` reproduced the legacy-default fence failure on `src/strategies/binary_oracle_edge_taker.rs:114` because `SubmitContext` still derived `Default`.
- T049 GREEN: runtime-literal classifications now cover the NT order-intent schema fields and GTD positive-expiry invariants, and `SubmitContext` no longer derives unused `Default`.
- T050 verification: `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed; `just fmt-check` passed after rerunning outside the sandbox cache-lock restriction; `just source-fence` passed after rerunning outside the sandbox cache-lock restriction; `cargo test bolt_v3_archetype_accepts_mixed_maker_taker_order_configs -- --nocapture` passed; `cargo test bolt_v3_archetype_accepts_gtd_limit_order_with_expiry -- --nocapture` passed; `cargo test partial_exit_fill_then_expire_restores_managed_residual_position -- --nocapture` passed; `git diff --check` passed; full `cargo test` passed.
- T051 no-mistakes state before this follow-up commit: `no-mistakes status` reported daemon running with active unrelated run `01KS2TFBX0R3XRM64Y027T9HZR` on branch `codex/374-t013-t014-red-tests`, head `9a504fa8`, status `running`. This is not proof for `codex/maker-order-proof-clean`; post-commit gate proof must use a new run for the pushed follow-up head.

## TDD Slice 10 Evidence

- NT source evidence: pinned NT exposes `OrderFactory::stop_market` at `crates/common/src/factories/order.rs:221`; the `StopMarket` model requires a positive `trigger_price` and GTD expiry when `time_in_force=Gtd` in `crates/model/src/orders/stop_market.rs:76-105`; its `Order::price()` returns optional `protection_price` at `stop_market.rs:310`; its `Order::trigger_price()` returns `Some(trigger_price)` at `stop_market.rs:314`.
- T052 RED: `cargo test stop_market_order_objects_preserve_nt_trigger_price_and_admission -- --nocapture` failed before implementation with `no field 'trigger_price' on type 'BinaryOracleEdgeTakerOrderConfig'`, proving the runtime order config had no TOML-owned trigger-price path.
- T052/T053 GREEN: the same command passed after adding optional `trigger_price` to entry/exit order config, validating it as a positive finite price for `order_type=stop_market`, and constructing the NT order through `OrderFactory::stop_market`.
- T054 RED: `cargo test bolt_v3_archetype_accepts_stop_market_entry_with_trigger_price -- --nocapture` failed before archetype support with `unknown field 'trigger_price'` in `entry_order`.
- T054 GREEN: the same command passed after adding `trigger_price` to the public archetype order schema, runtime table projection, and builder table validation.
- T055 verification: `cargo test stop_market_order_objects_preserve_nt_trigger_price_and_admission -- --nocapture` passed; `cargo test bolt_v3_archetype_accepts_stop_market_entry_with_trigger_price -- --nocapture` passed; `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed; `cargo fmt -- --check` passed; `git diff --check` passed; full `cargo test` passed with 261 library tests, 63 `tests/config_parsing.rs` tests, and 0 doc-tests. After the evidence docs were updated, `just fmt-check` and `just source-fence` each reproduced the known sandbox cache-lock failure on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`, then passed when rerun outside the sandbox cache-lock restriction.
- Exact-head gate state before this StopMarket commit: PR #434 pushed head `473fd898cd820315d82f6bb18eb13ff4f413fe84` had green GitHub checks, but that is historical only. The StopMarket changes were still local when this evidence was recorded, so a new post-commit/push gate is required.
- no-mistakes state before this StopMarket commit: `no-mistakes status` reported active unrelated run `01KS2W6GSACEQ7TW434MS1FQTJ` on branch `codex/374-t013-t014-red-tests`, head `49f88fd1`, status `running`. This is not proof for `codex/maker-order-proof-clean`; post-commit gate proof must use a new run for the pushed StopMarket head.
- Residual unsupported NT factory variants: `StopLimit`, `MarketIfTouched`, `LimitIfTouched`, and `TrailingStopMarket` still need one TDD variant slice each before support can be claimed. `MarketToLimit` and `TrailingStopLimit` still need separate approval/upstream factory support because the pinned NT `OrderFactory` does not expose single-order factory methods for them.
- Live/canary residual: this slice is source/unit proof only. It does not prove adapter-specific legality, strategy-free startup, live exchange submission, or live-submit execution for StopMarket.

## TDD Slice 11 Evidence

- External review: Gemini job `28a8f7db-12ed-4647-91ec-d7d6ad21c903` reviewed base `473fd898cd820315d82f6bb18eb13ff4f413fe84` to head `25080b7f381f1d8f4e78fe5d6d0e130c6e019090`, sent 6 files, and returned `Verdict: REQUEST_CHANGES`. Blocking finding: StopMarket admission fell back to pre-trigger `intent.price` because NT `StopMarket::price()` is `None`, underestimating notional when trigger price is worse than the current book price.
- External review: Claude job `0b15f602-8e7e-4513-bba0-c70918ba24f8` ran with subscription/OAuth mode, reviewed the same base/head and 6 files, and returned `Verdict: REQUEST_CHANGES`. Blocking finding: archetype projection used Debug-lowercase enum serialization, emitting `stopmarket` instead of NT's serde/display-compatible `stop_market`, so validation could pass while runtime strategy parsing failed.
- T057 RED: `cargo test stop_market_entry_submission_price_uses_trigger_price_for_notional_sizing -- --nocapture` failed with left `Some(0.41)` and right `Some(0.52)`, proving entry sizing still used pre-trigger book price. `cargo test stop_market_order_objects_preserve_nt_trigger_price_and_admission -- --nocapture` failed with left `0.8000` and right `1.040`, proving admission notional ignored the trigger price.
- T058 GREEN: StopMarket entry pricing now uses the TOML-owned positive trigger price for sizing/submission price, and submit admission falls back to NT `Order::trigger_price()` when `Order::price()` is absent. `cargo test stop_market -- --nocapture` passed after the fix.
- T059 RED/GREEN: `cargo test binary_oracle_runtime_mapping_preserves_stop_market_entry_order_round_trip -- --nocapture` first failed with left `Some("stopmarket")` and right `Some("stop_market")`. The archetype enum projection now uses the NT/strum display form lowercased instead of Debug-lowercase, and the same test passed, including `BinaryOracleEdgeTakerBuilder::build` from the raw runtime table.
- T060 verification: `cargo fmt -- --check` passed; `git diff --check` passed; `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed; full `cargo test` passed with 262 library tests, 63 `tests/config_parsing.rs` tests, 9 `tests/bolt_v3_strategy_registration.rs` tests, and 0 doc-tests; `just fmt-check` passed with cache-lock escalation; `just source-fence` passed with cache-lock escalation.
- Exact-head gate state: head `25080b7f381f1d8f4e78fe5d6d0e130c6e019090` is superseded by this local post-review fix. Its previous GitHub/no-mistakes state is historical only; a new post-commit/push exact-head gate is required before claiming the review blockers are resolved on the PR head.
- Residual non-blocking review item: Claude noted that archetype validation still permits stray `trigger_price` on non-StopMarket combinations and runtime validation rejects it later. This is fail-closed and is not a StopMarket blocker, but it remains a validation-quality cleanup candidate.

## TDD Slice 12 Evidence

- Pre-slice internal NT-source review compared `StopLimit`, `MarketIfTouched`, `LimitIfTouched`, and `TrailingStopMarket` in pinned NT checkout `38b912a8b0fe14e4046773973ff46a3b798b1e3e`. Evidence: `OrderFactory::stop_limit` accepts runtime `price`, TOML-owned `trigger_price`, optional TIF/expiry, post-only, reduce-only, and quote-quantity at `crates/common/src/factories/order.rs:276-326`; `StopLimitOrder::new_checked` validates positive quantity, display quantity, and GTD expiry at `crates/model/src/orders/stop_limit.rs:73-104`; `Order::price()` and `Order::trigger_price()` return `Some(price)` and `Some(trigger_price)` at `crates/model/src/orders/stop_limit.rs:313-318`; NT test coverage rejects GTD StopLimit without expiry at `crates/model/src/orders/stop_limit.rs:689-700`.
- Pre-slice internal Bolt-path review recommended `StopLimit` over `MarketIfTouched` for this slice because Bolt already has both required inputs: strategy-computed runtime limit `price` and TOML-owned `trigger_price`. `MarketIfTouched` is still source-supported by NT but would require a separate triggered-market sizing decision because its `Order::price()` is absent.
- Decision: implement `StopLimit` next. This advances the factory-variant support gate without a venue capability matrix and without changing `StopMarket`/`MarketIfTouched` sizing semantics.
- T064 RED: `cargo test stop_limit_order_objects_preserve_nt_price_trigger_and_admission -- --nocapture` failed before runtime support with `entry_order_type supports \`limit\`, \`market\`, or \`stop_market\`, got \`StopLimit\``.
- T064 GREEN: the same test passed after adding StopLimit NT-model validation and constructing the order through `OrderFactory::stop_limit`. The regression proves the compiled NT `StopLimit` preserves `price`, `trigger_price`, TIF, GTD `expire_time`, post-only/reduce-only/quote flags, and submit admission notional from `Order::price()`.
- T065 RED: `cargo test bolt_v3_archetype_accepts_stop_limit_entry_with_trigger_price -- --nocapture` failed at archetype validation because StopLimit was not an allowed `binary_oracle_edge_taker` order combination.
- T065 GREEN: the same command passed after allowing `order_type=stop_limit` with positive `trigger_price`, valid GTD expiry when needed, and strategy-scope `is_reduce_only=false` / `is_quote_quantity=false`. `is_post_only` remains TOML-owned and is not forced off for StopLimit because NT supports post-only on `OrderFactory::stop_limit`.
- T066 focused coverage: `cargo test stop_limit -- --nocapture` passed, including `binary_oracle_runtime_mapping_preserves_stop_limit_entry_order_round_trip`, which proves archetype projection emits `order_type="stop_limit"`, preserves `trigger_price`, preserves `is_post_only=true`, validates the raw runtime table, and builds the strategy config.
- T067 local verification: `cargo fmt -- --check` passed; `git diff --check` passed; `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed after updating the runtime literal audit for the shared GTD-expiry helper and StopLimit diagnostic string; full `cargo test` passed with 263 library tests, 64 `tests/config_parsing.rs` tests, 10 `tests/bolt_v3_strategy_registration.rs` tests, and 0 doc-tests; `just fmt-check` passed with cache-lock escalation; `just source-fence` passed with cache-lock escalation.
- T068 external review: Gemini job `f914a457-2cad-414c-ba97-d7e7e624b724` reviewed base `e28605e01ce722a14e09196da4ce3a9fa820333d` to head `35ac5faf9e17aa1924fba2d1b13956a04e483684`, sent 8 files, and returned `Verdict: APPROVE` with non-blocking regression gaps for exit-side StopLimit coverage. Claude job `d805de4e-7363-452b-9fe9-3471d48775bf` used subscription/OAuth mode and sent the same source, but the review slot failed quality audit with `review_quality_failed:not_reviewed` because it could not independently verify the diff boundary; its advisory output also listed missing GTD, post-only, negative archetype, and exit StopLimit tests.
- T069/T070/T071 regression coverage: the post-review patch adds GTD-expiry and post-only assertions to the real NT StopLimit entry and exit construction test, adds a StopLimit GTD-without-expiry rejection to the pre-factory validation test, adds public exit StopLimit validation coverage, adds raw exit StopLimit runtime round-trip/build coverage, and adds negative archetype coverage for missing trigger, zero trigger, negative trigger, GTD-without-expiry, `is_reduce_only=true`, and `is_quote_quantity=true`.
- T072 local verification before commit: `cargo test stop_limit -- --nocapture` passed, including one library StopLimit factory/admission test, entry/exit runtime round-trip tests, and six config parsing/archetype StopLimit tests. `cargo test configured_order_build_rejects_nt_model_invalid_tif_before_factory -- --nocapture` passed, covering the new StopLimit GTD-missing-expiry rejection. `cargo fmt -- --check` passed; `git diff --check` passed; `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed; full `cargo test` passed with 263 library tests, 69 `tests/config_parsing.rs` tests, 11 `tests/bolt_v3_strategy_registration.rs` tests, and 0 doc-tests. The second-pass `just fmt-check` and `just source-fence` reruns reached only the rust-verification cache lock and failed under sandbox with `PermissionError` for `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; the unsandboxed rerun request was rejected by the approval reviewer due the escalation usage limit. The sandbox-safe subchecks before those lock points passed, including runtime literal/provider leak/core boundary/naming/status map/pure Rust/legacy default/strategy policy/runtime capture verifier scripts, plus direct `python3 scripts/verify_runtime_capture_yaml.py`.
- T073 exact-head gate state: the final StopLimit regression head `d0618b942d8e68fef0583ff08899bdea4e11ce18` was pushed to `origin/codex/maker-order-proof-clean`, and `git ls-remote origin refs/heads/codex/maker-order-proof-clean` returned the same SHA. `gh pr checks 434` on that head passed GitHub-hosted checks including Analyze actions/rust, CodeQL, Greptile Review, actionlint, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test. `no-mistakes runs --limit 5` showed `codex/maker-order-proof-clean-d0618b94` failed at `d0618b94`; `/Users/spson/.no-mistakes/logs/daemon.log` recorded the failure as `step review failed: agent review: codex exited` because the Codex usage limit was reached. Final-head Claude job `294cc1ab-b948-4403-8f1b-02c469f666f5` and Gemini job `cb1ce5ee-e939-4b34-ba02-5b3c591c6bd8` both failed companion finalization with `ENOSPC` while writing sidecar state under `/var/folders/.../T`; `df -h /` showed only `115Mi` available. Therefore GitHub/Greptile exact-head evidence is green, but no-mistakes and final-head Claude/Gemini review are blocked infrastructure evidence, not approvals.
- Residual unsupported NT factory variants after this slice: `MarketIfTouched`, `LimitIfTouched`, and `TrailingStopMarket` still need one TDD variant slice each before support can be claimed. `MarketToLimit` and `TrailingStopLimit` still need separate approval/upstream factory support because the pinned NT `OrderFactory` does not expose single-order factory methods for them.
- Live/canary residual: this slice is source/unit proof only. It does not prove adapter-specific legality, strategy-free startup, live exchange submission, or live-submit execution for StopLimit.
- T074 internal reviews: the NT-source review recommended `MarketIfTouched` as the next one-test slice because pinned NT exposes `OrderFactory::market_if_touched` at `crates/common/src/factories/order.rs:339`, it needs only existing Bolt fields (`order_type`, `time_in_force`, optional `expire_time_unix_nanos`, TOML-owned `trigger_price`, `is_reduce_only`, `is_quote_quantity`), and NT `MarketIfTouchedOrder` exposes no limit `price` but does expose `trigger_price` for admission. The Bolt-path review confirmed normal entry/exit maker/taker uses one runtime construction path through `build_configured_order(...)` and NT `OrderFactory`, but warned that archetype allow-lists, forced-flat market exit, submit params, and Polymarket fee-provider wiring remain strategy/provider-specific residuals. The adversarial architecture review warned the remaining tuple checks must stay `binary_oracle_edge_taker` strategy policy, not a Bolt-wide capability table, and recommended `LimitIfTouched` as the next maker-like slice. Decision: implement `MarketIfTouched` first because it is the smallest NT-factory-supported variant that uses already-modeled fields and exercises the triggered-market admission/sizing path; `LimitIfTouched` remains next because its buy/sell trigger-vs-limit-price invariant requires a separate TDD slice.
- T075 RED: `cargo test market_if_touched_order_objects_preserve_nt_trigger_price_and_admission -- --nocapture` failed with `left: Some(0.41) right: Some(0.52)`, proving MarketIfTouched entry sizing still used the executable book ask rather than the TOML trigger price for triggered-market notional.
- T076 GREEN: the same test passed after generalizing triggered-market entry sizing from `StopMarket` to `StopMarket | MarketIfTouched`, adding MarketIfTouched trigger/GTD validation before NT factory construction, and constructing through `OrderFactory::market_if_touched`. The test proves NT `OrderAny::MarketIfTouched`, `Order::price() == None`, `Order::trigger_price() == Some(0.52)`, no post-only/reduce-only/quote flags by default, and submit admission notional from the compiled trigger price.
- T077 RED/GREEN: `cargo test bolt_v3_archetype_accepts_market_if_touched_entry_with_trigger_price -- --nocapture` first failed because the `binary_oracle_edge_taker` entry allow-list rejected `order_type=market_if_touched`; after adding a narrow entry-only archetype allowance with positive trigger/valid expiry and `is_post_only=false`, the test passed. `cargo test market_if_touched -- --nocapture` passed, including the public config test, the construction/admission test, and `binary_oracle_runtime_mapping_preserves_market_if_touched_entry_order_round_trip`, proving raw archetype mapping emits `order_type="market_if_touched"` and the runtime strategy builder accepts it.
- T078 pre-push local verification: `cargo fmt -- --check` passed; `git diff --check` passed; `python3 scripts/test_verify_bolt_v3_runtime_literals.py` passed; `python3 scripts/verify_bolt_v3_runtime_literals.py` passed; `cargo test stop_market -- --nocapture` passed to check the triggered-market sizing generalization did not regress StopMarket; full `cargo test` passed; `just fmt-check` and `just source-fence` first reproduced the sandbox cache-lock failure at `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`, then both passed when rerun outside the sandbox cache-lock restriction.
- T078 external-review follow-up: Claude job `7a99c5f8-62d5-47d8-a13e-f7d70541b2b3` and Gemini job `48e93c91-ba9f-4454-bb96-ae20b0d48945` reviewed pushed head `a5168d9f83d843dc5183c50ab2b9872c15d1611d` with no blocking findings, but both flagged missing MarketIfTouched negative archetype and GTD-without-expiry builder coverage. The follow-up added MarketIfTouched config rejections for missing/non-positive trigger price, GTD without expiry, and disallowed `is_post_only`/`is_reduce_only`/`is_quote_quantity`, plus the builder GTD-without-expiry case. `cargo test market_if_touched -- --nocapture` passed with one library MarketIfTouched object/admission test, one runtime round-trip test, and five config parsing tests; `cargo test configured_order_build_rejects_nt_model_invalid_tif_before_factory -- --nocapture` passed; `cargo fmt -- --check` passed; `git diff --check` passed; full `cargo test` passed.
- T078 second external-review follow-up: Claude job `7815c817-858a-48b5-be57-acda97038490` and Gemini job `3f64978f-28dd-4576-95dc-12fcae424928` reviewed pushed head `80f201fd577014f20e4ec88a6f8b900918b9452a` with no blocking findings. Claude flagged two remaining useful test gaps: no exit-side MarketIfTouched rejection test and no positive MarketIfTouched GTD expiry pass-through test. The follow-up added both tests; `cargo test market_if_touched -- --nocapture` passed with two library MarketIfTouched tests, one runtime round-trip test, and six config parsing tests.
- T078 final exact-head verification: pushed head `e703a4f639a911c36cec42c5985b74bca9751607` matched local `git rev-parse HEAD`, `git ls-remote origin refs/heads/codex/maker-order-proof-clean`, and PR #434 `headRefOid`. `cargo test market_if_touched -- --nocapture`, full `cargo test`, `cargo fmt -- --check`, `git diff --check`, `just fmt-check`, and `just source-fence` passed locally. `gh pr checks 434` passed required and full GitHub checks, including Analyze actions/rust, CodeQL, Greptile Review, actionlint, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test. Gemini job `683384d4-6fdb-4e12-ad2c-ecfd705b6061` approved exact diff `80f201fd577014f20e4ec88a6f8b900918b9452a..e703a4f639a911c36cec42c5985b74bca9751607` with no blocking findings. Claude job `3ab53149-0642-4590-8bc3-4bafda77acde` used subscription/OAuth mode and approved the same exact diff with no blocking findings, while noting supplemental file cross-checks were blocked by its contained runtime. The no-mistakes run exceeded the 15-minute wait cap and advanced its own gate branch to review commit `166b56231b22d80490a29f62a175ea623a35e17e`; that review commit was not merged because it introduced a new optional-field validation slice.

## TDD Slice 14 Evidence

- T079 no-mistakes review triage: no-mistakes generated commit `166b56231b22d80490a29f62a175ea623a35e17e` adding archetype validation for `trigger_price` on non-triggered orders and `expire_time_unix_nanos` on non-GTD orders. Local inspection and three read-only reviewers split the finding. Trigger-price coherence is a valid field-shape invariant: pinned NT `OrderFactory::market` has no trigger argument, `OrderFactory::limit` constructs a non-triggered order, and triggered factories require `trigger_price`; Bolt runtime already rejects `Limit | Market` with `trigger_price` in `validate_configured_order_against_nt_model`. Non-GTD `expire_time_unix_nanos` is not a pinned NT Rust model invariant: `crates/model/src/orders/mod.rs` only rejects `TimeInForce::Gtd` without expiry, and `OrderFactory::limit` / triggered factories accept `expire_time: Option<UnixNanos>` independently of TIF. The stricter Python model rejects non-GTD expiry, but this branch is the pure Rust path, so the stricter rejection is not adopted as NT crash-prevention or venue-agnostic enablement work.
- T080 characterization: added `bolt_v3_archetype_rejects_non_triggered_entry_order_with_trigger_price` through the public TOML parse plus `validate_strategies` path. The first local `cargo test bolt_v3_archetype_rejects_non_triggered_entry_order_with_trigger_price -- --nocapture` pass was not sufficient proof because the later CI nextest-archive isolated run failed with `messages: []`; see T083/T084.
- T081 decision: do not add a blanket non-GTD `expire_time_unix_nanos` rejection in this slice. If Bolt later wants that as config hygiene, it must be documented as deliberate Bolt policy and reviewed against the user's NT-first/no-narrowing constraint, not described as pinned NT Rust model validation.
- T082 verification: `cargo fmt -- --check`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, and full `cargo test` passed after the characterization test and documentation updates.

## TDD Slice 15 Evidence

- T083 RED: PR #434 CI at head `7fbbaf43b32c41f282fa43e04785cb46f84edf0f` failed `nextest shard 3 of 4`. The archived isolated run for `bolt_v3_archetype_rejects_non_triggered_entry_order_with_trigger_price` returned `messages: []`, proving the earlier local all-tests pass was insufficient evidence.
- T084 GREEN: added archetype-level trigger field-shape validation for `trigger_price` on non-triggered `Limit`/`Market` order configs and added exit-side regression coverage. The fix keeps the no-mistakes expiry recommendation out of scope because pinned NT Rust does not reject non-GTD `expire_time_unix_nanos`.
- T085 verification: `cargo fmt -- --check`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, `cargo nextest run --test config_parsing non_triggered`, full `cargo test`, `just test-archive /tmp/bolt-v2-nextest-trigger.tar.zst --test config_parsing`, and `just test-archive-run /tmp/bolt-v2-nextest-trigger.tar.zst /tmp/bolt-v2-nextest-trigger-extract non_triggered` passed locally. `no-mistakes status` remained stale on older branch `codex/maker-order-proof-clean-e703a4f6` and active run `01KS3XN2M7D7JJZ8M3CGJ5ADCC`, so it is not current-head proof.
- T086 RED: `just source-fence` failed on the exact-head follow-up because the first implementation added a new inline `"exit_order"` production literal in `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs`.
- T087 GREEN: the source-fence follow-up folds the non-triggered `trigger_price` rejection into the existing entry/exit order-combination predicates, so `Limit`/`Market` entries and exits with `trigger_price` are rejected without adding a new order-field literal or widening runtime literal policy.
- T088 verification: `cargo test --test config_parsing non_triggered -- --nocapture` passed; `cargo nextest run --test config_parsing non_triggered` passed after rerunning outside the sandbox cache-lock restriction; `cargo test --test config_parsing -- --nocapture` passed; `just source-fence` passed after rerunning outside the sandbox cache-lock restriction; `just fmt-check` passed after rerunning outside the sandbox cache-lock restriction; full `cargo test` passed; `just test-archive /tmp/bolt-v2-nextest-trigger.tar.zst --test config_parsing` and `just test-archive-run /tmp/bolt-v2-nextest-trigger.tar.zst /tmp/bolt-v2-nextest-trigger-extract non_triggered` passed after rerunning outside the sandbox cache-lock restriction; `git diff --check` passed.

## TDD Slice 17 Evidence

- T089 NT-source review: pinned NT is `7c2aafb30fb143069c915a3f2057bb12174405f6`. `OrderFactory::limit_if_touched` takes runtime `price`, TOML-owned `trigger_price`, optional trigger type, TIF, expiry, post-only, reduce-only, quote-quantity, display quantity, emulation/trigger-instrument fields, execution algorithm fields, tags, and client order id at `crates/common/src/factories/order.rs:394-450`. `LimitIfTouchedOrder::new_checked` validates positive quantity, display quantity, GTD expiry, and the side-aware trigger/limit invariant at `crates/model/src/orders/limit_if_touched.rs:72-120`: BUY requires `trigger_price <= price`, and SELL requires `trigger_price >= price`. `Order::price()` and `Order::trigger_price()` both return `Some(...)` at `crates/model/src/orders/limit_if_touched.rs:336-341`. NT tests reject GTD without expiry and invalid BUY/SELL trigger/price relationships at `crates/model/src/orders/limit_if_touched.rs:704-737`; the factory test asserts LIT price/trigger preservation at `crates/common/src/factories/order.rs:1451-1477`.
- T089 Bolt-path review: entry and exit already route through shared `build_configured_order(...)`; `LimitIfTouched` must be added there through NT `OrderFactory::limit_if_touched`, not by adding config fields, direct NT model constructors, or a second submit path. The review also identified the required Bolt-side pre-factory checks: GTD expiry, required positive `trigger_price`, and the pinned NT side invariant that BUY requires `trigger_price <= price` while SELL requires `trigger_price >= price`. `LimitIfTouched` must not be folded into the triggered-market sizing branch because NT exposes a limit `price`; admission should continue to derive from the constructed order's `price()`.
- T089 adversarial architecture review: proceed with `LimitIfTouched` only after adding a concrete task slice and correcting the stale support ledger. Keep `LimitIfTouched` as a `binary_oracle_edge_taker` strategy allowance plus NT model validation, not a Bolt-wide venue capability table. Entry and exit already route through shared `build_configured_order(...)`; the slice must add one factory branch there, not a second submit/build path. Claims remain source/unit construction claims only until adapter/strategy-free/live proof exists.
- T090 ledger correction: `data-model.md` now lists `MarketIfTouched` and `LimitIfTouched` under source/unit construction coverage and leaves only `TrailingStopMarket` as a remaining factory-reachable order type requiring a positive construction/admission slice.
- T091/T092 RED: `cargo test limit_if_touched -- --nocapture` failed after adding the runtime tests because the shared builder still rejected `OrderType::LimitIfTouched` as unsupported before entry/exit construction or BUY/SELL side-invariant validation.
- T093 GREEN: the implementation keeps the existing `build_configured_order(...)` path and calls NT `OrderFactory::limit_if_touched` with runtime limit `price`, TOML-owned `trigger_price`, TIF/expiry, post-only, reduce-only, quote-quantity, and client order id. `validate_configured_order_against_nt_model(...)` now rejects missing trigger price, missing GTD expiry, BUY `trigger_price > price`, and SELL `trigger_price < price` before the NT factory call. `cargo test limit_if_touched -- --nocapture` then passed the entry/exit object/admission test and the BUY/SELL invariant-rejection test.
- T094 RED/GREEN: public config tests for entry and exit `order_type=limit_if_touched` first failed in `cargo test --test config_parsing limit_if_touched -- --nocapture` on the archetype combination allow-list. Adding `LimitIfTouched` to the existing entry/exit strategy-policy predicates made the same command pass with acceptance plus missing trigger, non-positive trigger, GTD-without-expiry, and disallowed reduce/quote regression coverage on both entry and exit. `cargo test --test bolt_v3_strategy_registration limit_if_touched -- --nocapture` passed the raw runtime entry/exit round-trip tests without any new config fields.
- T095 verification: `cargo fmt -- --check` passed; `git diff --check` passed; `cargo test limit_if_touched -- --nocapture` passed 11 matching tests across library, runtime registration, and config parsing; `just source-fence` passed after rerunning outside the sandbox cache-lock restriction; full `cargo test` passed; `just fmt-check` passed after rerunning outside the sandbox cache-lock restriction.

## TDD Slice 18 And 19 Evidence

- T096 multi-agent finding: the current ledger overstates `MarketIfTouched` source/unit coverage as entry+exit. The runtime builder can construct `MarketIfTouched` through `OrderFactory::market_if_touched`, but public archetype validation only allows it for entry at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:839-900`; exit validation omits it at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:908-955`, and `tests/config_parsing.rs:1337-1379` explicitly asserted exit rejection. The next slice must either qualify `MarketIfTouched` as entry-only or enable coherent exit configs through the same path.
- T100 NT-source review: pinned NT exposes a single-order `OrderFactory::trailing_stop_market` at `crates/common/src/factories/order.rs:459-532`. It requires `trailing_offset`, accepts optional `trailing_offset_type`, `activation_price`, `trigger_price`, `trigger_type`, TIF, expiry, reduce-only, quote-quantity, display quantity, emulation/trigger-instrument fields, execution algorithm fields, tags, and client order id; it panics if neither `trigger_price` nor `activation_price` is provided at `order.rs:488-492`. The factory defaults `trigger_type` to `TriggerType::Default`, `trailing_offset_type` to `TrailingOffsetType::Price`, TIF to `Gtc`, and booleans to false at `order.rs:501-508`; Bolt must pass explicit TOML-owned values for enabled fields instead of relying on hidden defaults.
- T100 model review: `TrailingStopMarketOrder::new_checked` requires `trigger_price`, `trigger_type`, `trailing_offset`, `trailing_offset_type`, TIF, expiry, reduce-only, and quote-quantity at `crates/model/src/orders/trailing_stop_market.rs:79-107`, validates positive quantity, display quantity, and GTD expiry at `trailing_stop_market.rs:108-110`, and always initializes `post_only=false` at `trailing_stop_market.rs:112-123`. `Order::trigger_price`, `Order::activation_price`, `Order::trailing_offset`, and `Order::trailing_offset_type` expose the constructed values at `trailing_stop_market.rs:335-380`.
- T100 Bolt-path review: current public and runtime order config only carries side, position side, order type, TIF, optional expiry, optional trigger price, post-only, reduce-only, and quote-quantity at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:108-119` and `src/strategies/binary_oracle_edge_taker.rs:101-112`. The runtime order-field whitelist only permits those fields plus expiry/trigger at `src/strategies/binary_oracle_edge_taker.rs:227-242` and `src/strategies/binary_oracle_edge_taker.rs:4624-4658`. Therefore `TrailingStopMarket` cannot be enabled without adding explicit TOML-owned trailing fields and carrying them through archetype serialization, runtime config, exit decision config, and `build_configured_order(...)`.
- T100 factory-gap review: pinned NT model contains `MarketToLimit` and `TrailingStopLimit`, but `OrderFactory` exposes no single-order `market_to_limit` or `trailing_stop_limit` method. Enabling either through direct model constructors would violate this spec's `OrderFactory` boundary; they remain separate approval/upstream-factory-support scope.
- T097 RED: `cargo test --test config_parsing market_if_touched_exit -- --nocapture` failed with the existing public archetype rejection for `parameters.exit_order combination`; `cargo test --test bolt_v3_strategy_registration market_if_touched_exit -- --nocapture` passed before the production change, proving raw runtime construction already accepted the NT factory path.
- T098 GREEN: adding `MarketIfTouched` to the existing exit order combination predicate allowed coherent exit configs through the same order-template path without changing the runtime `OrderFactory::market_if_touched` branch.
- T099 local verification evidence: `cargo test market_if_touched -- --nocapture` passed 10 matching tests across library construction, raw runtime round trip, and public config parsing. `cargo fmt -- --check` passed; `git diff --check` passed; `just source-fence` passed after updating the runtime-literal audit and rerunning outside the sandbox cache-lock restriction; `just fmt-check` passed after rerunning outside the sandbox cache-lock restriction; full `cargo test` passed. `no-mistakes status` reported active run `01KS3XN2M7D7JJZ8M3CGJ5ADCC` for stale branch `codex/maker-order-proof-clean-e703a4f6`, head `71bcbb6a`, so it is not proof for this local head. Final branch-state proof must be refreshed after the checked diff is committed and pushed.
- T101/T102 RED: `cargo test trailing_stop_market -- --nocapture` first failed to compile after adding TrailingStopMarket tests because public/runtime order config lacked `activation_price`, `trigger_type`, `trailing_offset`, and `trailing_offset_type`.
- T103/T104 GREEN: Bolt now carries explicit TOML-owned trailing fields through public archetype serialization, runtime config validation, entry/exit decision config, and the shared `build_configured_order(...)` path. The implementation calls NT `OrderFactory::trailing_stop_market` and rejects missing trigger or activation input, missing trigger type, missing/non-positive trailing offset, missing trailing offset type, GTD without expiry, and `is_post_only=true` before the factory call.
- T105 local verification evidence: `cargo test trailing_stop_market -- --nocapture` passed seven matching tests across library construction/admission, raw runtime round trip, and public config parsing. `cargo fmt -- --check`, `git diff --check`, `just source-fence`, `just fmt-check`, and full `cargo test` passed locally. `git rev-parse HEAD`, `git ls-remote origin refs/heads/codex/maker-order-proof-clean`, and PR #434 `headRefOid` all reported `324d4f1132c1b7ee289cde9833725969b3397359` before this uncommitted local diff, so GitHub exact-head checks are historical only until this verified diff is committed and pushed. Final branch-state proof must be refreshed after the checked diff is committed and pushed.
- Phase 7 reconciliation: later one-variant slices satisfied the generic T027-T029 gate for the pinned single-order `OrderFactory` surface. Source/unit coverage now includes `StopMarket`, `StopLimit`, `MarketIfTouched`, `LimitIfTouched`, and `TrailingStopMarket` through NT `OrderFactory`; `MarketToLimit` and `TrailingStopLimit` remain separate approval/upstream-factory-support scope because pinned NT exposes no single-order factory methods for them.

## TDD Slice 20 Evidence

- T106 post-review finding: a Speckit/TDD reviewer found that adding `trigger_type` for `TrailingStopMarket` also enabled explicit trigger-type pass-through for existing triggered variants. This is intentional NT-surface preservation, but it needed explicit tests and task evidence because previous T100-T104 text only framed the field around `TrailingStopMarket`.
- T107 RED: after adding trigger-type assertions for `StopMarket`, `MarketIfTouched`, `StopLimit`, and `LimitIfTouched`, temporarily restoring the old behavior of passing `None` to those factory calls made `cargo test order_objects_preserve_nt -- --nocapture` fail on all four existing triggered variants. NT constructed `TriggerType::Default` instead of the configured `LastPrice` or `MarkPrice`, proving the regression tests cover the behavior.
- T108 GREEN: restoring explicit `trigger_type` pass-through to `OrderFactory::stop_market`, `OrderFactory::market_if_touched`, `OrderFactory::stop_limit`, and `OrderFactory::limit_if_touched` made `cargo test order_objects_preserve_nt -- --nocapture` pass. Public/raw config coverage also carries `trigger_type` through the existing `MarketIfTouched` entry/exit runtime round-trip tests.
- T109 local verification evidence: after the Phase 24 changes, `cargo fmt -- --check`, `git diff --check`, `cargo test order_objects_preserve_nt -- --nocapture`, `cargo test market_if_touched -- --nocapture`, `cargo test trailing_stop_market -- --nocapture`, `just source-fence`, `just fmt-check`, and full `cargo test` passed. The first sandboxed `just fmt-check` attempt failed only on the shared verification cache lock at `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; the rerun outside that sandbox restriction passed. Follow-up read-only review found one wording issue in this research ledger and no architecture findings; the wording issue is corrected here. Final branch-state proof must be refreshed after the checked diff is committed and pushed.

## TDD Slice 21 Evidence

- T110 current exact-head state: local `git rev-parse HEAD`, `git ls-remote origin refs/heads/codex/maker-order-proof-clean refs/pull/434/head`, and PR #434 head all report `600b8a583a9420a2a97daa2affa69e18d5869b8f`. `gh pr checks 434` passed GitHub checks including Analyze actions/rust, CodeQL, actionlint, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test. `no-mistakes status` reports daemon running with active run `01KS48XTZQZT8208BH2CECHM8J` on branch `codex/maker-order-proof-clean`, status `running`, head `52d87835`; this is current-branch state but not terminal no-mistakes proof for PR head `600b8a583a9420a2a97daa2affa69e18d5869b8f`.
- T110 Speckit review finding: T099/T105/T109 were checked in `tasks.md`, but T109 still said final branch proof needed refresh. The exact-head evidence above refreshes the GitHub/branch proof while preserving the no-mistakes caveat.
- T110 architecture review finding: public archetype validation still uses hardcoded entry and exit maker/taker tuple allowlists at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:877-1020`. This conflicts with the NT-first/no-hardcoded-policy goal because currently enabled NT order semantics should be validated by reusable order invariants, not by named maker/taker tuple shapes.
- T110 forced-exit review finding: forced-flat exit intentionally uses separate market-exit TOML fields and the same `build_configured_order(...)` path after `forced_exit_order_execution_config(...)` returns an `ExitOrderExecutionConfig`. This remains a strategy forced-exit behavior boundary, not generic maker/taker order-template proof; live adapter-specific position behavior remains residual.
- T110 provider-binding review finding: `binary_oracle_edge_taker` currently builds a Polymarket fee provider in the archetype registration path. This is provider/strategy registration scope, not the generic order-construction path; no venue-agnostic provider claim is made by this order-intent slice.
- T110 NT factory-gap review finding: pinned NT `7c2aafb30fb143069c915a3f2057bb12174405f6` exposes single-order `OrderFactory` methods for market, limit, stop market, stop limit, market if touched, limit if touched, and trailing stop market, but no single-order `market_to_limit` or `trailing_stop_limit` method. `MarketToLimit` and `TrailingStopLimit` remain separate approval/upstream-factory-support scope; enabling them through direct model constructors would violate the `OrderFactory` boundary.
- T111 RED: `cargo test --test config_parsing bolt_v3_archetype_accepts_nt_model_valid_limit_order_templates_without_maker_tuple_policy -- --nocapture` failed with `parameters.entry_order combination is not allowed` for `order_type=limit`, `time_in_force=gtc`, and `is_post_only=false`. This proves the public archetype still rejects an NT `OrderFactory::limit`-reachable order template solely because it is outside the residual maker/taker tuple policy.
- T112 GREEN: public archetype validation now routes entry and exit order checks through one `check_enabled_order_template(...)` helper. The helper validates enabled NT model invariants for market, limit, triggered-market, triggered-limit, and trailing-stop-market order templates instead of named maker/taker tuple shapes, while still rejecting `MarketToLimit` and `TrailingStopLimit` because the pinned single-order `OrderFactory` has no public constructors for them.
- T113 test refactor: tests that asserted old `is_reduce_only=false` / `is_quote_quantity=false` tuple policy for NT factories were changed to preserve real model-invariant rejections (`is_post_only` for market-style factories, missing trigger, non-positive trigger, missing GTD expiry, unsupported factory gaps) and to positively cover NT boolean flag pass-through for `StopLimit` and `LimitIfTouched`. Post-review found unsupported factory-gap coverage only pinned `MarketToLimit`; the unsupported-variant regression now covers both `market_to_limit` and `trailing_stop_limit`, matching the pinned NT `OrderFactory` gap recorded in T110. `cargo test --test config_parsing -- --nocapture` passed 88 tests, and `cargo test --test bolt_v3_strategy_registration -- --nocapture` passed 17 tests before the post-review regression addition; focused verification is refreshed in T114.
- T114 review findings before closeout: one architecture/NT-alignment reviewer reported no findings. The regression/docs reviewers found stale Phase 25 checklist wording, a too-narrow `expire_time_unix_nanos` source-fence audit reason, and missing `trailing_stop_limit` unsupported-variant coverage. The checklist wording now marks T112/T113 complete while keeping T114 open, the source-fence audit reason now covers all enabled GTD order-template invariants, and the unsupported-variant test now covers both factory-gap variants.
- T114 local verification evidence before push: `cargo fmt -- --check` passed; `git diff --check` passed; `cargo test --test config_parsing bolt_v3_archetype_rejects_unsupported_nt_order_type_variants -- --nocapture` passed; `cargo test --test config_parsing -- --nocapture` passed 88 tests; `just source-fence` passed after rerunning with access to the shared rust-verification cache lock; `just clippy` passed; `cargo test --locked --quiet` passed. Post-push exact-head verification for the implementation commit matched local `HEAD`, `origin/codex/maker-order-proof-clean`, and PR #434 head, and `gh pr checks 434` passed the GitHub gate set including `fmt-check`, `source-fence`, `clippy`, `nextest archive`, `nextest shard 1 of 4` through `nextest shard 4 of 4`, `gate`, and `test`.

## TDD Slice 22 Evidence

- T115 no-mistakes triage: fetching the local no-mistakes remote showed active branch head `52d87835723e8fb5052e1d5a23c43c3241043371` on `no-mistakes/codex/maker-order-proof-clean`, with final review commit `no-mistakes(review): Fix triggered exits and IOC entry liveness` and parent review commit `023bf5580c78eebbf4b5f0876324403eed6d9837` `no-mistakes(review): Block exits while entry remains working`. The branch also contained stale replayed Speckit/order commits, so it was not safe to merge wholesale. Local triage accepted only the runtime findings that were reproducible on current HEAD: exits should block while a resting entry can still fill, IOC/FOK entry fills should not retain pending entry state from cache liveness, and triggered-market exits should use configured trigger or activation price instead of requiring book liquidity.
- T116 RED: `cargo test managed_partial_entry_blocks_exit_until_entry_order_resolves -- --nocapture` failed with `left: None`, `right: Some("entry_order_still_working")`; `cargo test non_resting_entry_fill_does_not_keep_pending_entry_from_cache_state -- --nocapture` failed because `pending_entry` remained `Some(...)`; `cargo test stop_market_exit_submission_uses_trigger_price_without_book_liquidity -- --nocapture` failed with `left: Some("exit_price_missing")`, `right: None`.
- T117 GREEN: `entry_order_may_remain_working(...)` now returns false before cache lookup for non-resting entry TIFs, exit evaluation returns `entry_order_still_working` while managed exposure still has a pending entry, and exit submission price selection uses triggered-market `trigger_price` or trailing activation/trigger fields for triggered-market exit configs while preserving the same `build_exit_order_with_execution_config(...)` and NT `OrderFactory` construction path. The three RED tests passed after the fix.
- T118 pre-push local verification evidence: `cargo test managed_partial_entry_blocks_exit_until_entry_order_resolves -- --nocapture`, `cargo test non_resting_entry_fill_does_not_keep_pending_entry_from_cache_state -- --nocapture`, `cargo test stop_market_exit_submission_uses_trigger_price_without_book_liquidity -- --nocapture`, `cargo test exit_submission -- --nocapture`, and `cargo test entry_fill -- --nocapture` passed. `cargo fmt -- --check` passed; `git diff --check` passed; `just source-fence` passed after rerunning with access to the shared rust-verification cache lock; `just clippy` passed; `cargo test --locked --quiet` passed. Post-implementation internal Speckit/source-fence and runtime-correctness reviewers reported no findings for the Phase 26 diff. Post-push exact-head verification matched local `HEAD`, `origin/codex/maker-order-proof-clean`, and PR #434 head; PR #434 was open, non-draft, and mergeable; and `gh pr checks 434` passed. A fresh `no-mistakes rerun` started run `01KS4D04SKAH0ESXETJ3XQTGNW` for `codex/maker-order-proof-clean`, but after the 15-minute cutoff it still reported `status: running`, `head: 52d87835`; per the model-wait rule this is recorded as non-terminal reviewer state, not as an approval.

## TDD Slice 23 Evidence

- T119 source-backed doc drift: current code and tests support NT-backed `oms_type` variants beyond netting (`tests/config_parsing.rs::bolt_v3_strategy_oms_type_accepts_nt_variants`) and enabled order templates for `Limit`, `Market`, `StopMarket`, `StopLimit`, `MarketIfTouched`, `LimitIfTouched`, and `TrailingStopMarket` through `check_enabled_order_template(...)` in `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs`. The active schema doc still described `oms_type` as netting-only, order types as only `limit`/`market`, and a four-tuple maker/taker allowlist. The active status map still named `OmsType::Netting` as a single-value current constraint. Phase 28 superseded the temporary short-side acceptance claim because no-mistakes found the strategy economics were still long-side only.
- T120 RED: after adding `scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_schema_current.py` passed, while `python3 scripts/verify_bolt_v3_schema_current.py` failed on the stale schema/status docs. The failures included netting-only OMS wording, missing enabled order types, missing factory-gap order types, missing order-template fields, missing position-side boundary text, stale maker/taker tuple-policy text, and stale status-map `OmsType::Netting` wording.

## TDD Slice 24 Evidence

- T123 no-mistakes evidence: exact-head no-mistakes run `01KS4F4ZMC5HGYQ248253XFYBS` reviewed replay branch `codex/maker-order-proof-clean-7ea82c9e` and reported two runtime blockers. First, short-side contracts were accepted by config/runtime while `compute_worst_case_ev_bps(...)` and `entry_submission_decision_at(...)` still use long-outcome binary option EV/collateral assumptions. Second, `is_quote_quantity` was forwarded into NT orders while entry sizing still calculated base quantity as `sized_notional / entry_cost` and submit admission still calculated notional as `price * order.quantity()`.
- T123 pinned NT evidence: pinned NT `7c2aafb30fb143069c915a3f2057bb12174405f6` treats quote quantity as a real order quantity mode. `crates/risk/src/engine/mod.rs` skips base min/max quantity checks for quote quantities in `check_quantity(...)`, converts quote quantity to effective base quantity for risk checks, and keeps min/max notional checks as the authoritative cap. Binance spot adapter code maps quote-quantity orders to quote quantity parameters instead of base quantity. This supports fixing entry quote-quantity sizing/admission instead of rejecting the NT flag globally.
- T123 boundary decision: short-side binary-option entry is not an NT order construction invariant. It needs strategy-owned short economics, collateral, and exit semantics, so `binary_oracle_edge_taker` must reject short-side configs until a separate strategy slice proves that model. Entry quote quantity is an NT order-template field and can be supported by sizing entry quantity as quote notional, then admitting submit notional through the same effective-price shape NT risk uses for quote-quantity limit/stop-limit orders. Exit quote quantity remains unsupported in this strategy because exits are sized from the held base position quantity.
- T124 RED: config-path regressions first failed before production changes for short-side rejection and exit `is_quote_quantity` rejection because startup validation emitted no errors. Runtime regressions first failed because quote-quantity submit admission computed `price * order.quantity()` and because entry quote sizing still divided sized notional by entry cost.
- T125 GREEN: startup validation now rejects short-side position contracts for `binary_oracle_edge_taker`, rejects exit order templates with `is_quote_quantity = true`, sizes entry quote-quantity orders as quote notional, rejects exit quote-quantity at the runtime decision and order-build boundaries, and computes quote-quantity submit admission with NT-style effective notional for limit/stop-limit orders using NT cache quote fallback semantics. Focused tests passed: `cargo test quote_quantity -- --nocapture`, `cargo test exit_quote_quantity -- --nocapture`, and `cargo test --test config_parsing -- --nocapture`.
- T125 follow-up review findings: multi-agent follow-up found that the docs verifier missed unsupported-scope overclaims outside the schema doc, and that quote-quantity submit admission used the strategy active book instead of pinned NT `cache.quote(...)`. The verifier now fails injected short-side and exit quote-quantity support claims across status/research/tasks; submit admission now reads NT cache quotes and falls back to the order price when no quote exists. A scan removed hardcoded order-side strings from strategy intent fixtures and made the test-only submit-admission helper reject quote-quantity orders so future quote tests must use the strategy/cache-backed path.
- T125 second follow-up findings: the docs reviewer found the verifier still did not catch unsupported current-scope GTD/live, every-strategy/venue, and live-submit overclaims. `python3 scripts/test_verify_bolt_v3_schema_current.py` failed after adding that regression and passed after expanding the guardrail without rejecting documented GTD template validation facts. The NT quote-risk reviewer then found quote-quantity `Market` entries still fell back to raw quantity instead of NT's market price source. `cargo test quote_quantity_market_submit_admission_uses_nt_cache_quote_ask -- --nocapture` failed with admission notional `25.019` versus NT-derived `25.02060000`; the fix now chooses `cache.quote` ask/bid, then `cache.trade`, before running the same `calculate_base_quantity` and `calculate_notional_value` shape. `cargo test quote_quantity -- --nocapture` passed with seven matching regressions after the fix. A trailing-stop-market activation-price probe did not prove a gap because the compiled NT order exposes activation through `order.trigger_price()`, which the existing admission branch already uses.
- T125 final follow-up findings: the final NT quote-risk/order-side reviewer reported no source/runtime findings. The final docs/verifier reviewer found the schema-current verifier still missed equivalent unsupported-scope phrasings such as GTD live trading, canary support, all configured venues, and short/exit-quote support claims with `without` clauses. `python3 scripts/test_verify_bolt_v3_schema_current.py` failed on those injected cases; the verifier now catches those overclaims while allowing the schema's `RuntimeMode::Live` config wording, and both `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` pass.
- T126 local verification before final branch proof: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_status_map_current.py`, `python3 scripts/verify_bolt_v3_status_map_current.py`, `python3 scripts/verify_bolt_v3_naming.py`, `cargo test quote_quantity -- --nocapture`, `cargo test submit_admission -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `just source-fence` after cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. `just source-fence` first failed only on the shared verification cache lock, then passed with cache access.
- T127 no-mistakes trailing-stop finding: exact-head gate branch `codex/maker-order-proof-clean-806c4ca5` started from PR head `806c4ca5cee7ad04698ed16e9f6d666f42760bb2`; no-mistakes internal review commit `65d6986f744df4370d8ed498348d2b373a26d65d` found that `check_trailing_stop_market_combination(...)` accepted a non-positive `trigger_price` when `activation_price` was positive, and accepted a non-positive `activation_price` when `trigger_price` was positive. Boundary decision: this is a Bolt crash-prevention/order-model validation invariant for currently enabled TrailingStopMarket templates, not venue policy. Any provided companion price must be positive, and at least one of trigger or activation must be present.
- T127 external source review evidence: read-only review against pinned NT `7c2aafb30fb143069c915a3f2057bb12174405f6` confirmed the same gap. The strategy runtime builder already routes `trigger_price_from_config` and `activation_price_from_config` through positive finite price validation, but the public archetype validator did not. NT cannot be relied on here because pinned `OrderFactory::trailing_stop_market(...)` uses `trigger_price.or(activation_price)`, `TrailingStopMarketOrder::new_checked` does not enforce trigger-price positivity, and NT `Price` permits negative prices while positivity is a separate helper.
- T128/T129 RED/GREEN: adding only `non_positive_trigger_with_activation` to `tests/config_parsing.rs::bolt_v3_archetype_rejects_trailing_stop_market_invalid_combinations` made `cargo test --test config_parsing bolt_v3_archetype_rejects_trailing_stop_market_invalid_combinations -- --nocapture` fail with `got: []`; adding the trigger-price validation made it pass. Adding only `non_positive_activation_with_trigger` then made the same focused test fail with `got: []`; adding the activation-price validation made the same focused test pass.
- T130 local verification before branch proof: the post-fix reviewer reported no findings and confirmed the validator rejects any provided non-positive TrailingStopMarket `trigger_price` or `activation_price` while still allowing a single positive activation price. `cargo test --test config_parsing bolt_v3_archetype_rejects_trailing_stop_market_invalid_combinations -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `cargo test trailing_stop_market -- --nocapture`, `just source-fence` after cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. Post-commit exact-head PR and no-mistakes gate state remain pending for this follow-up head.
- T131 no-mistakes reduce-only entry finding: exact-head no-mistakes run `01KS4S400X336BJ5H6QW4K9AVX` for PR head `9239c813ad2503ffbaf7914e4102f51315dc7505` remained non-terminal past the 15-minute wait cap, but its `review.log` reported that `entry_order.is_reduce_only=true` now validates and is passed into entry NT order construction. Local source review and a read-only reviewer confirmed the finding: entry validation delegated to generic order-template checks with no entry-specific reduce-only rejection; runtime passed `self.config.entry_order.is_reduce_only` into `build_configured_order(...)`; `try_submit_entry_order(...)` submitted with `SubmitContext::with_client_id(...)`, so no `PositionId` is attached to the entry submit; pinned NT carries `reduce_only` on the order object and does not turn a reduce-only buy into an opening long. Boundary decision: this is a `binary_oracle_edge_taker` strategy invariant because entries open the managed position and exits reduce it; it is not venue policy and does not narrow NT order-template variants.
- T131 OMS warning triage: a parallel read-only review found the no-mistakes OMS warning is not proven as a current-head bug. Current code intentionally parses NT `OmsType::{Netting,Hedging,Unspecified}` and propagates the value into `StrategyConfig.oms_type`; focused OMS tests passed. The proof remains source/config-level only: docs already avoid claiming live behavior for every OMS mode, and the strategy remains single-managed-position and long-contract scoped. Decision: do not reintroduce a Bolt-only Netting allowlist without a proven runtime/accounting failure.
- T132/T133 RED/GREEN: adding `bolt_v3_archetype_rejects_reduce_only_entry_order` made `cargo test --test config_parsing bolt_v3_archetype_rejects_reduce_only_entry_order -- --nocapture` fail with `messages: []`; adding `reduce_only_entry_order_build_is_rejected_before_nt_factory` made `cargo test reduce_only_entry_order_build_is_rejected_before_nt_factory -- --nocapture` fail by constructing a `LimitOrder` with `reduce_only=true` and `position_id=None`. The fix rejects `entry_order.is_reduce_only=true` in public archetype validation and in the runtime entry builder before NT factory construction. After revising stale pass-through tests to keep entry quote-quantity coverage without reduce-only entry support, `cargo test --test config_parsing -- --nocapture` passed 90 tests and `cargo test reduce_only -- --nocapture` passed the new runtime rejection plus forced-flat reduce-only exit coverage.
- T134 local verification before branch proof: post-fix reviewer reported no findings and confirmed the reduce-only rejection is entry-scoped, exit reduce-only remains config-driven, and the OMS no-change decision matches source evidence. `cargo fmt -- --check`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, `cargo test reduce_only -- --nocapture`, `just source-fence` after runtime-literal audit update and cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. Post-commit exact-head PR and no-mistakes gate state remain pending for this follow-up head.
- T135 no-mistakes market-expiry finding: exact-head no-mistakes run `01KS4VHW1XBQ5JM5GS1CSZJ4GA` on PR head `67371dbadaefcb2146630a50f92831a516266cca` reported that market order templates accepted `expire_time_unix_nanos` while runtime `OrderFactory::market(...)` has no expiry argument. Boundary decision: this is an NT model-preservation invariant for currently enabled market templates, not venue policy.
- T136/T137 RED/GREEN: adding `bolt_v3_archetype_rejects_market_order_expiry` made `cargo test --test config_parsing bolt_v3_archetype_rejects_market_order_expiry -- --nocapture` fail with `got: []`; adding a runtime builder assertion to `configured_order_build_rejects_nt_model_invalid_tif_before_factory` made `cargo test configured_order_build_rejects_nt_model_invalid_tif_before_factory -- --nocapture` fail by constructing an NT `MarketOrder` with the configured expiry dropped. The fix rejects market `expire_time_unix_nanos` in public archetype validation and in the runtime pre-factory guard. The schema doc now lists market expiry as rejected, and the schema-current verifier now includes the active order-intent contract so stale short-side contract overclaims are caught.
- T139 no-mistakes triggered-exit EV finding: no-mistakes run `01KS4XMFYGVHESM4N9JQN7X2TS` on pushed gate ref `codex/maker-order-proof-clean-2f2b1ce1` generated a review fix showing normal exit EV still used live-book pricing while StopMarket exit submission used the configured trigger price. Boundary decision: this is an order-intent consistency invariant for configured triggered exits, not a venue-specific policy.
- T140/T141 RED/GREEN: adding `stop_market_exit_ev_uses_trigger_price_instead_of_live_book` made `cargo test stop_market_exit_ev_uses_trigger_price_instead_of_live_book -- --nocapture` fail because `exit_ev_bps` was computed from the live book instead of the configured trigger price. The fix routes `current_exit_ev_bps_at` through `current_exit_order_for_open_position_with_config`, so StopMarket, MarketIfTouched, and TrailingStopMarket exit EV use the same configured price path as exit submission/admission. The regression intentionally asserts EV pricing only; when the configured trigger makes exit EV worse than hold EV, the strategy correctly holds and does not populate a submission price.
- T122/T126/T130/T134/T138/T142 final branch-state verification: local `HEAD`, `origin/codex/maker-order-proof-clean`, PR #434 `headRefOid`, and no-mistakes gate ref `codex/maker-order-proof-clean-fc70f171` all resolved to `fc70f1713ccb14a878090f01e8cb8fe865de1272`. Local verification for the final implementation head passed `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo fmt -- --check`, `git diff --check`, `cargo test stop_market_exit_ev_uses_trigger_price_instead_of_live_book -- --nocapture`, `cargo test stop_market_exit -- --nocapture`, `cargo test --locked --quiet`, `just source-fence` after cache-lock escalation, and `just clippy`. PR #434 reported `mergeStateStatus: CLEAN`, and GitHub checks passed or skipped for CodeQL, Analyze actions/rust, actionlint, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test. The no-mistakes run `01KS4Z7WS6TR66Z7ZD350J9VPC` for gate branch `codex/maker-order-proof-clean-fc70f171` was still `running` after the 15-minute wait cap, with internal replay head `29d78cac`; its worktree had no uncommitted diff at `2026-05-21 19:08:21 KST`. Per the model-wait rule, this is recorded as non-terminal reviewer state, not a no-mistakes approval.
- T143/T145 spec short-side boundary fix: source inspection found `spec.md` still said coherent short-side configs should not be rejected merely because they are short-side, while current code rejects `binary_oracle_edge_taker` short-side position contracts at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:1107` and `src/strategies/binary_oracle_edge_taker.rs:1151`, with config regression coverage at `tests/config_parsing.rs:864`. The active schema already says short-side is parsed but rejected until strategy-owned short economics, collateral, and exit semantics exist. T144 RED added `test_validate_docs_rejects_stale_spec_short_side_claim`, and `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `TypeError: validate_docs() got an unexpected keyword argument 'spec'`. T145 includes `spec.md` in the schema-current verifier and updates the acceptance scenario to the current strategy-economics boundary.
- T146 verification: `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, and `git diff --check` passed after the spec/verifier update. A false-positive verifier failure on the spec architecture-risk sentence was fixed by treating guarded `wrong` / `instead of` risk wording as non-claim context, with self-test coverage in `test_validate_docs_allows_spec_architecture_risk_context`.
- T147 trailing-stop default review finding: multi-agent pinned-NT review on head `d13653d929dbec264caba67139248b3470623d7a` found that Bolt requires `trigger_type` and `trailing_offset_type` for `TrailingStopMarket` at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:1074` / `:1087` and `src/strategies/binary_oracle_edge_taker.rs:5322` / `:5333`, while pinned NT `OrderFactory::trailing_stop_market` defaults those optional inputs with `trigger_type.unwrap_or(TriggerType::Default)` and `trailing_offset_type.unwrap_or(TrailingOffsetType::Price)` at `crates/common/src/factories/order.rs:501`. Boundary decision: keeping explicit trigger/activation and trailing offset requirements remains strategy/order-model validation, but requiring NT-defaulted enum fields is extra Bolt narrowing and should be removed through the same order-template path.
- T148/T149 RED/GREEN: adding `bolt_v3_archetype_accepts_trailing_stop_market_entry_with_nt_default_fields` made `cargo test --test config_parsing bolt_v3_archetype_accepts_trailing_stop_market_entry_with_nt_default_fields -- --nocapture` fail with validation errors for missing `trigger_type` and `trailing_offset_type`. Adding `trailing_stop_market_order_objects_use_nt_default_types_when_omitted` made `cargo test trailing_stop_market_order_objects_use_nt_default_types_when_omitted -- --nocapture` fail with `entry_trigger_type is required for TrailingStopMarket orders`. The fix removes only those two extra requirements from public archetype and runtime pre-factory validation; the green runtime test proves NT defaults the compiled order to `TriggerType::Default` and `TrailingOffsetType::Price`.
- T150 local verification: `cargo test --test config_parsing bolt_v3_archetype_accepts_trailing_stop_market_entry_with_nt_default_fields -- --nocapture`, `cargo test trailing_stop_market_order_objects_use_nt_default_types_when_omitted -- --nocapture`, `cargo test trailing_stop_market -- --nocapture`, `cargo test --test config_parsing bolt_v3_archetype_rejects_trailing_stop_market_invalid_combinations -- --nocapture`, `cargo fmt -- --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, `cargo test --locked --quiet`, `just source-fence` after cache-lock escalation, and `just clippy` passed. Exact-head PR, GitHub CI, and no-mistakes state must be refreshed after committing this follow-up head.
- T151 no-mistakes non-GTD expiry triage: no-mistakes run `01KS519TNVQC53WQJ3XBCH94WH` remained non-terminal after the 15-minute wait cap, but its internal commit `c6e117e7dca0f4adadb1272e076354abe3970160` proposed a blanket public-validation rejection for `expire_time_unix_nanos` when TIF is not GTD. Local pinned-NT review and two read-only reviewers rejected that patch as NT-over-narrowing: pinned NT `check_time_in_force(...)` only rejects GTD with missing or zero expiry at `crates/model/src/orders/mod.rs:190`, `OrderFactory::{limit, stop_market, stop_limit, market_if_touched, limit_if_touched, trailing_stop_market}` all accept and pass `expire_time` through to their models, and those models store `expire_time`; Market remains the real exception because `OrderFactory::market` has no expiry argument and `MarketOrder::expire_time()` returns `None`. Boundary decision: keep rejecting Market expiry and future factories that drop expiry; do not add a blanket non-GTD expiry rejection unless it is explicitly documented as stricter Bolt config hygiene rather than pinned NT Rust behavior.
- T152/T153 RED/GREEN: adding `test_validate_docs_rejects_blanket_non_gtd_expiry_claim` made `python3 scripts/test_verify_bolt_v3_schema_current.py` fail with `TypeError: validate_docs() got an unexpected keyword argument 'data_model'`. The green change includes `data-model.md` in the schema-current verifier, rejects the stale GTD-only expiry phrase, updates the data model to state that enabled non-market NT factories pass expiry through while GTD requires positive expiry and Market rejects expiry, and adds public/runtime regressions proving non-GTD Limit expiry remains valid and reaches the compiled NT order.
- T154 local verification: `cargo test --test config_parsing bolt_v3_archetype_accepts_non_gtd_limit_expiry_as_nt_pass_through -- --nocapture`, `cargo test non_gtd_limit_order_objects_preserve_nt_expire_time -- --nocapture`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo fmt -- --check`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, `cargo test --locked --quiet`, `just source-fence` after cache-lock escalation, and `just clippy` passed. Exact-head PR, GitHub CI, and no-mistakes state must be refreshed after committing this follow-up head.

## TDD Slice 32 Evidence

- T155 no-mistakes canary-envelope triage: current head `415f016f0be0fc4c078bc2437bacf7da5dd76854` binds the Phase 8 financial envelope to `entry/exit_order_type`, `entry/exit_time_in_force`, `entry/exit_is_post_only`, `entry/exit_is_reduce_only`, and `entry/exit_is_quote_quantity` in `src/bolt_v3_single_submit_evidence.rs`, but does not bind `side`, `position_side`, `expire_time_unix_nanos`, `trigger_price`, `activation_price`, `trigger_type`, `trailing_offset`, or `trailing_offset_type`. Two read-only reviewers classified no-mistakes commit `b30f40440892ea90ff689c3df2b5abfe568c6bc6` as a real live-submit evidence gap, not a source order-construction gap. Boundary decision: adopt this only as Phase 8 approval hardening; do not add any venue, order-template, maker/taker, or NT-factory policy.
- T156 RED: `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` failed after adding the side/position drift regression. Using the actual cargo binary to avoid stale shared-target artifacts, the test failed with `entry side/position drift against loaded TOML should fail closed: ()`, proving current approval validation can return `Ok(())` and consume approval after an entry side or position-side change.
- T157 GREEN: the same focused test passed after binding `entry_side`, `entry_position_side`, `exit_side`, and `exit_position_side` from loaded TOML into the Phase 8 financial envelope and comparison logic. This compares already-approved TOML facts only; it adds no maker/taker, venue, or order-template policy.
- T158 RED: after adding optional order-shape drift cases, the same focused test failed with `optional order-shape drift against loaded TOML should fail closed: ()`, proving expiry, trigger, activation, trigger type, trailing offset, or trailing offset type can drift from the approved financial envelope and still consume approval.
- T159 GREEN: the focused canary precondition test passed after binding optional `expire_time_unix_nanos`, `trigger_price`, `activation_price`, `trigger_type`, `trailing_offset`, and `trailing_offset_type` for both entry and exit orders. Optional fields are compared directly from loaded TOML into approval evidence; no order-template allowlist, venue behavior, or NT factory policy was added.
- T160 local verification: `cargo fmt -- --check`, `git diff --check`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo test --locked --quiet`, `just source-fence` after cache-lock escalation, and `just clippy` passed. The first sandboxed `just source-fence` attempt failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; the rerun with cache-lock access passed. Post-push exact-head PR and no-mistakes state must be refreshed after committing this follow-up head.

## TDD Slice 33 Evidence

- T161 read-only audit evidence: exact-head source inspection and a read-only subagent found that `BoltV3OrderIntentEvidence` only carried strategy id, intent kind, instrument id, client order id, side, price, and quantity, while `data-model.md` required selected compiled order fields needed to explain admission. The same source inspection found `BoltV3AdmissionDecisionEvidence` already records the post-gate outcome separately after `submit_admission().admit(...)`, so duplicating admission outcome into the pre-admission order-intent record would move information backward across the gate boundary.
- T162 RED: adding order-field assertions to `encode_order_intent_line_wraps_intent_with_full_gate_metadata` made `/Users/spson/.cargo/bin/cargo test encode_order_intent_line_wraps_intent_with_full_gate_metadata -- --nocapture` fail to compile with missing `BoltV3OrderIntentOrderFields` and missing `order_fields`, proving the JSONL order-intent schema did not bind selected compiled NT order fields.
- T163 GREEN: `BoltV3OrderIntentEvidence::from_compiled_order(...)` now derives instrument id, client order id, order side, selected admission price, quantity, and nested `BoltV3OrderIntentOrderFields` from the compiled NT `OrderAny`. The nested fields are order type, TIF, compiled price, trigger price, activation price, trigger type, trailing offset, trailing offset type, expiry, post-only, reduce-only, and quote-quantity. The decision-evidence schema version is bumped for the JSON shape change. Entry and exit production paths now construct intent evidence from the compiled order; no maker/taker tuple, venue capability, or order-template policy was added.
- T164 data-model boundary update: `OrderIntentEvidence` is now documented as pre-admission compiled-order evidence, and `AdmissionDecisionEvidence` is documented as the linked post-gate outcome record keyed by strategy id, client order id, and instrument id. This preserves the existing record order: intent evidence, submit-admission decision, then NT submit.
- T165 verification: `/Users/spson/.cargo/bin/cargo test order_intent -- --nocapture` passed, including the compiled `StopMarket` helper regression proving trigger price and selected NT order fields are recorded from the compiled order rather than a fallback literal. `/Users/spson/.cargo/bin/cargo test binary_oracle_edge_taker_records_evidence_then_admission_before_only_direct_submit_call -- --nocapture` passed, preserving intent -> admission -> NT submit ordering. `/Users/spson/.cargo/bin/cargo test submit_admission -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo test --locked --quiet`, `just source-fence`, and `just clippy` passed. `just source-fence` first found the stale runtime-literal audit entry for decision-evidence schema v2 and then hit the shared rust-verification cache lock under sandbox; updating the audit entry to v3 and rerunning with cache-lock access passed.

## TDD Slice 34 Evidence

- T166 read-only audit evidence: pinned NT `Strategy::submit_order(...)` accepts `params: Option<Params>` at `crates/trading/src/strategy/mod.rs:109`, NT `SubmitOrder` stores `params` at `crates/common/src/messages/execution/submit.rs:30`, NT `Params` is generic JSON-backed key/value storage at `crates/core/src/params.rs:36`, and Binance Futures reads adapter-specific submit params such as `close_position` and `price_match` at `crates/adapters/binance/src/futures/execution.rs:427`. Current Bolt already had `SubmitContext.params` and forwarded it into `self.submit_order(...)`, but both constructors set `params: None`, entry used only `SubmitContext::with_client_id(...)`, exit used only `SubmitContext::with_client_id_and_position_id(...)`, and the existing submit-boundary test only string-matched the forwarding call.
- T167 RED: adding `submit_context_routes_non_empty_nt_params_to_submit_order` and calling `SubmitContext::from_parts(Some(client_id), None, Some(params))` made `/Users/spson/.cargo/bin/cargo test submit_context_routes_non_empty_nt_params_to_submit_order -- --nocapture` fail to compile with `no function or associated item named from_parts`, proving there was no generic constructor path that could produce non-empty submit params.
- T168 GREEN: `SubmitContext::from_parts(client_id, position_id, params)` now constructs the generic submit context, and the existing client-id and client-id-plus-position-id constructors delegate to it with `params=None`. The focused regression passed and captured the real NT `TradingCommand::SubmitOrder` emitted through `Strategy::submit_order(...)`, proving the non-empty `Params` map reaches NT as command params. No adapter param keys, TOML schema, maker/taker policy, or venue capability table was added.
- T169 architecture boundary: this slice only proves the generic order-intent submit boundary can carry already-typed NT params. Concrete submit param names and values remain provider-binding or strategy-specific config scope, and must be added through separate TDD slices with adapter source evidence before any venue-specific claim is made.
- T170 verification: `/Users/spson/.cargo/bin/cargo test submit_context_routes_non_empty_nt_params_to_submit_order -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo test --locked --quiet`, `just source-fence`, and `just clippy` passed. The first sandboxed `just source-fence` attempt passed the pure Python fences and failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed. Exact-head PR, GitHub CI, and no-mistakes state must be refreshed after committing this follow-up head.
- T171 follow-up review evidence: a read-only pinned-NT order-template review found no silent parse-and-drop for unmodeled factory fields because public/runtime configs use `deny_unknown_fields`, but did find that `trigger_instrument_id` is an accepted-scope gap for enabled triggered order variants. The same review classified `display_qty`, `exec_algorithm_id`, `exec_algorithm_params`, and `tags` as intentionally unenabled slices already gated by the data model, and `exec_spawn_id` as NT-derived from `exec_algorithm_id` and `client_order_id`. `emulation_trigger` is an unmodeled NT factory argument that should be recorded as residual scope unless enabled through a future TDD slice.

## TDD Slice 35 Evidence

- T171 pinned NT evidence: pinned NT `7c2aafb30fb143069c915a3f2057bb12174405f6` exposes `trigger_instrument_id: Option<InstrumentId>` on enabled triggered `OrderFactory` methods: `stop_market` at `crates/common/src/factories/order.rs:221-238`, `stop_limit` at `:278-298`, `market_if_touched` at `:339-355`, `limit_if_touched` at `:394-413`, and `trailing_stop_market` at `:459-479`. The same signatures expose `emulation_trigger: Option<TriggerType>` immediately before it. NT `Order` exposes `trigger_instrument_id()` and `emulation_trigger()` at `crates/model/src/orders/mod.rs:313-314`. Local source before this slice had no `trigger_instrument_id` in the runtime order config, public archetype projection, order-intent evidence, or Phase 8 approval envelope.
- T172 RED/GREEN construction path: `/Users/spson/.cargo/bin/cargo test --test config_parsing trigger_instrument_id -- --nocapture` passed after the public archetype row accepted `trigger_instrument_id` only for triggered templates and rejected it for non-triggered templates. `/Users/spson/.cargo/bin/cargo test trigger_instrument_id -- --nocapture` first failed because triggered orders compiled with `trigger_instrument_id=None` and non-triggered orders silently dropped the configured field; it passed after threading TOML-owned `trigger_instrument_id` through runtime config, validation, `ExitOrderExecutionConfig`, and the existing enabled triggered `OrderFactory` calls. Limit and Market still reject the field before factory construction.
- T173 decision/live-submit evidence RED/GREEN: adding `trigger_instrument_id` assertions to `order_intent_from_compiled_order_binds_selected_nt_order_fields` first failed to compile because `BoltV3OrderIntentOrderFields` did not expose the field. The green change records compiled NT `order.trigger_instrument_id()` and bumps the decision-evidence schema to v4. Adding entry/exit `trigger_instrument_id` drift cases to `operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config` first failed with `optional order-shape drift against loaded TOML should fail closed: ()`; the green change binds entry and exit trigger instrument IDs into the Phase 8 financial envelope comparison.
- T174 residual order-emulation boundary: `emulation_trigger` is deliberately not added to TOML config, evidence, or submit behavior in this slice. Pinned NT routes emulated orders through strategy order-emulation behavior when `emulation_trigger` is not `NoTrigger`, while current Bolt live-node setup does not enable an emulator. Enabling it needs a separate order-emulation TDD slice with NT emulator source evidence and exact smoke/live-submit proof before any live support claim.
- T175 refreshed current-head verification: `/Users/spson/.cargo/bin/cargo test trigger_instrument_id -- --nocapture` passed at PR #434 head `f7e873bb3906cf4c9842107f941e7dd728a4031a`, including the two runtime builder regressions and two public config parsing regressions for triggered and non-triggered `trigger_instrument_id`. Branch status was clean before the later Phase 42 hardening patch.

## TDD Slice 36 Evidence

- T176 Greptile blocker evidence: PR review thread `PRRT_kwDOR7A3_s6Dz9fG` reported that `exit_evaluation_at(...)` computed `forced_flat_reasons` but returned `entry_order_still_working` before the forced-flat branch when `managed.pending_entry` was present. Local source inspection confirmed `exit_submission_decision_at(...)` then returned without order fields for that block reason. A read-only subagent independently confirmed the same path and noted the existing `managed_partial_entry_blocks_exit_until_entry_order_resolves` regression asserted the unsafe behavior with `SelectionPhase::Freeze`.
- T177 RED: `/Users/spson/.cargo/bin/cargo test forced_flat_exit_submits_despite_resting_pending_entry -- --nocapture` failed with `left: Some("entry_order_still_working")`, `right: None` after adding a regression that materializes a managed position with a resting pending entry and forced-flat reason.
- T178 GREEN: `exit_evaluation_at(...)` now keeps no-position and exit-already-pending as hard blocks, then gives non-empty `forced_flat_reasons` precedence over the managed pending-entry guard. The normal pending-entry guard remains in place for non-forced exits. The split regression enumerates configured outcome instruments from the active fixture instead of selecting a hardcoded side.
- T179 RED/GREEN lifecycle proof: `/Users/spson/.cargo/bin/cargo test forced_flat_submit_cancels_resting_entry_and_recovers_if_entry_fill_races -- --nocapture` first failed because no `TradingCommand::CancelOrder` was emitted for the resting entry. Pinned NT `Strategy::cancel_order(...)` at `~/.cargo/git/checkouts/nautilus_trader-*/7c2aafb/crates/trading/src/strategy/mod.rs:418-490` reads the order from cache, marks it pending-cancel, sends `TradingCommand::CancelOrder` through emulator/algo/exec routing, and clears the GTD expiry timer when NT manages it. The green change calls that NT path before forced-flat exit reliance and marks a raced managed-entry fill as residual exposure while exit is pending.
- T181 local verification after the lifecycle fix: `/Users/spson/.cargo/bin/cargo test forced_flat -- --nocapture`, `/Users/spson/.cargo/bin/cargo test managed_partial_entry_blocks_normal_exit_until_entry_order_resolves -- --nocapture`, `/Users/spson/.cargo/bin/cargo test exit_submission -- --nocapture`, `/Users/spson/.cargo/bin/cargo test entry_fill -- --nocapture`, `/Users/spson/.cargo/bin/cargo test trigger_instrument_id -- --nocapture`, `/Users/spson/.cargo/bin/cargo test order_intent -- --nocapture`, `/Users/spson/.cargo/bin/cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just source-fence` after rust-verification cache-lock escalation, `just clippy`, and `/Users/spson/.cargo/bin/cargo test --locked --quiet` passed. Exact-head PR, GitHub CI, Greptile thread state, and no-mistakes state must still be refreshed after committing and pushing this head.
- T181 refreshed current-head verification: `/Users/spson/.cargo/bin/cargo test forced_flat -- --nocapture` passed at PR #434 head `f7e873bb3906cf4c9842107f941e7dd728a4031a`, including the forced-flat market-exit selection, reduce-only preservation, pending-entry override, NT cancel emission, and raced-fill residual recovery regressions. Branch status was clean before the later Phase 42 hardening patch.

## TDD Slice 37 Evidence

- T182 multi-agent architecture evidence: two read-only reviewers agreed that current PR head `c11d759d0142049c274ea20afb9a805f90aeb308` uses NT `OrderFactory` broadly, but reusable NT order-template mechanics were still housed in `src/strategies/binary_oracle_edge_taker.rs` and generic public template checks were still in `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs`. The reviewers separated movable mechanics (`OrderFactory` validation/building and generic template checks) from strategy-owned behavior (position contracts, entry reduce-only rejection, exit quote-quantity sizing, forced-flat lifecycle, evidence, admission, and submit context).
- T183 RED: `cargo test --test bolt_v3_order_intent -- --nocapture` failed with `could not find bolt_v3_order_intent in bolt_v2` after adding a strategy/venue/market agnostic regression that expects `NtOrderTemplate + NtOrderBuildInputs + NT OrderFactory -> OrderAny` and a source fence forbidding strategy, provider, market-family, admission, and submit coupling in the shared module.
- T184/T185 GREEN: `src/bolt_v3_order_intent.rs` now owns only typed NT template structs, config-time NT template invariant checks, runtime NT model validation, and `build_nt_order(order_factory, prefix, template, inputs)`. It accepts NT `OrderFactory` directly and does not import `StrategyCore`, strategy IDs, position side, provider bindings, evidence, admission, or submit context. `binary_oracle_edge_taker` converts its local config into an `NtOrderTemplate` and calls the shared builder while keeping strategy economics, lifecycle, evidence, admission, and submit context in the strategy module.
- T186 verification: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo test --test config_parsing maker -- --nocapture`, `cargo test --test config_parsing unsupported_nt_order_type_variants -- --nocapture`, `cargo test --lib configured_order -- --nocapture`, `cargo test --lib limit_if_touched -- --nocapture`, `cargo test --lib trailing_stop_market -- --nocapture`, `cargo test --lib submit_context_routes_non_empty_nt_params_to_submit_order -- --nocapture`, `cargo test --lib forced_flat -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test --test config_parsing -- --nocapture`, `cargo test --lib -- --nocapture`, `just source-fence` after rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. The first sandboxed `just source-fence` failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed.

## TDD Slice 38 Evidence

- T187 latest-head external review state: PR #434 head was confirmed as `f7e873bb3906cf4c9842107f941e7dd728a4031a` against `main` by `gh pr view --json number,headRefName,baseRefName,headRefOid,url,title,state` after `git fetch origin`. Gemini review job `440914cf-87b0-4b9e-8432-a4faaa690b74` sent 38 branch-diff files for head `f7e873bb3906cf4c9842107f941e7dd728a4031a` versus base `d55ccfefc316928a67a23cf076c8b7e584e011bd` and returned `Verdict: APPROVE` with no blocking or non-blocking findings. Grok job `job_3b3e1712-875a-4e09-a957-1b1499cbb98c` failed before source transmission with `scope_file_too_large:src/strategies/binary_oracle_edge_taker.rs:536419`. Kimi job `dc8cbe26-0855-4eb4-ab05-a9374029bcb4` sent source but failed quality gating with `review_not_completed: missing_verdict`; its prose was not treated as approval. Claude job `91d9b5d1-06b9-40da-ba9b-ba9d4560bc5e` used subscription/OAuth mode, sent source, returned raw `Verdict: APPROVE`, but failed the clean review slot with `review_not_completed: permission_blocked` because it could not read the pinned NT checkout or git state.
- T187 usable reviewer finding from failed Claude slot: although the Claude slot is not a clean approval, its raw review identified a concrete non-blocking public-API validation asymmetry: `check_nt_order_template_config(...)` rejected non-positive trigger and activation prices, but direct callers of the new shared `build_nt_order(...)` API could still pass zero `Price` values into `OrderFactory`. This is a Bolt generic NT-model validation gap, not venue policy.
- T188 RED: `cargo test --test bolt_v3_order_intent shared_nt_order_template_rejects_non_positive_trigger_inputs_before_nt_factory -- --nocapture` failed because a zero-price `StopMarket` template constructed an NT `StopMarketOrder` instead of returning a Bolt validation error before the factory call.
- T189 GREEN: `validate_nt_order_template(...)` now rejects non-positive `trigger_price` for enabled triggered factory variants and rejects non-positive `trigger_price` or `activation_price` for `TrailingStopMarket` before `OrderFactory` construction. The fix only mirrors existing generic NT model validation on typed runtime inputs and adds no strategy, venue, market, maker-only, or taker-only policy.
- T190 verification: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, source-fence grep against `src/bolt_v3_order_intent.rs`, `just source-fence` after rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed.

## TDD Slice 39 Evidence

- T191 latest-head Claude NB3: Claude Code review job `6307b6f5-5f1d-4847-b62c-3441881ba66f` reviewed PR #434 head `afcfeb89fab3845bf2ab91ab2a90ed04c42b6e32` against base `d55ccfefc316928a67a23cf076c8b7e584e011bd`, sent source, returned `Verdict: APPROVE`, and listed no blocking findings. Its NB3 noted that the direct shared-builder regression only exercised StopMarket zero-trigger and TrailingStopMarket zero-activation, while StopLimit, MarketIfTouched, LimitIfTouched, and TrailingStopMarket zero-trigger were not enumerated in that direct API test.
- T192 regression closure: `tests/bolt_v3_order_intent.rs::shared_nt_order_template_rejects_non_positive_trigger_inputs_before_nt_factory` now enumerates StopMarket, StopLimit, MarketIfTouched, LimitIfTouched zero-trigger rejection, plus TrailingStopMarket zero-trigger and zero-activation rejection, through the shared `build_nt_order(...)` API. `cargo test --test bolt_v3_order_intent shared_nt_order_template_rejects_non_positive_trigger_inputs_before_nt_factory -- --nocapture` passed immediately after the test expansion, proving Phase 42 production validation already covered these enabled triggered variants and no production behavior change was needed.
- T193 verification: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just clippy`, and `cargo test --locked --quiet` passed. `just source-fence` first passed all pure Python fences and failed only on the shared rust-verification cache lock at `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed.

## TDD Slice 40 Evidence

- T194 latest-head external review state: PR #434 head was confirmed as `3e3679f3763c59139041bb36bff460f05136668d` after pushing Phase 43. Gemini job `14f47943-7218-4307-b353-66f8b968c00d` reviewed head `3e3679f3763c59139041bb36bff460f05136668d` against `origin/main` `c1a4edbdd5202ea507750f069c64d8edca044381`, sent source, returned `Verdict: APPROVE`, and listed no blocking or non-blocking concerns. Claude Code subscription job `5e7624e2-011d-4d13-a3fa-f786b034196f` reviewed the same head/base, sent source, returned `Verdict: APPROVE`, and listed no blocking findings. Its non-blocking concerns were direct shared-builder coverage nits: add negative trigger/activation coverage, and add direct API coverage for other generic `validate_nt_order_template(...)` invariants that had only indirect strategy/config coverage.
- T195/T196 regression closure: `tests/bolt_v3_order_intent.rs` now covers negative trigger/activation rejection for StopMarket, StopLimit, MarketIfTouched, LimitIfTouched, and TrailingStopMarket through `build_nt_order(...)`. It also directly covers GTD-without-expiry rejection, Market GTD rejection, TrailingStopMarket post-only rejection, LimitIfTouched BUY/SELL trigger-vs-price rejection, and non-triggered Limit/Market trigger rejection. `cargo test --test bolt_v3_order_intent -- --nocapture` passed immediately with 6 tests, proving the existing production validation already enforced the expanded direct API matrix and no production behavior change was needed.
- T197 verification: After refactoring the direct-builder regression helpers to derive invalid boundary prices from the base NT `Price` value instead of standalone arbitrary numeric prices, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just clippy`, and `cargo test --locked --quiet` passed. `just source-fence` again passed all pure Python fences and failed only on sandbox access to `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed, including `tests/bolt_v3_controlled_connect.rs` and `tests/bolt_v3_production_entrypoint.rs`.

## TDD Slice 41 Evidence

- T198 latest-head review state: After pushing Phase 44, PR #434 head was confirmed as `e48c5493321ec3cd05b7d2baa6010720527f22a2` against `origin/main` `c1a4edbdd5202ea507750f069c64d8edca044381`; GitHub checks `Analyze (actions)`, `Analyze (rust)`, and `CodeQL` passed. Gemini job `9822321c-7ccf-43fb-8db5-08320a1eb0a5` reviewed that exact head/base, sent source, returned `Verdict: APPROVE`, and listed no blocking or non-blocking concerns. Claude Code subscription job `0fa47d4b-1161-45c9-9473-51ca207689f6` reviewed the same head/base, sent source, returned `Verdict: APPROVE`, and listed no blocking findings. Its non-blocking residuals were remaining direct-builder validation arms covered only indirectly, Market/StopMarket/MarketIfTouched post-only checks without direct tests, and pre-existing `Entry`/`Exit` source-fence brittleness.
- T199/T200 regression closure: `tests/bolt_v3_order_intent.rs` now directly covers Market-with-expiry rejection, missing trigger prices for triggered order types, non-trailing activation/trailing fields, non-triggered trigger type/instrument fields, TrailingStopMarket trigger/activation and trailing-offset requirements, and order-arm post-only rejection for Market, StopMarket, and MarketIfTouched. The source fence now tokenizes the broad `Entry`/`Exit` terms while preserving substring checks for the more specific forbidden strategy/venue/submit patterns. The first focused run failed because the post-only Limit test setup still used the old literal expiry while the assertion used the derived expiry helper; after fixing the setup and replacing remaining LIT literal price assertions with derived base-price helpers, `cargo test --test bolt_v3_order_intent -- --nocapture` passed with 8 tests.
- T201 verification: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just clippy`, and `cargo test --locked --quiet` passed. `just source-fence` passed all pure Python fences and failed only on sandbox access to `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed, including `tests/bolt_v3_controlled_connect.rs` and `tests/bolt_v3_production_entrypoint.rs`.

## TDD Slice 42 Evidence

- T202 latest-head review state: PR #434 head remained `9c57563d9717642d643f4c59d789a69ea64e588d` and `origin/main` was `c1a4edbdd5202ea507750f069c64d8edca044381`. Gemini job `9eb5bcef-bbb4-48dc-8d08-cf1e7206aa15` sent source and returned `Verdict: APPROVE` with no blockers; its only non-blocker was redundant TrailingStopMarket `is_post_only` validation. Claude Code subscription job `f0104b2e-e64e-49d5-b866-67370f4848c4` failed with provider `usage_limited` and was not counted. Kimi job `40981595-7507-4bcd-bb2e-ec12f681de5b` sent source but failed quality gating with `review_not_completed: review_quality_failed:shallow_output,missing_verdict`. Grok branch job `job_240387ee-9ce2-4136-994b-aa74dc03db3b` failed before source transmission with `scope_file_too_large:src/strategies/binary_oracle_edge_taker.rs:536419`; Grok custom job `job_11395bee-79e0-49b8-9e49-18cd9d689f9a` failed before source transmission because local Grok CLI auth timed out. Claude Code subscription retry job `7569413f-6c9d-4fdb-a2fb-91f9c8306114` sent 38 branch-diff files, returned `Verdict: APPROVE`, and listed no blocking findings.
- T203 source-fence disposition: Claude retry NB1 suspected runtime-literal audit drift for the five `value.as_u64() == 0` expiry checks in `src/bolt_v3_order_intent.rs`. Exact-head `just source-fence` disproved the concern: all pure verifiers, including `scripts/verify_bolt_v3_runtime_literals.py`, passed; the first sandboxed run failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`, and the rerun with cache-lock access passed through `tests/bolt_v3_controlled_connect.rs` and `tests/bolt_v3_production_entrypoint.rs`.
- T204 trigger-instrument regression: Claude retry identified missing direct coverage for non-`None trigger_instrument_id` on StopLimit, MarketIfTouched, LimitIfTouched, and TrailingStopMarket. `tests/bolt_v3_order_intent.rs::shared_nt_order_template_preserves_trigger_instrument_id_for_triggered_factories` now enumerates StopMarket, StopLimit, MarketIfTouched, LimitIfTouched, and TrailingStopMarket through shared `build_nt_order(...)` and asserts the compiled NT order preserves `trigger_instrument_id`. `cargo test --test bolt_v3_order_intent shared_nt_order_template_preserves_trigger_instrument_id_for_triggered_factories -- --nocapture` passed immediately, proving the implementation was already correct and only coverage was missing.
- T205 limit-price disposition: Claude retry suggested rejecting non-positive `inputs.price` for limit-style orders before NT factory construction. A red experiment proved the current shared builder lets zero Limit price reach NT and NT constructs the order. Pinned NT source then showed this is not a local NT crash-prevention invariant: `LimitOrder::new_checked`, `StopLimitOrder::new_checked`, and `LimitIfTouchedOrder::new_checked` document and enforce quantity/display/GTD/side-trigger checks but do not reject non-positive limit `price`; `OrderFactory::{limit,stop_limit,limit_if_touched}` pass `price` directly to those models. Because the order-intent layer must not add venue/market/strategy policy or narrow NT-supported values, no shared-layer positive limit-price rejection was added. Strategy economics still filter live strategy-derived prices with `is_positive_finite(...)` before order build.
- T206 verification: after the Phase 46 regression/docs refresh, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just clippy`, `just source-fence`, and `cargo test --locked --quiet` all passed locally. The branch still requires a post-push PR check refresh for the new head.

## TDD Slice 43 Evidence

- T207 current-head architecture evidence: PR #434 head `fb9c592d07aad5c177d934842e6174cd12811e90` was clean locally and pushed before this slice. Read-only multi-agent review found the shared `src/bolt_v3_order_intent.rs` builder remained strategy, venue, market, and submit agnostic, but `BinaryOracleEdgeTaker::forced_exit_order_execution_config(...)` still synthesized forced-flat order semantics in strategy code: `OrderType::Market`, no expiry, no trigger or trailing fields, `is_post_only=false`, `is_quote_quantity=false`, and reduce-only/TIF from separate market-exit fields. That was a real scope gap because forced-exit urgency is strategy behavior, while order shape must be TOML-owned NT order-template data.
- T208 RED evidence: adding `bolt_v3_archetype_accepts_configured_forced_exit_order_template` first failed because public strategy TOML rejected unknown `forced_exit_order`; adding `forced_flat_exit_order_object_uses_configured_forced_exit_template` first failed to compile because `BinaryOracleEdgeTakerConfig` had no `forced_exit_order` field. Those failures proved the configured forced-exit template was not accepted or used end to end.
- T209 GREEN evidence: `forced_exit_order` now uses the same typed `OrderParams`/`BinaryOracleEdgeTakerOrderConfig` shape as entry and exit orders, is validated through `check_nt_order_template_config(...)`, is projected through raw runtime config, and is converted to `ExitOrderExecutionConfig` before the existing shared `build_configured_order(...)`/`build_nt_order(...)` path. The old top-level `market_exit_time_in_force` and `market_exit_reduce_only` config fields and dead parser were removed. Forced-exit-specific strategy policy is limited to sizing from base position quantity and requiring side/position-side consistency with the normal exit order.
- T209 focused verification: `cargo test --test config_parsing bolt_v3_archetype_accepts_configured_forced_exit_order_template -- --nocapture`, `cargo test forced_flat_exit_order_object_uses_configured_forced_exit_template -- --nocapture`, `cargo test forced_flat -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `cargo test --test bolt_v3_strategy_registration -- --nocapture`, and `cargo test --test bolt_v3_order_intent -- --nocapture` passed after the green change and config-test helper isolation.
- T210 final local verification: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. The first sandboxed `just source-fence` attempt failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`.

## TDD Slice 44 Evidence

- T211 latest-head multi-agent evidence: follow-up review on pushed head `b84141dc2d2e223bbe07202a033eeb197d91a6a8` found that active schema docs still described removed top-level `market_exit_time_in_force` and `market_exit_reduce_only` fields, and that `manage_stop=true` still delegates to NT's built-in market close path. Pinned NT source confirms `Strategy::close_all_positions(...)` constructs `core.order_factory().market(...)` at `/Users/spson/.cargo/git/checkouts/nautilus_trader-3c6af4345b4d438b/7c2aafb/crates/trading/src/strategy/mod.rs:943`, and `Strategy::market_exit(...)` calls `close_all_positions(... Some(time_in_force), Some(reduce_only), None)` around the same module's market-exit loop. Boundary decision: Bolt must not pretend `manage_stop=true` can honor a non-market configured forced-exit template. The guard is an NT capability boundary for this strategy mode, not a venue, market, maker, or taker policy.
- T212 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` first failed after adding coverage for removed market-exit field names and required `forced_exit_order` docs. The failure reported the expected stale forced-exit fragment was not caught, proving the verifier did not guard the current schema contract.
- T213 RED: `cargo test --test config_parsing bolt_v3_archetype_rejects_manage_stop_with_non_market_forced_exit_order -- --nocapture` first failed with `messages: []` after mutating the public strategy fixture to `manage_stop = true` and `parameters.forced_exit_order.order_type = "limit"`, proving config validation accepted semantics NT `close_all_positions` cannot honor.
- T214 GREEN: the schema verifier now rejects stale `market_exit_time_in_force`, `market_exit_reduce_only`, and "separate market-exit TOML fields" wording and requires the active schema to document `forced_exit_order` plus the NT `manage_stop` market-close boundary. The active schema examples now include `[parameters.forced_exit_order]` and no longer include removed top-level market-exit order fields. Public strategy validation now rejects `manage_stop=true` unless `parameters.forced_exit_order.order_type = "market"`, while preserving non-market forced-exit templates for the strategy-owned forced-flat path when `manage_stop=false`.
- T214 focused verification: `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo test --test config_parsing bolt_v3_archetype_rejects_manage_stop_with_non_market_forced_exit_order -- --nocapture`, and `cargo test --test config_parsing bolt_v3_archetype_accepts_configured_forced_exit_order_template -- --nocapture` passed after the GREEN change. Full local verification, commit/push, exact-head PR checks, and no-mistakes state still need refresh for T215.
- T215 local verification before commit: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test --test config_parsing -- --nocapture`, `cargo test forced_flat -- --nocapture`, `cargo test --test bolt_v3_order_intent -- --nocapture`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed. Post-push exact-head PR checks and no-mistakes state must be refreshed after committing this verified diff.

## TDD Slice 45 Evidence

- T216 pinned NT model-surface evidence: `OrderType` includes `MarketToLimit = 5` and `TrailingStopLimit = 9` in `/Users/spson/.cargo/git/checkouts/nautilus_trader-3c6af4345b4d438b/7c2aafb/crates/model/src/enums.rs:1552` and `:1560`. The Rust single-order `OrderFactory` exposes `market`, `limit`, `stop_market`, `stop_limit`, `market_if_touched`, `limit_if_touched`, and `trailing_stop_market` at `/Users/spson/.cargo/git/checkouts/nautilus_trader-3c6af4345b4d438b/7c2aafb/crates/common/src/factories/order.rs:119`, `:164`, `:221`, `:278`, `:339`, `:394`, and `:459`, but no direct single-order `market_to_limit` or `trailing_stop_limit` methods. NT model builders and direct constructors can construct both variants, but doing that from Bolt would require Bolt to supply factory-owned IDs, UUIDs, timestamps, defaults, contingency fields, and exec-spawn semantics itself.
- T217 Bolt price/admission evidence: the shared `NtOrderBuildInputs` required `price` even though `build_nt_order(...)` consumes that price only for `Limit`, `StopLimit`, `LimitIfTouched`, and LimitIfTouched side validation. Strategy entry, normal exit, and forced exit still require a positive selected price before submission for strategy economics, sizing, order-intent fallback price, and submit-admission notional; admission already derives price from compiled `order.price()`, then `order.trigger_price()`, then the intent fallback price.
- T218 architecture decision: keep the shared order-intent layer Rust `OrderFactory`-only for PR #434. `MarketToLimit` and `TrailingStopLimit` remain explicit residual scope unless upstream Rust factory support or a separately approved design justifies direct model construction. This preserves the Speckit contract that Bolt does not reimplement NT lifecycle/defaulting and does not add direct constructors, venue matrices, or a parallel submit path.
- T219 RED/GREEN: `cargo test --test bolt_v3_order_intent shared_nt_order_template_price_is_required_only_for_limit_price_factories -- --nocapture` first failed to compile because `NtOrderBuildInputs.price` was a mandatory `Price`; after making only the shared builder input `Option<Price>`, market-like factory paths build without a limit price and `Limit`, `StopLimit`, and `LimitIfTouched` reject missing price before NT factory construction. Strategy call sites continue passing `Some(price)` so evidence/admission economics remain unchanged. Verification after the GREEN change: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `cargo test forced_flat -- --nocapture`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed.

## TDD Slice 46 Evidence

- T220 current-head review state: PR #434 head is `459382d90efef75af7c2536c1472980b8702e9e9`; GitHub GraphQL reported `reviewThreads` total `6`, unresolved `0`, and current unresolved `0`. The old PR body still contained a Greptile "not safe to merge" summary from stale commit `c11d759d0142049c274ea20afb9a805f90aeb308`; the PR body was refreshed to current head, task `T219`, green checks, unresolved thread count `0`, and explicit residual scope. The stale body nevertheless identified two current-source behavior gaps: `visible_book_notional_cap(...)` still calls `max_execution_within_vwap_slippage_bps(order_side, ...)` directly in `src/strategies/binary_oracle_edge_taker.rs`, so post-only Buy entries cap against ask depth instead of passive bid depth; `on_position_closed(...)` still turns `ExposureState::Managed(position)` into `Flat` without cancelling `position.pending_entry`, unlike the forced-flat path which already calls NT `Strategy::cancel_order(...)` before relying on the forced exit.
- T221 RED/GREEN: `cargo test post_only_entry_book_impact_cap_uses_passive_side_book -- --nocapture` first failed with `left: Some(60.0)` and `right: Some(3.08)`, proving post-only Buy entry sizing used taker ask-side visible depth instead of passive bid-side depth. The GREEN change maps strategy-owned visible-book sizing to the order side that would consume the resting side under the configured post-only mode, leaving shared NT order construction unchanged. The focused test then passed.
- T222/T223 RED/GREEN: `cargo test position_closed_cancels_managed_resting_pending_entry_before_flattening -- --nocapture` first failed because no `TradingCommand::CancelOrder` was emitted when an externally closed Managed position still had a resting pending entry. The GREEN change uses NT `Strategy::cancel_order(...)` for that pending entry on the Managed-close path and retains `ExposureState::PendingEntry(...)` until the cancel lifecycle event clears it, preserving context for cancel/fill races instead of dropping to Flat early. `cargo test position_closed_cancels_managed_resting_pending_entry_and_keeps_context -- --nocapture` passed.

## TDD Slice 47 Evidence

- T225 current-head multi-agent evidence: internal review on current pushed head `5496282dee8e1d6111c08ce90ca215198e6b2c9d` found active schema drift for `TrailingStopMarket` defaults. Current code/tests accept omitted `trigger_type` and `trailing_offset_type` in `tests/config_parsing.rs` and prove compiled NT orders use defaults in `src/strategies/binary_oracle_edge_taker.rs`, while `docs/bolt-v3/2026-04-25-bolt-v3-schema.md` still said both fields were required.
- Pinned NT evidence: `OrderFactory::trailing_stop_market` at `~/.cargo/git/checkouts/nautilus_trader-*/7c2aafb/crates/common/src/factories/order.rs:459` accepts optional `trigger_type` and `trailing_offset_type`, then defaults omitted values with `trigger_type.unwrap_or(TriggerType::Default)` and `trailing_offset_type.unwrap_or(TrailingOffsetType::Price)` at the same factory method.
- T226 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `AssertionError: expected stale TrailingStopMarket fragment 'trigger_type', got []`, proving the schema-current verifier did not catch stale required-default-field wording.
- T227 GREEN: the verifier now rejects `trigger_type` and `trailing_offset_type` sections that require `trailing_stop_market` defaulted fields, rejects the stale validation sentence requiring explicit type fields, and requires active schema text documenting NT default pass-through. `docs/bolt-v3/2026-04-25-bolt-v3-schema.md` now describes both fields as optional for `trailing_stop_market` with NT defaults. `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` passed.
- T229 internal review follow-up: a read-only reviewer found that the first verifier patch caught only the exact old `- required for trailing_stop_market` lines and missed equivalent stale wording such as ``trigger_type` is required for `trailing_stop_market`` or ``trailing_offset_type` must be provided when order_type is `trailing_stop_market``. This was a verifier robustness gap, not a runtime order-construction gap.
- T229 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `AssertionError: expected equivalent stale TrailingStopMarket fragment 'trigger_type', got []` after adding equivalent-wording coverage.
- T230 GREEN: the verifier now scans the `trigger_type` and `trailing_offset_type` sections for required/mandatory/must-be-provided language tied to `trailing_stop_market`, while ignoring optional/default/omitted wording. `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` passed.
- Second read-only current-head review found no additional docs/evidence mismatch: pinned NT defaults, active schema, shared builder validation/pass-through, config parsing coverage, strategy order-object coverage, and Speckit Phase 51 evidence were aligned.

## TDD Slice 48 Evidence

- T231 read-only Speckit audit on exact head `415236cee56eeb7a94ed3c914399765473e32c21` found stale dependency notes for completed Phase 47, Phase 48, and Phase 50 work. The dependency section still described forced-flat market synthesis, stale market-exit schema/manage-stop behavior, and maker sizing/Managed-close lifecycle bugs as active blockers even though the task rows and research evidence record those fixes as complete.
- T231 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `AssertionError: expected stale completed-Phase-50 fragment 'Phase 50', got []` after adding stale Phase 50 dependency-note coverage. After expanding the RED set for the audit findings, the verifier also needed coverage for stale Phase 47 and Phase 48 blocker wording and missing Phase 51 dependency tracking.
- T232 GREEN: the schema-current verifier now rejects stale completed Phase 47, Phase 48, and Phase 50 dependency-note phrases in `tasks.md`, requires a Phase 51 dependency note when tasks are checked, and the Speckit dependency section no longer records those completed slices as active blockers. `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo fmt -- --check`, and `git diff --check` passed before commit.
- Current-head architecture audit found no shared-layer findings: `src/bolt_v3_order_intent.rs` imports only NT factory/model types, builds supported variants through NT `OrderFactory`, rejects factory-gap variants, keeps maker/taker as NT fields rather than modes, and keeps venue/runtime proof boundaries explicit in the contract and schema.

## TDD Slice 49 Evidence

- T234 read-only Speckit audit on exact head `ad098eb0227ee63d3ced29b13be046ae0ccc41f6` found that the dependency section still described Phase 34 as an active completion blocker for TrailingStopMarket optional NT-default fields even though Phase 34 tasks are complete and Phase 51 records the schema/default wording closure.
- T234 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `AssertionError: expected stale completed-Phase-34 fragment 'Phase 34', got []` after adding completed-Phase-34 dependency-note coverage.
- T235 GREEN: the schema-current verifier now rejects the stale Phase 34 dependency-note sentence, and the Speckit dependency section records Phase 34 as the closed NT-default pass-through implementation with Phase 51 owning the later schema/default wording closure.

## TDD Slice 50 Evidence

- T237 exact-head no-mistakes evidence: current PR head `09d6bbf81ea3426f713b6c2d6c550281a095d38f` was pushed to the local no-mistakes gate branch `codex/maker-order-proof-clean-09d6bbf8`. The daemon started run `01KS6TZ12T26DKSVRQ4HNBH01A` for that exact head at `2026-05-22 12:16:48 KST`. The run advanced its internal review head to `49c29be0e81a4adc2b9cb0d7017fe779424e27d8`, commit message `no-mistakes(review): Bind forced-exit approval envelope`, changing only `src/bolt_v3_single_submit_evidence.rs` and `tests/bolt_v3_single_submit_preconditions.rs`. Read-only triage accepted the finding as a Phase 8 operator approval-envelope gap: Phase 47 made forced-flat order semantics TOML-owned through `forced_exit_order`, but `Phase8FinancialEnvelopeEvidenceFile` still bound only entry and normal-exit order-shape fields. This is not a shared order-intent-layer or venue-policy gap.
- T238 RED: after adding a regression that mutates loaded TOML `parameters.forced_exit_order.time_in_force` to the normal exit TIF, `command cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` failed with `forced exit order drift against loaded TOML should fail closed: ()`, proving the existing approval envelope accepted the drift and consumed approval.
- T239 GREEN: `Phase8FinancialEnvelopeEvidenceFile` now requires and compares the TOML-owned forced-exit side, position side, order type, TIF, optional expiry, trigger, activation, trigger type, trigger instrument, trailing offset, trailing offset type, post-only, reduce-only, and quote-quantity fields. The existing approval-envelope test now includes forced-exit fields in valid envelope fixtures, covers required forced-exit TIF, order-type, and reduce-only drift, and covers forced-exit optional-field drift in the same order-shape loop as entry and normal exit. `command cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` passed, followed by the full `command cargo test --test bolt_v3_single_submit_preconditions -- --nocapture` suite with 57 passing tests.
- T224/T228/T233/T236/T240 implementation-head verification: pre-closure implementation head `64147176a2ac0b74c893ccfc661f93c0573047af` matched local `git rev-parse HEAD` and PR #434 `headRefOid` before the docs/verifier closure commit, with `mergeStateStatus=CLEAN` and `mergeable=MERGEABLE`. GitHub checks passed for Analyze actions/rust, CodeQL, Greptile Review, actionlint, actionlint-annotations, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test. Local verification after clearing stale Cargo artifacts passed `cargo test --lib post_only_entry_book_impact_cap_uses_passive_side_book -- --nocapture`, `cargo test --lib position_closed_cancels_managed_resting_pending_entry_and_keeps_context -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `cargo fmt -- --check`, `git diff --check`, and `just source-fence` after the expected rust-verification cache-lock escalation. Read-only architecture review on that head found no shared order-intent findings: `src/bolt_v3_order_intent.rs` stays strategy, venue, market-family, submit, and admission agnostic; maker/taker behavior is represented as NT order-template fields forwarded to NT `OrderFactory`; and no shared order construction path hardcodes side, order type, TIF, post-only, reduce-only, quote quantity, venue, market family, or strategy name. Read-only Speckit audit found T224, T228, T233, T236, and T240 were terminal-verification rows only, with no unimplemented architecture or code work.
- T224/T228/T233/T236/T240 no-mistakes disposition: no-mistakes run `01KS6WD4H8GRR6ESGDMB79JVKG` on branch `codex/maker-order-proof-clean-64147176` started at `2026-05-22 12:41:59 KST` and still reported `status=running`, `head=64147176` at `2026-05-22 13:03:35 KST`, exceeding the 15-minute wait cap. `git ls-remote no-mistakes refs/heads/codex/maker-order-proof-clean-64147176` returned `64147176a2ac0b74c893ccfc661f93c0573047af`, so that gate branch had not advanced to a hidden review commit. This is recorded as non-terminal reviewer state, not as a no-mistakes approval. Follow-up docs/verifier-only heads must be checked against live PR/no-mistakes state rather than treating this historical implementation-head row as current-head proof.

## TDD Slice 52 Evidence

- T244 coverage disposition: failed Claude subscription slot `5ee4c34c-fd35-436a-9e51-10fd04480591` could not be counted as approval because it failed quality gating with `review_not_completed:permission_blocked`, but its raw non-blocking malformed `.specify/feature.json` coverage concern was source-valid. The verifier implementation already catches `json.JSONDecodeError`; `test_validate_docs_rejects_malformed_feature_json_without_crashing` locks that branch as regression coverage without changing production runtime behavior.

## Refactor Slice 53 Evidence

- T246 no-mistakes evidence: no-mistakes run `01KS7148C3EN2T61K4AFHE5RX1` on branch `codex/maker-order-proof-clean-05b58e83` advanced its review head to `6d363eca96543ca84bfa5eb1e1ca47780329317e`, commit `no-mistakes(review): Deduplicate NT order template mapping`. The review finding was `duplicated-order-template-mapping` at `src/strategies/binary_oracle_edge_taker.rs:5920`, severity `info`, because `BinaryOracleEdgeTakerOrderConfig::nt_order_template(...)` and `ExitOrderExecutionConfig::nt_order_template(...)` both copied the same NT template fields.
- T246 boundary decision: accept the dedupe as a behavior-preserving strategy-local refactor, not as a new architecture or policy layer. The shared `src/bolt_v3_order_intent.rs` module remains the only shared NT builder path and still has no strategy, venue, market, submit, admission, entry, or exit coupling. The generated no-mistakes source-count test was not adopted because source-shape counting would be brittle implementation coupling rather than behavior evidence.
- T247 refactor: `ConfiguredNtOrderTemplate` now owns the strategy-local raw configured NT template fields and converts them once into shared `NtOrderTemplate`. Entry, normal exit, forced exit, and `ExitSubmissionDecision::execution_config(...)` route through that same strategy-local template value before calling the existing shared `build_nt_order(...)` path. No runtime value is hardcoded and no order side, order type, TIF, post-only, reduce-only, quote-quantity, venue, market, or strategy policy moved into the shared builder.
- T248 local verification: `cargo test post_only_maker_order_objects_preserve_nt_limit_gtc_fields -- --nocapture`, `cargo test forced_flat_exit_order_object_uses_configured_forced_exit_template -- --nocapture`, `cargo test --test bolt_v3_order_intent shared_nt_order_template_source_has_no_strategy_venue_market_or_submit_coupling -- --nocapture`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo fmt -- --check`, `git diff --check`, `just source-fence` after expected cache-lock escalation, `cargo test --locked --quiet`, and `just clippy` passed locally at `2026-05-22 14:39:09 KST`. Post-push exact-head PR checks and no-mistakes state still need refresh for this follow-up head.

## Review Slice 54 Evidence

- T249 exact-head no-mistakes evidence: PR #434 head `b908fee733c9223a338c208fae95f5c8c092a8e9` was pushed to no-mistakes branch `codex/maker-order-proof-clean-b908fee7`. Run `01KS753Q244V27KCV8VC8JTNM1` reported finding `BOLT-V3-OMS-VALIDATION` at `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs:809`, claiming `binary_oracle_edge_taker` should fail closed to `Netting` because its current exposure lifecycle has proof only for one managed position.
- T250 NT source evidence: pinned NT defines `OmsType::{Unspecified, Netting, Hedging}` in `crates/model/src/enums.rs`, documents Hedging as supporting multiple positions by position/ticket ID or virtual tracking, validates submitted `position_id` shape only when the resolved OMS is `Netting` in `crates/execution/src/engine/mod.rs::check_position_id_against_oms`, and determines fill position ids through `determine_hedging_position_id` for `Hedging` while `Unspecified` defers to the venue/client OMS. NT's Netting validation message explicitly directs custom position IDs to Hedging. Adapter source also shows venue/market-specific OMS choices, for example OKX spot uses `Hedging` while derivatives use `Netting`.
- T251 Bolt source evidence: `binary_oracle_edge_taker` parses the configured NT `OmsType` into `StrategyConfig.oms_type`, submits entries without a custom `PositionId`, materializes the managed position from NT position events/fills, and submits exits with the managed `PositionId` through `SubmitContext::with_client_id_and_position_id(...)`. Existing regressions `bolt_v3_strategy_oms_type_accepts_nt_variants` and `strategy_core_accepts_nt_hedging_oms_type` lock that Bolt does not narrow NT's OMS enum at public validation or runtime strategy parsing.
- T252 external review disposition: Gemini job `340b6f61-9869-4250-92ed-cc2eb2757d05` and Claude subscription job `d9ed7dad-780c-4980-b8f3-9999576d5731` both reviewed the focused OMS packet and concluded a Netting-only guard is not source-justified; both noted live adapter-specific position behavior remains unproven, but treated that as an evidence gap rather than a reason to narrow NT's supported OMS values. Boundary decision: do not add a Bolt-only OMS allowlist or strategy-specific Netting guard without a proven runtime/accounting failure. Keep the shared order-intent layer and strategy config path NT-backed, venue-agnostic, market-agnostic, and strategy-agnostic.
- T253 local verification: `cargo test --test config_parsing bolt_v3_strategy_oms_type_accepts_nt_variants -- --nocapture`, `cargo test --lib strategy_core_accepts_nt_hedging_oms_type -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `just source-fence` after the expected rust-verification cache-lock escalation, and `cargo test --locked --quiet` passed locally. Post-push exact-head PR checks and no-mistakes state still need refresh for this docs-only disposition head.

## TDD Slice 55 Evidence

- T254 current-head review state: PR #434 head `c3c975d5a7c111fa51dc13f495d9c515c91f0dbd` was clean and GitHub checks were green before this follow-up. Gemini branch-diff review `da056ecb-e836-41d6-bfdb-afc91a4bac7d` failed with `usage_limited`; Kimi review `ed652a0d-ff52-493f-8146-d1a245dc00d8` failed quality gating with `review_quality_failed:shallow_output,missing_verdict`; Grok branch-diff review `job_da35c627-9b62-40f0-b70f-72095dd0b149` failed before source transmission because `src/strategies/binary_oracle_edge_taker.rs` exceeded its file-size limit. GLM custom review `job_0356fe00-d52c-48e2-89ec-5e83b632ede0` completed with `Verdict: APPROVE` and no blocking findings, but reported non-blocking direct-validator completeness, trigger/trailing direct-coverage, and diagnostic-coupling concerns. Claude subscription custom review `09b141d0-59e0-48d0-afe5-b96fa5c63a79` completed with `Verdict: APPROVE` and no blockers, but separately flagged the diagnostic-coupling and unsupported-variant direct-builder coverage gaps. The earlier no-mistakes run `01KS76HGS1D4G112XJWHC8BP5H` on branch `codex/maker-order-proof-clean-c3c975d5` reviewed `c3c975d5` and logged no findings, but `no-mistakes status` still reported the run active at `2026-05-22 15:56:12 KST`; post-push no-mistakes state must be refreshed for this follow-up commit.
- T255 RED/GREEN: `cargo test --test bolt_v3_order_intent direct_nt_order_template_validation_rejects_market_like_post_only_orders -- --nocapture` first failed because direct `validate_nt_order_template(...)` returned `Ok(())` for post-only `Market`. The GREEN change moves `Market`, `StopMarket`, and `MarketIfTouched` post-only rejection into `validate_nt_order_template(...)` and removes the duplicate builder-branch rejection for those variants. `cargo test --test bolt_v3_order_intent direct_nt_order_template_validation_rejects_market_like_post_only_orders -- --nocapture` then passed.
- T257/T258 RED/GREEN: `cargo test --test bolt_v3_order_intent shared_nt_order_template_source_has_no_strategy_venue_market_or_submit_coupling -- --nocapture` first failed after adding `parameters.` to the shared-module source fence, proving `src/bolt_v3_order_intent.rs` hardcoded archetype TOML diagnostic paths. The GREEN change keeps full TOML paths in `binary_oracle_edge_taker`'s archetype-owned caller and passes them into `check_nt_order_template_config(...)`, leaving the shared order-intent module free of archetype path literals. Direct shared-builder coverage now also proves configured `trigger_type` and `trailing_offset_type` pass through NT factories and that `MarketToLimit`/`TrailingStopLimit` fail closed at the pinned NT `OrderFactory` boundary.
- T259 local verification: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo test --test config_parsing bolt_v3_archetype_rejects_unsupported_nt_order_type_variants -- --nocapture`, `cargo test --test config_parsing bolt_v3_archetype_rejects_market_if_touched_entry_post_only -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 16:10:00 KST`. Exact-head PR checks, no-mistakes, and external reviewer state must be refreshed after commit and push.

## TDD Slice 56 Evidence

- T260 current-head review state: PR #434 head `4f153d1ba1e15f3519d665c0c4a8fe7cc46532c9` had green GitHub checks before this follow-up. Gemini branch-diff review `03b918b9-0580-412b-983a-285324ff0d8c` completed with `Verdict: APPROVE` and no findings. Claude subscription branch-diff review `6367c932-2330-4e5b-a708-7f4643267ff3` completed with `Verdict: APPROVE` but reported non-blocking direct-validator unsupported factory-gap, duplicate TrailingStopMarket post-only guard, and hardcoded unsupported-builder diagnostic concerns. Kimi branch-diff review `14dd3281-17f6-4224-a1a8-6f1f078a86e5` completed with `Verdict: APPROVE with non-blocking concerns` and separately flagged the hardcoded supported-variant diagnostic list plus duplicate TrailingStopMarket guard. GLM direct API review `job_bde04e26-6ca6-4a50-8bf1-cb18050e291e` did not run because the plugin required explicit approval; selected source was not sent. The no-mistakes run `01KS78RAKEM1CEKHQ07FZA9MVW` was still active on branch `codex/maker-order-proof-clean-4f153d1b` when Phase 60 began.
- T261/T263 RED/GREEN: `cargo test --test bolt_v3_order_intent factory_gap -- --nocapture` first failed because direct `validate_nt_order_template(...)` returned `Ok(())` for `MarketToLimit` and the builder catch-all still emitted a hardcoded supported-type list. The GREEN change rejects `MarketToLimit` and `TrailingStopLimit` in `validate_nt_order_template(...)` with the same pinned NT single-order `OrderFactory` boundary message used by the builder catch-all. `cargo test --test bolt_v3_order_intent factory_gap -- --nocapture` then passed.
- T262/T263 RED/GREEN: `cargo test --test bolt_v3_order_intent shared_nt_order_template_directly_rejects_trailing_stop_market_post_only_once -- --nocapture` first failed because the TrailingStopMarket post-only error string appeared twice in `src/bolt_v3_order_intent.rs`. The GREEN change leaves TrailingStopMarket post-only rejection in direct validation and removes the unreachable duplicate builder guard. `cargo test --test bolt_v3_order_intent shared_nt_order_template_directly_rejects_trailing_stop_market_post_only_once -- --nocapture` then passed.
- T264 local verification evidence: `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 16:37:21 KST`. Exact-head PR checks, reviewer state, and no-mistakes state must be refreshed after commit and push.

## TDD Slice 57 Evidence

- T265 review state: no-mistakes run `01KS79XP977GZT8TQC1FGY3YFJ` on branch `codex/maker-order-proof-clean-374803f2` advanced its gate head to review commit `4688ccb43bf64398b751e911e494a81fb8b19b28`, `no-mistakes(review): Bind Phase 8 envelope oms_type`. Gemini exact-diff review `2fbe1633-6887-4ac8-a691-f180f632e9dc` completed with source sent and `Verdict: APPROVE` with no findings. Claude subscription review attempts `564e92a4-6451-4620-8c57-2080e344e86f` and `5b5c782e-cf2e-4317-8190-f4bee5c53140` sent source but failed to produce a usable review because the subscription session limit was reached. Kimi exact-diff review `44350bc6-625b-478a-b39c-7eff45ccb099` sent source but failed quality audit for shallow output; Kimi narrowed custom review `b894386a-afb9-43b4-b318-585e6485087f` completed with source sent, `Verdict: APPROVE`, no blockers, and minor concerns about direct config-checker coverage, validation-split maintenance risk, and source-string assertion brittleness.
- T266/T267 RED/GREEN: `env CARGO_TARGET_DIR=/tmp/bolt-v2-maker-oms-red-target cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` first failed with `strategy OMS type mismatch against loaded TOML should fail closed: ()`, proving Phase 8 approval consumption did not bind loaded NT `oms_type`. The GREEN change adds `oms_type` to `Phase8FinancialEnvelopeEvidenceFile`, derives it from the loaded NT enum display value, compares it during financial-envelope validation, and updates test financial-envelope JSON from the loaded fixture rather than a literal OMS policy value. `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` then passed.
- T268 GREEN: `cargo test --test bolt_v3_order_intent shared_nt_order_template_config_rejects_unsupported_factory_gap_variants -- --nocapture` passed, directly covering `check_nt_order_template_config(...)` rejection for `MarketToLimit` and `TrailingStopLimit` at the pinned NT single-order `OrderFactory` boundary.
- T269 local verification evidence: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 17:04:22 KST`. Exact-head PR checks, reviewer state, and no-mistakes state must be refreshed after commit and push.

## TDD Slice 58 Evidence

- T270 current-head review state: PR #434 head `940ab4e663c8ea4c4fd2eeb7dd6019cbb570a35e` was clean and GitHub checks passed before this follow-up. Gemini exact-diff review `cbe5518a-6696-471f-b807-2e78e9daac17` completed with source sent and `Verdict: APPROVE` with no findings. Kimi exact-diff review `1d8f1394-fe5a-4171-895a-56d0b485cbb0` completed with source sent and `Verdict: APPROVE`, no blockers, and non-blocking concerns about Phase 8 OMS case-sensitivity, validation-split maintenance risk, and source-fence tokenization. Disposition: fix the OMS canonicalization concern because it is a real NT-backed comparison gap; no production change for the validation-split concern because the explicit pinned-NT gap list is already centralized in direct validation while config and builder fail closed on unhandled variants; no production change for the Entry/Exit source-fence concern because exact-token failure is intentional architecture protection against entry/exit coupling in the shared order-intent module. Claude subscription review remained unavailable until the session-limit reset.
- T271 RED: `CARGO_TARGET_DIR=/tmp/bolt-v2-maker-oms-case-red2-target cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` failed with `financial envelope should canonicalize OMS through NT parsing: phase8 financial envelope 'oms_type' does not match loaded TOML`, proving the approval envelope compared raw OMS text rather than NT-equivalent enum values.
- T272 GREEN: `Phase8FinancialEnvelopeEvidenceFile::validate_matches(...)` now parses the approved envelope `oms_type` through NautilusTrader `OmsType`, converts the parsed enum through the same lowercase canonical display helper used for loaded TOML, and compares the canonical values. Unknown OMS strings fail closed before approval consumption with a Phase 8 financial-envelope diagnostic.
- T273 docs/runtime-literal update: the active schema now states that Phase 8 approval-envelope validation canonicalizes `oms_type` through NautilusTrader `OmsType`; the new invalid-OMS diagnostic remains verifier-ignored diagnostic text because it contains diagnostic wording and is not a runtime value.
- T274 local verification evidence: `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `just source-fence` after the expected rust-verification cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 17:29:35 KST`. Exact-head PR checks, reviewer state, and no-mistakes state must be refreshed after commit and push.

## Review Slice 59 Evidence

- T275 current-head review state: Gemini exact-diff review `85faeb82-16a4-437c-ba46-782f86b22e34` on `940ab4e6..e5c8f0f6` completed with source sent, `Verdict: APPROVE`, and no findings. Kimi exact-diff review `4cf3d2f2-ff2e-4079-b553-79c7842cce64` completed with source sent, `Verdict: APPROVE`, no blockers, and non-blocking concerns about missing invalid-OMS regression coverage, `alternate_oms_type(...)` handling of `OmsType::Unspecified`, and T274 task state. Disposition: add invalid-OMS coverage; directly disprove the helper concern with an all-current-NT-variants helper regression; leave T274 unchecked because it explicitly includes exact-head PR/no-mistakes evidence that is still pending.
- T276 invalid-OMS coverage: `operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config` now mutates the approved financial envelope to an OMS value derived from the loaded approved OMS plus an invalid suffix, recomputes the envelope hash, and asserts validation fails closed with no approval-consumption evidence.
- T277 helper disproof coverage: `phase8_oms_alternate_helper_covers_nt_oms_variants` proves `alternate_oms_type(...)` returns a non-approved alternate for `OmsType::Netting`, `OmsType::Hedging`, and `OmsType::Unspecified`. `cargo test --test bolt_v3_single_submit_preconditions phase8_oms_alternate_helper_covers_nt_oms_variants -- --nocapture` passed before any helper change, proving Kimi's helper-panic concern was not a current-code bug.
- T278 local verification evidence: `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions phase8_oms_alternate_helper_covers_nt_oms_variants -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, and `just source-fence` passed locally at `2026-05-22 17:40:27 KST`. Exact-head PR/reviewer/no-mistakes state must be refreshed after commit and push.
- T264/T269/T274/T278 final exact-head refresh: local `HEAD`, `origin/codex/maker-order-proof-clean`, `refs/pull/434/head`, and no-mistakes gate ref `codex/maker-order-proof-clean-c5bbec6c` all resolved to `c5bbec6ce0a4db7ac2b2ac13163ede1a41d5bcfc` at `2026-05-22 17:57:57 KST`. `git status --short --branch` reported a clean branch tracking `origin/codex/maker-order-proof-clean` before this documentation-only evidence update. `gh pr view 434 --json headRefOid,mergeStateStatus,mergeable,isDraft,reviewDecision,headRefName,baseRefName` reported head `c5bbec6ce0a4db7ac2b2ac13163ede1a41d5bcfc`, base `main`, `isDraft=false`, `mergeStateStatus=CLEAN`, and `mergeable=MERGEABLE`. `gh pr checks 434` passed Analyze actions/rust, CodeQL, actionlint, actionlint-annotations, build, check-aarch64, clippy, deny, detector, fmt-check, gate, nextest archive, nextest shards 1-4, source-fence, and test, with only same-sha-main-evidence and deploy skipped. Gemini exact-diff review `5d068cbb-1397-401e-9531-4c2ada646cdc` on `e5c8f0f6..c5bbec6c` completed with source sent, `Verdict: APPROVE`, and no blockers or non-blocking concerns. Kimi exact-diff review `4c1d3e5f-86f0-4e57-b774-4b85f7cbd2cd` on the same exact diff completed with source sent, `Verdict: APPROVE`, no blockers, and only non-blocking future-proofing/test-shape notes. `no-mistakes status` still reported run `01KS7DHSRBMJJCTK3W7SB2BGGW` on branch `codex/maker-order-proof-clean-c5bbec6c`, status `running`, head `c5bbec6c`, started `2026-05-22 17:41:38`; after the 15-minute wait cap, `git ls-remote no-mistakes refs/heads/codex/maker-order-proof-clean-c5bbec6c` still returned `c5bbec6ce0a4db7ac2b2ac13163ede1a41d5bcfc`, and the daemon log had no post-intent review commit or finding after `2026-05-22 17:42:05 KST`. This records no-mistakes as refreshed non-terminal evidence, not as a daemon approval.

## TDD Slice 60 Evidence

- T279 no-mistakes evidence: after the documentation-only refresh commit `2672a006fecc81c37d310f8f9768ea3871604a5c`, the local no-mistakes gate branch `codex/maker-order-proof-clean-2672a006` advanced to review commit `386850dc581fb2d8e9c07ec61e52468812c49700`, `no-mistakes(review): Canonicalize phase8 order envelope enums`, changing only `src/bolt_v3_single_submit_evidence.rs` and `tests/bolt_v3_single_submit_preconditions.rs`. The finding is accepted as a Phase 8 approval-envelope gap because `oms_type` was canonicalized through NT parsing while entry, exit, and forced-exit order enum fields were still compared as raw strings. The generated patch shape was not adopted verbatim because its tests introduced concrete order enum values; this slice keeps regression inputs derived from existing fixture values and uses schema field names only.
- T280 RED: `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` first failed with the expected `entry_side` mismatch diagnostic, proving the approved envelope rejected an NT-equivalent non-identical spelling before the production change. The regression mutates existing approved and loaded order enum strings by transformation, not by hardcoded enum value literals, and mutates the invalid case by appending an invalid suffix to an existing approved enum string.
- T281 GREEN: `Phase8FinancialEnvelopeEvidenceFile` now canonicalizes loaded TOML order enum fields and approved financial-envelope order enum fields through pinned NautilusTrader `OrderSide`, `PositionSide`, `OrderType`, `TimeInForce`, `TriggerType`, and `TrailingOffsetType` parsing before comparison. Unknown approved strings fail closed before approval consumption with a Phase 8 financial-envelope diagnostic. No venue, market, strategy, maker-only, taker-only, or Bolt-owned enum allowlist policy was added.
- T281 focused verification: after the GREEN change and formatting, `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture` passed locally at `2026-05-22 19:14:00 KST`.
- T282 local verification before commit: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test --test bolt_v3_single_submit_preconditions operator_approval_envelope_verifies_financial_envelope_hash_and_loaded_config -- --nocapture`, `cargo test --test bolt_v3_single_submit_preconditions -- --nocapture`, `just source-fence` after expected sandbox cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 19:20:26 KST`. Commit/push, exact-head PR checks, external reviewer state, and no-mistakes state still need refresh for this head.
- T282 post-push refresh: PR #434 head `49b6ffa1d28faa751b6df9a353d8a8f596fb7422` had green GitHub checks at `2026-05-22 19:30:57 KST`. Gemini exact-diff review `80ccbf79-1a6c-418f-9a46-6cf86467fe9a` completed with `Verdict: APPROVE` and no findings. Claude subscription exact-diff review `343dab69-6fcd-41bf-9a23-8688fd852227` completed with `Verdict: APPROVE`, no blockers, and only non-blocking diagnostic/maintenance notes. Kimi retry `8da3d3ef-c598-4240-8330-2fb95dbff199` completed with `Verdict: APPROVE`, no blockers, and one non-blocking optional-field positive-coverage note; an earlier Kimi run `c7a74ad8-2421-40bd-8210-e30ebbcb94fa` sent source but failed the plugin quality gate for missing the required verdict marker. The no-mistakes run `01KS7K8G180AA068A7AWQ7BVDK` on branch `codex/maker-order-proof-clean-49b6ffa1` advanced to review commit `c4adfa05cd063aed3ace7028010bf0015535db6c`, so T282 remains open and Phase 65 tracks the new admission evidence finding.

## TDD Slice 61 Evidence

- T283 no-mistakes evidence: the run `01KS7K8G180AA068A7AWQ7BVDK` advanced the local no-mistakes gate branch `codex/maker-order-proof-clean-49b6ffa1` from `49b6ffa1d28faa751b6df9a353d8a8f596fb7422` to review commit `c4adfa05cd063aed3ace7028010bf0015535db6c`, `no-mistakes(review): Use activation price for admission evidence`, changing `src/bolt_v3_decision_evidence.rs` and `src/strategies/binary_oracle_edge_taker.rs`. Pinned NautilusTrader is `7c2aafb30fb143069c915a3f2057bb12174405f6`; its order trait exposes `activation_price()` as a separate order field, and `OrderFactory::trailing_stop_market(...)` accepts `activation_price`, falls back to it for the initial trigger when `trigger_price` is absent, and then preserves `activation_price` on the order. The accepted fix is a generic compiled-order price-source selector over NT `Order` fields, not a strategy, venue, market, maker/taker, or order-type policy.
- T284 RED: `cargo test compiled_order_price_source_prefers_activation_price_before_fallback -- --nocapture` first failed to compile with `cannot find function selected_compiled_order_price_source in this scope`, proving there was no central selector covering the activation-price-before-fallback case.
- T285 GREEN: `BoltV3OrderIntentEvidence::from_compiled_order(...)` and both runtime/test submit-admission helpers now use one `compiled_order_price_source(...)` selector with precedence `price -> trigger_price -> activation_price -> fallback`. The existing TrailingStopMarket activation-only exit fixture now also asserts submit-admission notional is derived from the compiled activation price retained by the NT order. No hardcoded venue, market, strategy, maker-only, taker-only, or order-type admission policy was added.
- T285 focused verification: `cargo test compiled_order_price_source_prefers_activation_price_before_fallback -- --nocapture` and `cargo test trailing_stop_market_order_objects_preserve_nt_trailing_fields_and_admission -- --nocapture` passed locally at `2026-05-22 19:41:42 KST`.
- T286 local verification before commit: after formatting, `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test compiled_order_price_source_prefers_activation_price_before_fallback -- --nocapture`, `cargo test trailing_stop_market_order_objects_preserve_nt_trailing_fields_and_admission -- --nocapture`, `just source-fence` after the expected cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 19:45:58 KST`. Commit/push, exact-head PR checks, reviewer state, and no-mistakes state still need refresh for this head.

## TDD Slice 62 Evidence

- T287 no-mistakes evidence: after Phase 65 commit `2ae587058ed2b394afaa5173da344fe586918437`, no-mistakes run `01KS7MR1FBSKQ8DZ1Q6K6DQCN0` advanced the gate branch `codex/maker-order-proof-clean-2ae58705` to review commit `d537b295cac29fa2df09cf49bf4f79d2190971a7`, `no-mistakes(review): Sync financial envelope schema fields`, changing only `docs/bolt-v3/2026-04-25-bolt-v3-schema.md`, `scripts/test_verify_bolt_v3_schema_current.py`, and `scripts/verify_bolt_v3_schema_current.py`. Source inspection accepted the finding: `src/bolt_v3_single_submit_evidence.rs` already defines `Phase8FinancialEnvelopeEvidenceFile` with entry, exit, and forced-exit side, position side, order type, time-in-force, optional expiry, trigger, activation, trigger type, trigger instrument, trailing offset, trailing offset type, post-only, reduce-only, and quote-quantity fields, but the active schema doc named only entry/exit order type, time-in-force, and boolean fields. The accepted fix is a source-derived documentation guard over the Rust struct, not a new static field allowlist.
- T288 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` first failed with `AttributeError: module 'verify_bolt_v3_schema_current' has no attribute 'extract_phase8_financial_envelope_fields'` after adding self-tests for source-derived `Phase8FinancialEnvelopeEvidenceFile` extraction and missing-field schema rejection, proving the active verifier had no source-derived financial-envelope schema guard.
- T289 GREEN: `scripts/verify_bolt_v3_schema_current.py` now derives financial-envelope field names from the Rust `Phase8FinancialEnvelopeEvidenceFile` source struct and checks that the active schema's `financial_envelope` section names every source field. The active schema now names the entry, exit, and forced-exit side, position side, order type, time-in-force, optional expiry, trigger, activation, trigger type, trigger instrument, trailing offset, trailing offset type, post-only, reduce-only, and quote-quantity fields. `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` passed locally.
- T290 local verification before commit: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test compiled_order_price_source_prefers_activation_price_before_fallback -- --nocapture`, `cargo test trailing_stop_market_order_objects_preserve_nt_trailing_fields_and_admission -- --nocapture`, `just clippy`, `cargo test --locked --quiet`, and `just source-fence` after expected sandbox cache-lock escalation passed locally at `2026-05-22 20:08:28 KST`. Commit/push, exact-head PR checks, reviewer state, and no-mistakes state still need refresh for this head.

## TDD Slice 63 Evidence

- T291 Kimi review evidence: exact-head Kimi review job `552bd436-e629-4ffd-877d-765163b94cb1` for diff `2ae587058ed2b394afaa5173da344fe586918437..30a71390ca8e0620f3a16afe06c630cd1195cdb1` returned `Verdict: APPROVE` but raised a non-blocking substantive finding: `scripts/verify_bolt_v3_schema_current.py` checked every Rust `Phase8FinancialEnvelopeEvidenceFile` field was present in the active schema doc, but did not reject stale doc-only `financial_envelope` fields absent from the Rust source. The accepted root fix is exact source-vs-doc field-set validation, not a static allowlist.
- T292 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `AssertionError: expected extra doc field finding, got []` after adding `test_validate_docs_rejects_financial_envelope_schema_extra_doc_field`, proving the Phase 66 guard still accepted stale doc-only `financial_envelope` fields.
- T293 GREEN: `scripts/verify_bolt_v3_schema_current.py` now parses documented field names from the bullet prefixes in the active `financial_envelope` section and compares that documented set against the Rust source-derived `Phase8FinancialEnvelopeEvidenceFile` field set in both directions. This keeps the guard source-derived while rejecting both missing source fields and unknown doc-only fields. `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` passed locally.
- T294 local verification before commit: `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo fmt -- --check`, `cargo test compiled_order_price_source_prefers_activation_price_before_fallback -- --nocapture`, `cargo test trailing_stop_market_order_objects_preserve_nt_trailing_fields_and_admission -- --nocapture`, `just source-fence` after expected sandbox cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 20:44:36 KST`. Commit/push, exact-head PR checks, reviewer state, and no-mistakes state still need refresh for this head.

## TDD Slice 64 Evidence

- T295 no-mistakes document evidence: no-mistakes run `01KS7R2RW346RXZNRY6QH6F9FV` on branch `codex/maker-order-proof-clean-71ce1d37` first produced an OMS warning that had already been source-disproved by T249-T253, then passed `cargo test` with `all tests passed`, then paused in `Document` with five documentation findings. The accepted findings were active-doc drift for decision-evidence JSONL schema v4/order fields, runtime order-submission field lists, status-map order-construction source evidence, stale Phase 022 forced-exit text, and stale Phase 022 GTD rejection text. The accepted fix is docs/verifier coverage only; no runtime path, NT order construction path, venue policy, market policy, strategy policy, maker-only policy, or taker-only policy changes.
- T296 RED: `python3 scripts/test_verify_bolt_v3_schema_current.py` first failed with `TypeError: validate_docs() got an unexpected keyword argument 'runtime_contracts'` after adding `test_validate_docs_rejects_decision_evidence_and_maker_scope_doc_drift`, proving the active verifier did not cover runtime-contract or Phase 022 maker-scope doc drift. After wiring the verifier, `python3 scripts/verify_bolt_v3_schema_current.py` failed against current docs with missing JSONL schema v4 contract, missing runtime order-template evidence fields, missing forced-exit builder status evidence, and stale Phase 022 forced-exit/GTD phrases.
- T297 GREEN: `scripts/verify_bolt_v3_schema_current.py` now checks the active schema doc for decision-evidence JSONL schema v4, checks runtime contracts for compiled NT order-template evidence fields, checks the status map for the shared `src/bolt_v3_order_intent.rs` entry/exit/forced-exit builder path, and checks Phase 022 maker-scope docs for stale forced-exit/GTD superseded text. The affected docs now document `order_intent.order_fields`, runtime order-template evidence fields, forced-exit builder evidence, and Phase 022 supersession to the active Phase 023 order-intent layer. `python3 scripts/test_verify_bolt_v3_schema_current.py` and `python3 scripts/verify_bolt_v3_schema_current.py` passed locally at `2026-05-22 21:24:50 KST`.

## TDD Slice 65 Evidence

- T299 no-mistakes review evidence: exact-head run `01KS7TJSJXK2VPMC6AC46PGHPT` on branch `codex/maker-order-proof-clean-3840caa9` reported two findings. The `oms-type-widening` finding repeats the earlier Netting-only narrowing request already rejected by T249-T253 with pinned NT source, focused regressions, and Gemini/Claude review evidence; no new source evidence was supplied, so no Bolt-only OMS allowlist is added. The `strategy-schema-version-not-bumped` finding is accepted because `SUPPORTED_STRATEGY_SCHEMA_VERSION` remained `1` while the current strategy schema requires `[parameters.forced_exit_order]` and has removed the old top-level market-exit order-shape fields.
- T300 RED: `cargo test --test config_parsing rejects_previous_strategy_schema_version_after_forced_exit_order_schema_update -- --nocapture` failed with `expected previous strategy schema version to be rejected, got: []`, proving strategy schema version `1` still passed validation. `python3 scripts/test_verify_bolt_v3_schema_current.py` also failed with `TypeError: validate_docs() got an unexpected keyword argument 'validate_source'` after adding a source-backed schema-current regression for stale strategy schema examples.
- T301 GREEN: `SUPPORTED_STRATEGY_SCHEMA_VERSION` is now `2`, the current strategy fixture and active strategy schema examples use `schema_version = 2`, root schema examples remain `schema_version = 1`, and `scripts/verify_bolt_v3_schema_current.py` derives the supported strategy schema version from `src/bolt_v3_validate.rs` before checking strategy examples in the active schema doc. This versions the strategy-file shape only and adds no venue, market, strategy, maker-only, or taker-only policy. `cargo test --test config_parsing rejects_previous_strategy_schema_version_after_forced_exit_order_schema_update -- --nocapture` and `python3 scripts/test_verify_bolt_v3_schema_current.py` passed locally at `2026-05-22`.
- T302 local verification before commit: `cargo test --test config_parsing schema_version -- --nocapture`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo fmt -- --check`, `git diff --check`, `cargo test --test config_parsing -- --nocapture`, `just source-fence` after expected sandbox cache-lock escalation, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22`. Commit/push, exact-head PR checks, external reviewer state, and no-mistakes state still need refresh for this head.

## TDD Slice 66 Evidence

- T303 no-mistakes review evidence: exact-head run `01KS7VGCW8RMP17DCFVZZB1V5P` on branch `codex/maker-order-proof-clean-774ccf8c` reported `invalid-strategy-example-schema-version` because `config/strategies/binary_oracle.example.toml` still declared strategy `schema_version = 1`, and `decision-evidence-schema-doc-mismatch` because active docs described a `record_type` envelope while `src/bolt_v3_decision_evidence.rs` serializes `kind` plus `intent` or `decision`.
- T304 RED: `cargo test --test config_parsing shipped_binary_oracle_example_uses_supported_strategy_schema_version -- --nocapture` failed with `left: 1 right: 2`, proving the shipped example lagged the supported strategy schema. `python3 scripts/test_verify_bolt_v3_schema_current.py` failed with `expected stale record_type wording finding, got []`, proving the schema-current verifier did not reject stale decision-evidence envelope wording.
- T305 GREEN: the shipped binary-oracle strategy example now declares `schema_version = 2`, the active schema doc describes the actual decision-evidence JSONL envelope as `schema_version`, `recorded_at_utc_ns`, `gate_version`, `gate_id`, `kind`, and either `intent` or `decision`, and the schema-current verifier now rejects stale `record_type` wording while requiring the source-backed `kind`/payload wording. `cargo test --test config_parsing shipped_binary_oracle_example_uses_supported_strategy_schema_version -- --nocapture`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, and `python3 scripts/verify_bolt_v3_schema_current.py` passed locally.
- T306 local verification: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo test --test config_parsing schema_version -- --nocapture`, `cargo test --test config_parsing -- --nocapture`, `just source-fence`, `just clippy`, and `cargo test --locked --quiet` passed locally. `just source-fence` first hit the sandboxed rust-verification cache lock under `~/.cache`; rerunning with cache-lock access passed. Branch cleanliness, exact-head PR checks, reviewer state, and no-mistakes state remain pending until this slice is committed and pushed.

## TDD Slice 67 Evidence

- T307 review state: after Phase 70 commit `5d861172d0b9c8b6264a54ca84d6ba0125cfdd3d`, GitHub PR checks were green and PR #434 reported `mergeStateStatus=CLEAN`. Claude subscription review attempts `e2c2d76c-46fa-48d5-af2e-01f371e3f24e` and `d7959a3f-da40-437a-ab28-a63aa958d811` sent source but failed with `parse_error` / `Session ID ... is already in use`, so they produced no usable current-head review. Gemini plugin review attempts `be9d0b64-5da5-4038-b97c-406221ba39a2` and `d7452563-f561-46ba-8ad1-a15edfea7def` sent source but failed as `usage_limited`; `gemini doctor` still reported subscription OAuth ready, so the user relayed the same review prompt manually. Kimi review `2844b7e5-12ce-4c84-84cc-8c47a2ee83e3` sent source but failed with `step_limit_exceeded`. Grok docs/spec/verifier shard `job_dd524456-1fe0-42e5-8def-2149d417c025` completed with `Verdict: APPROVE` and no blockers; Grok source shard `job_b6579e79-2fe2-46d0-943b-f772d61eaa7e` returned raw `APPROVE` text but failed the plugin quality gate for `not_reviewed` scope language, so it is recorded as raw non-terminal reviewer evidence only. no-mistakes still listed `codex/maker-order-proof-clean-5d861172` at head `5d861172` as `running`, not terminal approval.
- T308 RED: the manual Gemini relay reported an all-targets clippy blocker for `Price::from_raw(price.raw - price.raw, price.precision)` plus boolean assertion comparisons and unit let-bindings. `cargo clippy --test bolt_v3_order_intent -- -D warnings` reproduced the primary blocker with `clippy::eq_op` at `tests/bolt_v3_order_intent.rs:99`. After the first mechanical zero-helper change, the same command exposed a raw-type mismatch; after correcting the helper to use the inferred raw default, the focused command passed. `cargo clippy --all-targets -- -D warnings` then failed with three `clippy::bool_assert_comparison` errors in `src/bolt_v3_decision_evidence.rs` test code and two `clippy::let_unit_value` errors in `src/strategies/binary_oracle_edge_taker.rs` tests.
- T309 GREEN: `zero_price()` now constructs the NT raw zero with the inferred raw type default rather than equal-operand subtraction; decision-evidence test assertions use `assert!(!...)`; the two strategy test helper calls no longer bind unit values. These are test-only clippy fixes and do not change order-intent runtime behavior, NT `OrderFactory` delegation, venue policy, market policy, strategy policy, maker-only policy, or taker-only policy.
- T310 local verification before commit: `cargo fmt -- --check`, `git diff --check`, `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo clippy --all-targets -- -D warnings`, `cargo test --test bolt_v3_order_intent -- --nocapture`, `cargo test --lib bolt_v3_decision_evidence -- --nocapture`, `cargo test --lib forced_flat_exit_order_object -- --nocapture`, `just source-fence`, `just clippy`, and `cargo test --locked --quiet` passed locally at `2026-05-22 23:11:26 KST`. The first sandboxed `cargo clippy --all-targets -- -D warnings` and `just source-fence` attempts failed only on `/Users/spson/.cache/rust-verification/bolt-v2/cache.lock`; rerunning with cache-lock access passed. Commit/push, branch cleanliness, exact-head PR checks, reviewer state, and no-mistakes state still need refresh for this head.

## TDD Slice 68 Evidence

- T311 exact-head review and follow-up issue evidence: no-mistakes completed run `codex/maker-order-proof-clean-408ae365-rerun` for exact PR #434 head `408ae3656582932eb9620249b394c56c794a1649` and opened PR #450. Its embedded Greptile summary reported that `expected_position_side_for_entry_order`, `expected_exit_order_side_for_position`, and `is_observed_open_side` were duplicated in `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` and `src/strategies/binary_oracle_edge_taker.rs`. Current PR #434 review-thread inspection showed the older forced-flat thread is resolved and current source has forced-flat checks before the managed pending-entry guard, so the accepted source-proven finding for this slice is the duplicate helper path only. The generic admission/submission wrapper remains a separate architecture follow-up; GitHub issue #451 was created as P1 with `architecture` and `bolt-v3` labels.
- T312 RED: `python3 scripts/verify_bolt_v3_schema_current.py` first failed after adding the source-derived position-contract helper guard. It reported no shared helper source and local duplicate definitions in both the archetype and strategy modules for all three helpers, proving the current source still had two position-contract helper paths.
- T313 GREEN: added `src/bolt_v3_position_contract.rs` as the single neutral helper source for `expected_position_side_for_entry_order`, `expected_exit_order_side_for_position`, and `is_observed_open_side`; exported it from `src/lib.rs`; and imported those helpers into both `src/bolt_v3_archetypes/binary_oracle_edge_taker.rs` and `src/strategies/binary_oracle_edge_taker.rs`. This removes the duplicate helper definitions without changing order construction, admission, venue policy, market policy, strategy policy, maker-only policy, or taker-only policy.
- T314 local verification before commit: `python3 scripts/test_verify_bolt_v3_schema_current.py`, `python3 scripts/verify_bolt_v3_schema_current.py`, `python3 scripts/test_verify_bolt_v3_runtime_literals.py`, `python3 scripts/verify_bolt_v3_runtime_literals.py`, `cargo fmt -- --check`, `git diff --check`, `cargo test position_contract -- --nocapture`, `just source-fence`, `just clippy`, and `cargo test --locked --quiet` passed locally. Branch cleanliness, exact-head PR checks, reviewer state, and no-mistakes state still need refresh after commit and push.
