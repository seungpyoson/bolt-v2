# Tasks: Research Analytics

- [ ] RA-001 Gate-0: wire durable + immutable S3 write into the existing venue converters and add config-driven venue dispatch so all converted NT catalog data lands in the canonical S3 artifact_root instead of a local reproduce-on-demand path. Durability/immutability is WE-BUILD via the conditional create-only writer over object_store's PutMode::Create (S3 If-None-Match) per reference/normalization-catalog-plan.v3.md — NT's own write_to_parquet is non-atomic head()+PutMode::Overwrite, not a create-only facility. Read access stays NT (from_uri S3).
- [ ] RA-002 Define point-in-time feature join rules and leakage fixtures.
- [ ] RA-003 Implement leakage-check fixtures that fail closed on future-data access.
- [ ] RA-004 Implement a thin reader helper (~dozens LOC) over NT typed query<T> (instrument + time + SQL where pushdown) and DataBackendSession (DataFusion SQL → Arrow) to expose catalog data to research notebooks without duplicating NT's query surface.
- [ ] RA-005 Define notebook read-only boundary and prohibited runtime actions.
- [ ] RA-006 Add a Cython-engine-import boundary test that fails if any notebook or research module imports `nautilus_trader.backtest.engine` or `.node`; the ONE Rust BTE is the only permitted backtest runner.
- [ ] RA-007 Lift the lead-lag measurement lane off raw archive reads and onto NT catalog reads once Gate-0 (RA-001) is complete; document the raw-archive fallback sunset tied to issue #677 (converter ts_init = capture_time).
- [ ] RA-008 Implement sweep orchestration: write N typed run-spec TOMLs, invoke the existing BTE entrypoint in sequence — each run also supplies the accepted object bytes whose sha256 the run-spec pins (operator::run_from_run_spec(spec, gz_bytes, output_dir); CLI --object-gz), so it is not "TOML in, result out" — and collect each persisted BacktestResultContract per run; RA owns no runner of its own. (NT's in-process BacktestResult is #[derive(Debug)]-only and never persisted; RA, out-of-process, reads the Contract JSON the entrypoint writes.)
- [ ] RA-009 Implement Polymarket cost realism as FeeModel / FillModel / LatencyModel trait impls registered with the BTE; these are the first sweep inputs that reflect real venue economics.
- [ ] RA-010 Implement domain metrics as PortfolioStatistic trait impls registered via register_statistic BEFORE the run and run IN-PROCESS during the backtest (the trait methods take live &Returns / Vec<Box<dyn Order>> / &[Position] per crates/analysis/src/statistic.rs, not the flat stat maps the persisted BacktestResultContract carries — do not re-run statistics over the persisted contract). Post-hoc domain metrics are computed from the Contract aggregates, or require the BTE to explicitly export per-period returns / OrderFilled / PositionOpened series. Do not duplicate metrics already present in NT PortfolioAnalyzer.
- [ ] RA-011 Implement a thin run-id → params → result-pointer index over catalog.list_backtest_runs; single artifact_root + sha256 content hashing; no lifecycle metadata or promotion state.
- [ ] RA-012 Wire off-the-shelf BI surface: duckdb / polars reads over NT catalog Arrow output (NT reporter.py / tearsheet.py are Cython-kernel-only — they need a live engine.kernel.cache + list[Order]/list[Position] the single-engine invariant bans, so they are NOT on the Rust path and are excluded); add notebook ergonomics for the dashboard product; custom UI only if off-the-shelf cannot satisfy a confirmed requirement.
- [ ] RA-013 Add a test that analytics venue/provider resolution and feature joins do not branch on hardcoded venue/provider names; one test, not a >=2-binding fixture obligation.
- [ ] RA-014 Document the known prerequisite: the BTE runner today registers only an NT example strategy (HurstVpinDirectional) over one venue (bybit-spot); bolt's binary_oracle_edge_taker + venue normalization must be wired into the BTE before Phase-3 sweeps produce valid results.
