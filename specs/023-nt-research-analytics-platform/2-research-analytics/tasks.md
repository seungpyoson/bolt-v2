# Tasks: Research Analytics

- [x] RA-001 Gate-0: provide a config-selected durable S3 path for operator runs that opt into artifact-store publication. Durability/immutability is WE-BUILD via manifest-bound, create-only publication and exact-version hydration; NT's catalog writer is not the publication authority. Backtest reads stay NT through the sole hydrated local-catalog binding.
- [x] RA-002 Define point-in-time feature join rules and leakage fixtures.
- [x] RA-003 Implement leakage-check fixtures that fail closed on future-data access.
- [x] RA-004 Implement a thin reader helper (~dozens LOC) over NT typed query<T> (instrument + time + SQL where pushdown) and DataBackendSession (DataFusion SQL -> Arrow) to expose catalog data to research notebooks without duplicating NT's query surface.
- [x] RA-005 Define notebook read-only boundary and prohibited runtime actions.
- [x] RA-006 Add a Cython-engine-import boundary test that fails if any notebook or research module imports `nautilus_trader.backtest.engine` or `.node`; the ONE Rust BTE is the only permitted backtest runner.
- [x] RA-007 Lift the lead-lag measurement lane off raw archive reads and onto NT catalog reads once Gate-0 (RA-001) is complete; document the raw-archive fallback sunset tied to issue #677 (converter ts_init = capture_time).
- [x] RA-008 Implement sweep orchestration: write N typed run-spec TOMLs, invoke the existing BTE entrypoint in sequence — each run also supplies the accepted object bytes whose sha256 the run-spec pins (operator::run_from_run_spec(spec, gz_bytes, output_dir); CLI --object-gz), so it is not "TOML in, result out" — and collect each persisted BacktestResultContract per run; RA owns no runner of its own. (NT's in-process BacktestResult is #[derive(Debug)]-only and never persisted; RA, out-of-process, reads the Contract JSON the entrypoint writes.)
- [x] RA-009 Implement Polymarket cost realism as FeeModel / FillModel / LatencyModel trait impls registered with the BTE; these are the first sweep inputs that reflect real venue economics.
- [x] RA-010 Implement domain metrics as PortfolioStatistic trait impls registered via the PortfolioAnalyzer::register_statistic method BEFORE the run and run IN-PROCESS during the backtest (the trait methods take live &Returns / Vec<Box<dyn Order>> / &[Position] per crates/analysis/src/statistic.rs, not the flat stat maps the persisted BacktestResultContract carries — do not re-run statistics over the persisted contract). Post-hoc domain metrics are computed from the Contract aggregates, or require the BTE to explicitly export per-period returns / OrderFilled / PositionOpened series. Do not duplicate metrics already present in NT PortfolioAnalyzer.
- [x] RA-011 Implement the Findings & Promotion model (model defined in ../reference/data-model.md ResearchAnalyticsArtifact (experiment-results verdict fields); requirement in spec.md Findings & Promotion; approach in plan.md Verdict And Re-Measurement Rules): record each RA verdict per that field set so a verdict cannot silently go stale; add the conditional promotion gate that, ONLY when a real GO finding exists, populates the typed promotion-config field/URI on the experiment-results artifact (per ../reference/data-model.md / ../reference/contracts.md Result And Promotion Boundary) — never a separate artifact/family/path, never auto-merge, auto-enable a strategy, schedule live trading, touch SSM credentials, or mutate production runtime config. There is no standing PromotionPackage machine. The verdict is persisted on the experiment-results artifact and committed into the Artifact Index per RA-012. Add a test that the gate stays inert with no GO finding, that no separate promotion-config artifact/family/path is emitted, and that a recorded verdict carries the required field set (per ../reference/data-model.md).
- [x] RA-012 Implement the producer-owned Artifact Index commit for every RA-owned artifact family (datasets, feature-tables, experiment-results) per ../reference/data-model.md (ResearchAnalyticsArtifact) + ../reference/contracts.md (Artifact Index Contract + Artifact Lifecycle Contract). The verdict and any promoted promotion-config ride on the experiment-results artifact and commit the same way. Add a test that a persisted RA artifact — including a verdict-bearing experiment-results — has a committed index snapshot + recorded lifecycle state, and that consumers cannot mutate RA index records.
- [x] RA-013 Implement a thin run-id -> params -> result-pointer index over catalog.list_backtest_runs; single artifact_root + sha256 content hashing. This thin run-pointer helper carries no lifecycle metadata or promotion state — that scoping is for this convenience index only, NOT for the experiment-results Artifact Index commits in RA-012 (see plan.md Verdict And Re-Measurement Rules).
- [x] RA-014 Wire off-the-shelf BI surface: duckdb / polars reads over NT catalog Arrow output (NT reporter.py / tearsheet.py are Python (pandas) analysis APIs excluded because the single-engine invariant keeps RA on the Rust path and bans importing NT's Python/Cython engine layer — not because they need a live cache; the reporter.py helpers take plain list[Order]/list[Position] and create_tearsheet_from_stats takes precomputed stats, only the engine-driven create_tearsheet needs engine.kernel.cache); add notebook ergonomics for the dashboard product; custom UI only if off-the-shelf cannot satisfy a confirmed requirement.
- [x] RA-015 Add a test that analytics venue/provider resolution and feature joins do not branch on hardcoded venue/provider names; one test, not a >=2-binding fixture obligation.
- [x] RA-016 Document the known prerequisite: the BTE runner today registers only an NT example strategy (HurstVpinDirectional) over one venue (bybit-spot); bolt's binary_oracle_edge_taker + venue normalization must be wired into the BTE before Phase-3 sweeps produce valid results.
